# Backend Audit — Fixes Tracker

Tracks progress against every finding in `BACKEND_AUDIT.md` (2026-07-13 full backend audit). Item numbers match that file exactly. Updated as work lands — do not renumber; add new items at the end of their section if new gaps are found later.

**Status at a glance:** 16 of 23 items fixed (all 5 🔴 critical items, all operational items but one, 3 of 6 major product-feature items — #7/#8/#9 partially or fully done — and 1 of 4 minor items, #20), 7 remaining (3 major product features, 1 operational, 2 minor, plus the deferred sub-parts of #8).

---

## ✅ Fixed (2026-07-13)

| # | Item | What was done | Files |
|---|---|---|---|
| 1 | HEIC decoding broken on Linux production | New cross-platform WASM fallback decoder (`heic-convert`/`libheif-js` — no native dependency, works identically on macOS and Linux, unlike the old macOS-only `sips` fallback). Verified against the EXACT real file that originally reproduced the bug: sharp still throws `"iref box (48) exceeds... 16"`, the new WASM tier decodes it cleanly into a valid 3024×4032 JPEG. Live-verified end-to-end through the real worker pipeline (reclassify): the photo went from `sharp failed` → WASM success → classified → filed into "Person 1", no error. EXIF extraction still only has the macOS-only `sips` fallback (smaller, separately-disclosed gap — doesn't block thumbnails/classification the way decode failure did). | `backend/src/worker.ts` (new `decodeHeicViaWasm`, 3-tier `decodeToJpeg`), `backend/src/types/heic-convert.d.ts` (new, no official types package exists), `backend/package.json` |
| 2 | No `trust proxy` setting | `app.set("trust proxy", 1)` in production only | `backend/src/app.ts` |
| 3 | CSRF gap in production | Origin/Referer validation middleware on all non-GET routes in production, checked against the existing CORS allow-list | `backend/src/app.ts` |
| 4 | No account deletion path | `DELETE /api/auth/me` — requires re-entering the current password, rate-limited like login. Order: best-effort S3 cleanup for every photo (`purgeAllStorageForOwner`, new) → best-effort Rekognition face-collection delete (`deleteFaceCollection`, new `DeleteCollectionCommand` — never called anywhere before this) → audit row written (`account_deleted`, survives the deletion — `AuditLog` has no FK to `User`) → `prisma.user.delete()`, which cascades every other DB row (photos/folders/collections/sessions/guest data) in one transaction via existing `onDelete: Cascade` relations. Live-verified via real HTTP calls: wrong password → 401 (nothing touched), correct password → `{"deleted":true}`, session cookie dead immediately after (401 on `/me`). 4 new tests cover the full contract including cascade + audit-row survival. | `backend/src/routes/auth.ts` (new `DELETE /me`), `backend/src/lib/purge.ts` (new `purgeAllStorageForOwner`), `backend/src/lib/classification/faces.ts` (new `deleteCollection`/`deleteFaceCollection`), `backend/src/lib/audit.ts` (new `account_deleted` action + `user` resource type), `backend/src/__tests__/auth.smoke.test.ts` |
| 5 | No env validation at boot | New `validateEnv()` — fails fast in production with a full list of every missing required var, branched by which real providers (S3/Rekognition/Resend) are actually enabled | `backend/src/lib/validateEnv.ts` (new), called from `server.ts` + `worker.ts` |
| 12 | Unbounded table growth (sessions, guest sessions, invite tokens, access requests, processing jobs) | New housekeeping sweep, piggybacked on the existing daily auto-purge job — no new scheduling infrastructure | `backend/src/lib/trashPurgeJob.ts` |
| 13 | Rate-limit coverage gaps (guest portal unprotected, no global fallback) | New guest-portal limiter (keyed by guest session id) applied to all 6 guest routes; new generous global fallback limiter (production only) | `backend/src/middleware/guestRateLimiter.ts` (new), `backend/src/routes/guest.ts`, `backend/src/app.ts` |
| 14 | Ops visibility — `/health` checked nothing, no worker heartbeat, console-only logging, no stale-pending-photo sweep | **Four parts, all shipped:** (a) `/health` now does a real `SELECT 1` + Redis `PING`, returns 503 if either fails, live-verified (`{"status":"ok","checks":{"database":"ok","redis":"ok",...}}`); (b) worker writes a heartbeat to Redis every 15s, `/health` surfaces it (`worker.status`/`lastSeenMsAgo`) — live-verified refreshing in real time; (c) a photo stuck `pending`/`processing` >1hr with no active `processing_jobs` row now gets reconciled to `failed` (existing Reclassify button is the recovery path — no new UI needed), added to the same daily job, 2 new tests; (d) **all 25 `console.*` call sites across the backend replaced with structured `pino` logging** (JSON in prod, pretty-printed in dev) — hit and fixed a real `tsx`-specific bug along the way: modules reached transitively before `dotenv.config()` runs saw `NODE_ENV` as `undefined`, permanently deciding "not dev" — fixed by making the logger's dev/prod decision lazy (computed on first real log call, not at import time), confirmed live. | `backend/src/app.ts` (`/health`), `backend/src/lib/workerHeartbeat.ts` (new), `backend/src/lib/trashPurgeJob.ts` (stale-photo reconciliation), `backend/src/lib/logger.ts` (new), `backend/src/worker.ts`, `backend/src/server.ts`, `backend/src/lib/{audit,validateEnv,purge,notifications/index}.ts`, `backend/src/lib/classification/faces.ts`, `backend/src/__tests__/health.smoke.test.ts` (new), `backend/src/__tests__/trash-system.smoke.test.ts` |
| 15 | No graceful shutdown on the API | `SIGTERM`/`SIGINT` handlers: stop accepting connections, drain in-flight requests (10s timeout), disconnect Prisma. Also added the missing `prisma.$disconnect()` to the worker's existing shutdown handlers | `backend/src/server.ts`, `backend/src/worker.ts` |
| 17 | Misleading guest telemetry (`lastUsedAt` never updated after session creation) | Fire-and-forget update on every authenticated guest-portal request | `backend/src/middleware/requireGuest.ts` |
| 18 | No JSON 404 handler | Catch-all `{ error: "Not found" }` handler after all routes, before the global error handler. Verified live: `curl /api/does-not-exist` → `{"error":"Not found"}` | `backend/src/app.ts` |
| 21 | No password max-length | Added `max(72)` (bcrypt silently truncates beyond 72 bytes) | `backend/src/lib/validation.ts` |
| 7 | Billing / plan enforcement — free-tier 3-guest limit unenforced | **Guest-count enforcement only** (no billing — Stripe stays deferred per CLAUDE.md). `checkGuestLimit()` reads `users.plan` (finally — the field existed since the original schema but was never read anywhere) and counts ACTIVE guests (live, non-revoked folder permission) for `POST /api/guests`; free tier capped at 3, returns 402 with a clear message once hit. Revoking a guest frees their slot — proven by a live-DB test that hits the limit, revokes one, and confirms a new invite succeeds again. | `backend/src/lib/plans.ts` (new), `backend/src/routes/guests.ts`, `backend/src/__tests__/plans.smoke.test.ts` (new) |
| 8 | Auth surface incomplete — no password reset, profile update | **Password reset + profile update shipped; magic link, email verification, and Google OAuth deliberately deferred** (each needs its own scope — OAuth needs a registered external app, the others are smaller independent features, not blockers bundled with this batch). Shipped: `POST /api/auth/forgot-password` (always 200, anti-enumeration, mints a 1-hour single-use token, emails a reset link) + `POST /api/auth/reset-password` (redeems the token, revokes EVERY session for the user — there's no "current session" to preserve when you couldn't log in); `PATCH /api/auth/me` (name); `POST /api/auth/change-password` (requires current password, revokes every OTHER session but keeps the current one alive — the "someone else has my password" response). New `PasswordResetToken` table, same opaque-token/SHA-256-hash pattern as every other token surface in this app. Live-verified via real HTTP calls (anti-enumeration 200, profile rename). | `backend/prisma/schema.prisma` (new `PasswordResetToken` model + migration), `backend/src/routes/auth.ts`, `backend/src/lib/validation.ts`, `backend/src/__tests__/auth.smoke.test.ts` |
| 9 | Guest invite emails missing | **Revised per Abhishek's explicit call**: NOT auto-sent on creation. `POST /api/guests` only generates the link (unchanged response shape); a separate `POST /api/guests/:id/send-invite { inviteUrl }` fires the actual email — meant to back a frontend "Send" button that appears once the link is generated, so the owner decides whether/when to email it (vs. sharing it some other way). The backend never persists the raw invite token (only its SHA-256 hash, same as every other token in this app), so the frontend passes the inviteUrl it already has back to this endpoint rather than the backend re-deriving it. Safe to call more than once (resend). Rate-limited (20/15min), 404s for a guest that isn't yours. Live-verified: real Resend call correctly attempted and cleanly reported a 502 (Resend's sandbox sender only delivers to the account owner's own inbox, a known limitation from earlier this session — not a bug in this code); the 404-ownership path verified directly; the full send/resend contract verified via 3 automated tests against the mock provider. | `backend/src/routes/guests.ts` (new `POST /:id/send-invite`), `backend/src/lib/validation.ts` (new `sendGuestInviteSchema`), `backend/src/lib/notifications/index.ts` (`sendGuestInvite`/`GuestInviteMessage`, unchanged from the original shipped version), `backend/src/__tests__/plans.smoke.test.ts` |
| 20 | `bcryptjs` → native `bcrypt` swap | Confirmed the native build compiles cleanly in this environment (the roadmap's flagged risk) — swapped `import bcrypt from "bcryptjs"` to the native `bcrypt` package in the one file that used it (`routes/auth.ts`), removed `bcryptjs`/`@types/bcryptjs`. Zero-migration: native `bcrypt` produces the same `$2b$` hash format as `bcryptjs`, so every existing password hash in the DB keeps verifying correctly — no backfill, no forced re-login. Fixes the real event-loop-blocking risk under concurrent login bursts flagged in `SCALABILITY_ROADMAP.md`. Live-verified: fresh signup hashes correctly, wrong password 401s, correct password 200s. | `backend/src/routes/auth.ts`, `backend/package.json` |

**Verification for all 15:** `npx tsc --noEmit` clean (backend + frontend), full backend suite **268/268 passing**. App restarted and confirmed live end-to-end: anti-enumeration `forgot-password` hit directly (200 for an unknown email), profile rename hit directly and confirmed via a follow-up `/me` call, guest send-invite hit directly (real Resend attempt + correct error handling, plus the 404-ownership check). 907 stale failed BullMQ jobs (accumulated across this session's test runs against the same dev Redis) cleaned up as a side effect of an earlier restart.

**Frontend still needed for everything in this batch (backend-only so far):** delete-account UI, forgot/reset-password pages, profile settings (rename + change password), guest-limit error surfacing, and — the one with an explicit product decision already made — a **"Send" button** on the Guests page that appears once an invite link is generated, calling the new `send-invite` endpoint on click.

---

## 🟠 Major product gaps — still open (all require their own Planner spec, not a quick fix)

| # | Item |
|---|---|
| 6 | Cloud export (Drive/Dropbox) — entirely missing |
| 8 (remainder) | Email verification, magic-link login, Google OAuth — deliberately deferred from this batch (OAuth specifically needs a registered external app + credentials, similar to the AWS/GCP walkthroughs earlier) |
| 10 | Multi-collection workflow doesn't exist (no collection CRUD beyond list) |
| 11 | AI labels aren't searchable or editable as tags |
| 7 (remainder) | Full Stripe billing (upgrading `users.plan` itself) — deferred per CLAUDE.md until explicitly approved; only guest-count enforcement shipped so far |

## 🟡 Operational hardening — still open

| # | Item |
|---|---|
| 16 | Orphaned S3 objects — no reconciliation job in either direction |
| — | *(#12, #13, #14, #15, #17 — every operational item from the original audit — are now fixed above)* |

## 🟢 Minor polish — still open

| # | Item |
|---|---|
| 19 | No API versioning / OpenAPI docs (not urgent — single first-party frontend) |
| 22 | No pre-upload duplicate-check endpoint |
| 23 | CI pipeline — *already tracked separately* in `dashboard/suggestions.json` (`ci-pipeline`, backlog tier) |

---

## Suggested next batch

Every 🔴 critical item and every 🟡 operational item except one (#16, orphaned S3 reconciliation — explicitly low-urgency) is fixed, and #7/#8/#9 are now partially or fully shipped. What's left is either (a) genuinely large net-new features needing external setup — cloud export (#6, Drive/Dropbox OAuth apps), Google OAuth login (rest of #8), full Stripe billing (rest of #7) — or (b) medium, self-contained features that could be the next batch without new external dependencies: **multi-collection CRUD (#10)**, **label search + tag editing (#11)**, or **email verification + magic-link login** (the smaller remaining pieces of #8, reusing the exact `PasswordResetToken`-style pattern just built).

**Product-facing follow-ups worth flagging:**
- #4's backend endpoint (`DELETE /api/auth/me`) has no frontend UI yet — purely a backend capability today, reachable via API only.
- Same is now true for #7/#8/#9 from this batch: `POST /api/auth/forgot-password`, `POST /api/auth/reset-password`, `PATCH /api/auth/me`, `POST /api/auth/change-password` all work but have no frontend page/form yet.
- The dev-only `tsx`/`dotenv` load-order bug found and fixed while building #14d (a module reached transitively before `dotenv.config()` runs sees `process.env` as if `.env` were never loaded) is worth keeping in mind for any FUTURE module that reads `process.env` at top-level/module-load time — the safe pattern demonstrated in `lib/logger.ts` is to defer the read into a function/lazy-getter rather than a top-level `const`.
