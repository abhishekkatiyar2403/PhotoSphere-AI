# Test Report — 2026-07-05 20:25

**Run type:** manual (focused fix-verification pass — the two Guest Access UI bugs from `reports/2026-07-05_2015.md`, after Developer's fix commit `c27c09d`)
**Dev server:** backend localhost:4000 — reachable: yes (`/health` → `{"status":"ok"}`). Frontend localhost:3000 — reachable: yes (`/guests` → 200, `/g/<anytoken>` → 200). `NODE_ENV=development` + `NOTIFICATIONS_EXPOSE_OTP="true"` confirmed live. Docker Postgres/Redis/MinIO healthy (backend + worker consuming).
**Tested against commit:** `9ad65de` (docs commit recording the fix `c27c09d` on `feature/ai-classification` — "fix: guest-access frontend BUG-1 + BUG-2"). Verified `git show --stat c27c09d` touches ONLY `frontend/src/app/guests/page.tsx`, `frontend/src/app/g/[token]/page.tsx`, plus docs/MR — **zero backend files** (as claimed).
**Features in scope:** the two fixed frontend files only. BUG-1 (wrong OTP must not eject the owner) re-verify, BUG-2 (returning approved guest + spent-invite message) re-verify, the privacy constraint re-verify (must not have been reintroduced by the new load-time probe), plus a light regression across all pages and the full backend suite.

**Method note:** No standalone Playwright/`@playwright/test` is installed in this repo (only Next's bundled testmode wrapper). I installed `playwright-core` into the scratchpad (no browser download) and drove the **installed system Chrome** headless via CDP — the same practical outcome as Playwright for regression: real navigation, real clicks, DOM assertions, and full network-request inspection. Owner/guest fixtures were seeded directly via Prisma with **known raw tokens** (I control the SHA-256 hashing), so all owner-session and guest-session cookies are genuine and every assertion below is black-box over real HTTP through a real browser — no mocking of the app. This also dodges the signup/login rate limiters (documented benign class) for setup while keeping the actual assertions honest. All seeded fixtures were deleted after the run.

---

## Summary

Passed: **30 / 30** browser + HTTP assertions, plus backend suite **71 / 71**.
Failed: **0** (0 critical, 0 high, 0 medium, 0 low).

**Verdicts:**
- **BUG-1 [was High] — FIXED, verified.** A wrong OTP no longer ejects the owner; it shows "Invalid code" inline and the owner stays on `/guests`. The 3-wrong auto-deny state is now reachable and surfaces (request drops out of pending). Happy path still works. Genuine owner-session-loss still redirects to `/login` (not ripped out).
- **BUG-2 [was Medium] — FIXED, verified.** A returning approved guest (live guest session) lands directly in the **unlocked** state via the new load-time probe — no "Request access" click, no 404 dead-end. Spent-invite now shows a clear "already used / ask for a new link" message.
- **PRIVACY CONSTRAINT — STILL HOLDS, verified adversarially via network inspection.** The new load-time probe did NOT reintroduce any leak. An unapproved visitor's ONLY `/api/*` call is the probe `GET /api/guest/folders` (401 → stays locked); **zero** real guest-data calls, **zero** image/thumbnail/pre-signed resource loads, **zero** `<img>` elements (6 decorative tiles only).
- **Backend suite:** 71/71, unchanged.

**Overall verdict: the Guest Access UI is now SHIP-READY.** Both ship-blocking/flagged bugs are genuinely fixed, the privacy constraint that mattered most is intact, and nothing regressed.

---

## BUG-1 re-verify [was High] — wrong OTP must NOT eject the owner

**Backend contract confirmed first (curl, owner session cookie):**

| Call | Response |
|---|---|
| wrong OTP attempt 1 (`POST /api/access-requests/:id/approve` `{otp:"000000"}`) | `401 {"error":"Invalid code"}` |
| wrong OTP attempt 2 (`{otp:"111111"}`) | `401 {"error":"Invalid code"}` |
| wrong OTP attempt 3 (`{otp:"222222"}`) | `403 {"error":"Request denied after too many incorrect codes"}` |
| list pending after 3rd wrong | `{"requests":[]}` — auto-denied request gone from pending |

**Frontend behavior confirmed in a real browser (headless Chrome, seeded owner session cookie), 9/9:**

| Assertion | Result |
|---|---|
| Pending request card + OTP input renders on `/guests` | Pass |
| After wrong OTP #1, owner STAYS on `/guests` (no navigation) | Pass — `url` stays `/guests` |
| NOT redirected to `/login` or `/dashboard` | Pass — nav log = `["/guests","/guests"]`, no bounce |
| Inline **"Invalid code"** error shown on the request card | Pass — `.guests-pending-error` text = "Invalid code" |
| Request card still present — owner can retry | Pass |
| After wrong OTP #2, still on `/guests` | Pass |
| After wrong OTP #3 (backend 403 auto-deny), still on `/guests` (NOT ejected) | Pass — **auto-deny is now reachable** where before the 1st wrong code already ejected the owner |
| 3rd-wrong auto-deny surfaced: request dropped out of pending (frontend's `load()` on 403) | Pass |
| No page errors during the whole wrong-OTP flow | Pass |

**Happy path + session-loss sanity (4/4):**

| Assertion | Result |
|---|---|
| Correct OTP → Approve → owner stays on `/guests`, approved request removed from pending stream | Pass |
| Genuine owner-session loss: `/guests` with **no** owner session cookie → redirect to `/login` | Pass — the redirect was NOT ripped out; it still fires where a 401 genuinely means "owner logged out" |

Root cause of the original bug is closed exactly as described: `handleApprove`/`handleDeny` no longer carry the blanket `isAuthError`→`/login` branch (confirmed by reading `frontend/src/app/guests/page.tsx` lines 100–142); the `isAuthError`→redirect is retained only on the `authApi.me()` gate, the `load()` list calls, and `handleRevoke`.

---

## BUG-2 re-verify [was Medium] — returning approved guest + privacy intact

**Returning approved guest (real browser, live guest-session cookie), 3/3:**

| Assertion | Result |
|---|---|
| Loading `/g/:token` with a live guest session lands DIRECTLY in the **unlocked** state — no "Request access" click | Pass (via the new load-time `GET /api/guest/folders` probe → 200 → `unlocked`) |
| Unlocked state shows the scoped folder (Nature) | Pass |
| No landing / Request-access 404 dead-end for the returning guest | Pass |

**Spent-invite message (real browser, no session, exhausted single-use invite), 4/4:**

The exhausted invite (`useCount=1 >= maxUses=1`) returns `404 {"error":"Invite not found"}` over HTTP (confirmed by curl; the invite rate limiter did NOT shadow it this window — bucket had headroom since setup was seeded via Prisma). In the browser:

| Assertion | Result |
|---|---|
| Probe 401 → locked landing state | Pass |
| An inline error message is shown on 404 (no crash / no blank) | Pass |
| Message is the clearer copy: **"This invite link has already been used or is no longer active. Ask the owner to share a new link."** | Pass |
| Stays on landing, no crash | Pass |

---

## PRIVACY CONSTRAINT re-verify (CRITICAL) — verified adversarially via network inspection

**VERDICT: PASS. The new load-time probe did NOT reintroduce a leak. No real image data loads pre-approval.**

Unapproved visitor: `/g/:token` with **no** cookies of any kind, network fully instrumented (every request logged; images matched by `resourceType==="image"` and by `X-Amz`/`:9000`/`minio`/`amazonaws`/`/thumb/`/`/orig/`/`presign`/`download` URL patterns). 6/6:

| Assertion | Method | Result |
|---|---|---|
| Unapproved visitor lands in the **landing** state (Request access CTA shown, not unlocked) | DOM | Pass |
| The ONLY `/api/*` call pre-approval is the load-time probe `GET /api/guest/folders` | full request log | Pass — every logged `/api/*` request was `GET /api/guest/folders`, nothing else |
| ZERO real guest-data calls (no `folders/:id/photos`, no `photos/:id`, no `download`) | request log filter | Pass — 0 |
| ZERO real image/thumbnail/pre-signed resource requests | image-resource filter | Pass — 0 |
| Locked grid renders ZERO `<img>` elements | `img` count = 0 | Pass |
| Decorative locked tiles present | 6 `portal-locked-tile` | Pass |

The probe's 401 (`{"error":"Unauthorized"}` over HTTP for a no-session visitor) keeps the guest on the decorative locked grid — exactly the pre-fix behavior, plus the new "already have access?" check that only ever *succeeds* for a genuinely-approved guest. "Zero real image data before approval" holds exactly as originally built.

---

## Regression (light — the fix touched 2 page files), 8/8

| Area | Test | Result |
|---|---|---|
| Dashboard | `/dashboard` loads (200), no pageerrors | Pass |
| Dashboard | "Guests" top-bar link present (`.dashboard-guests-link`) | Pass |
| Organize | `/organize` loads, no pageerrors | Pass |
| Browse | `/browse` loads, no pageerrors | Pass |
| Upload | `/upload` loads + file input present, no pageerrors | Pass |
| Login | `/login` renders email + password form (logged out), no pageerrors | Pass |
| Guests | `/guests` loads (200), no pageerrors | Pass |
| Guest portal | `/g/:garbage-token` loads public (no `/login` redirect), no pageerrors | Pass |

**Backend suite:** `npm test -w backend` → **71 / 71 passed** (8 files, ~27s). Unchanged from the last two runs. The lone "Unique constraint failed on (collection_id, name)" log line printed at the top is the **expected P2002 catch-and-refetch** in the race-safe concurrent-folder-creation test (its test passed) — the same benign log documented in every prior report, not a failure.

---

## New Failures

None.

---

## Notes for Developer Agent

- **Both bugs are genuinely closed, not just trusted from the MR draft.** BUG-1 was re-driven through a real browser with wrong codes (the exact case the original build never exercised): the owner stays put and sees "Invalid code" inline, and — importantly — the auto-deny state that was previously *unreachable* (because the 1st wrong code ejected the owner) is now reachable and surfaces correctly on the 3rd wrong attempt. BUG-2's returning-guest probe works and, verified adversarially, did not open a privacy hole.
- **Benign harness classes (NOT app bugs), for the next Tester:**
  - The load-time probe `GET /api/guest/folders` fires **twice** on mount in dev — this is React 18 StrictMode's intentional double-invoke of effects in `next dev`, not a bug. Both are the same harmless 401 folders-list call with no image data; in a production build the effect runs once. Not counted against the privacy assertion (0 data calls either way).
  - The invite-request rate limiter (10/15min/IP) is armed (`NODE_ENV=development`) and will cascade 429s on repeated `POST /api/invites/:token/request`. I avoided it by seeding fixtures via Prisma and hitting the request endpoint only where needed; the spent-invite 404 landed cleanly this window. If a future run sees the spent-invite test show "Too many requests" instead of the 404 message, that's the limiter shadowing the 404 — the frontend's 404→message branch is confirmed present (`g/[token]/page.tsx` lines 152–158) and was hit live this run.
  - Pre-login `auth/me` 401s on owner pages before `authApi.me()` resolves are the same benign first-paint pattern; not counted.
- **Housekeeping:** process tree clean — exactly one `server.ts` (watcher + child), one `worker.ts` (watcher + child), one `:4000` listener, one `:3000` listener; no stale duplicates. All seeded test owners/guests deleted after the run (6 owners cascaded away). No throwaway scripts left in the repo (test scripts live in the session scratchpad, outside the repo). Untracked repo files observed but NOT created by me: `Day3.md`, `dashboard/`, `reports/2026-07-05_2015.md`.
