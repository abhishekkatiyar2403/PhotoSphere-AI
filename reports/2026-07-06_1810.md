# Test Report — 2026-07-06 18:10

**Run type:** manual (Tester dispatch — P4/P5/P6 deferred-feature backend regression)
**Dev server:** backend localhost:4000 — reachable: yes (`{"status":"ok"}`). Frontend/browser NOT touched this pass (backend-only, by design — avoids the two prior 600s harness stalls).
**Tested against commit:** `d3ce81e` (branch `feature/ai-classification`; feature commits P4 `057605c` + `/unfiled` fix `9664b6b`, P5 `3e7683f`, P6 `1c7ba12` all present)
**Features in scope (new/changed since last report):** P4 folder rename/merge/delete (`routes/folders.ts`), P5 bulk zip download-all (owner + guest, `folderZip.ts`/`folderDownload.ts`/`storage.getObjectStream`), P6 basic search (`routes/search.ts`). All three are backend-only (UIs wireframe-blocked / thin button — not built).

---

## Summary

Passed: **208 / 208** (137 automated suite + 71 live black-box HTTP/DB/MinIO). Failed: 0 (0 critical, 0 high, 0 medium, 0 low).

- **Priority 0 — full backend automated suite:** `npm test -w backend` → **137/137 green** (12 files, 31.7s), the expected count. Zero flake.
- **71 live black-box assertions** over HTTP :4000 with Prisma/MinIO seeding (owners, guests, folders, photos with real MinIO objects, permissions, sessions minted directly). Every fetch hard-timed at 10s. All seed data + throwaway scripts cleaned up; **0 residual `regr7test` rows** confirmed post-run.

**Ship-ready verdict per part: P4 SHIP-READY, P5 SHIP-READY, P6 SHIP-READY** (backend). No bugs, no security holes.

---

## Regression Suite Results

### Priority 0 — automated backend suite
| Area | Test | Expected | Actual | Result |
|---|---|---|---|---|
| All | `npm test -w backend` | 137/137 | 137 passed (12 files) | Pass |

The `prisma:error` "Unique constraint (collection_id,name)" log during `classification.smoke.test.ts` is the deliberate P2002 collision assertion (known harness artifact) — not a failure.

### Priority 1 — P4 folder rename / merge / delete (25 live assertions)
| Test | Expected | Result |
|---|---|---|
| Rename owned folder | 200 + DB name updated, photoCount/categoryType untouched | Pass |
| Rename to colliding name (same collection) | 409 (caught P2002) | Pass |
| Rename empty/whitespace | 400 before any DB write | Pass |
| Rename cross-owner folder | 404, no change | Pass |
| Merge A(3)→B(2) | 200, `photosMoved:3`, `targetPhotoCount:5`, A row deleted, no photo points at A | Pass |
| Merge count EXACT (no drift) | `B.photoCount == live COUNT(folderId=B) == 5` | **Pass** |
| Merge into self | 400 | Pass |
| Merge into cross-owner target | 404 | Pass |
| `folder_merged` audit row | exactly 1, actorType owner, correct ownerId | Pass |
| Delete owned folder w/ 1 done photo | 200, `photosOrphaned:1`, folder row gone | Pass |
| Delete → photo `folderId=null` (Unfiled) | DB-confirmed null | Pass |
| Delete → photo row + `s3Key` survive (no cascade) | Pass | Pass |
| **Delete → done orphan REACHABLE via `GET /api/photos/unfiled`** | orphan id present in unfiled list | **Pass (the `9664b6b` reachability fix works live)** |
| Delete → MinIO original survives (`GET /api/photos/:id` 200) | Pass | Pass |
| `folder_deleted` audit row | exactly 1 | Pass |
| Delete cross-owner | 404 | Pass |

**F1 GUARD (load-bearing security) — 8 adversarial, DB-confirmed:**
| Test | Expected | Result |
|---|---|---|
| Merge of live-shared folder (guest `view` perm) | 409 | **Pass** |
| ...and NOTHING moved (2 photos + folder row intact, DB-confirmed) | Pass | **Pass** |
| Delete of same live-shared folder | 409 | **Pass** |
| ...and NOTHING deleted (2 photos + folder row intact, DB-confirmed) | Pass | **Pass** |
| After revoking the guest perm → merge now succeeds | 200, 2 photos moved | Pass |
| Second folder shared w/ `download_all` → delete blocked 409 | 409 | Pass |
| ...after revoke → delete now succeeds | 200 | Pass |

The F1 guard runs FIRST (before any data move) and blocks both merge and delete with 409 while any live (`revokedAt=null`, non-expired) `folder_permission` points at the source; revocation cleanly re-enables both. Verified adversarially and DB-confirmed no partial mutation.

### Priority 2 — P5 bulk zip (17 live assertions)
| Test | Expected | Result |
|---|---|---|
| Owner `GET /api/folders/:id/download-all` | 200, `Content-Type: application/zip`, `Content-Disposition: attachment` | Pass |
| Body is a valid zip (PK local sig + EOCD central-directory parse, no unzip dep) | valid | Pass |
| Zip contains exactly the downloadable photos (3; `failed` + `duplicate` excluded per Z4) | 3 entries | Pass |
| Duplicate original filename de-duplicated (3 distinct entry names) | Pass | Pass |
| Entry names are original filenames, not `ownerId/…/original.jpg` keys | Pass | Pass |
| **NO RAW KEY LEAK** — scanned full body + all headers for `ownerId/`, `original.jpg`, `X-Amz-`, `http://minio`, `:9000`, `.amazonaws.com` | 0 matches | **Pass** |
| Owner cross-owner folder | 404 | Pass |
| Owner empty folder / no-downloadable folder (only failed) | 400 | Pass |
| **Guest Z1 gate:** `download_all` guest | 200 zip | **Pass** |
| Guest `download`-only | 403 | **Pass** |
| Guest `view`-only | 403 | **Pass** |
| Guest folder outside permitted set | 404 | Pass |
| Guest no session | 401 | Pass |
| **Z7 audit:** exactly ONE `folder_downloaded` row for the successful guest zip; the 403/404/401 refusals wrote NONE | delta == +1 | **Pass** |

### Priority 3 — P6 search (18 live assertions)
| Test | Expected | Result |
|---|---|---|
| **LEAK-PROOF `q`:** owner A's `?q=beachsunset` returns ONLY A's 3 photos (all `-A*`) | Pass | **Pass** |
| ...owner B's same query returns ONLY B's 2 photos (all `-B*`) | Pass | **Pass (both directions)** |
| Result carries pre-signed thumb field, no `s3Key` in payload | Pass | Pass |
| `q` case-insensitive (uppercase substring) | 3 results | Pass |
| `?folderId=<owned>` restricts to that folder | 1 result | Pass |
| **`?folderId=<B's folder>` from A** | 404 (no leak) | **Pass** |
| `?folderId=unfiled` → `folderId=null` photos only | 1 (the unfiled one) | Pass |
| `?category=Nature` → only A's own Nature-folder photos (owner-scoped, not B's Nature) | 1 (`-A1`) | **Pass** |
| Unknown `?category=Bogus` | 400 (Zod enum) | Pass |
| `?from`/`?to` range covering today | A's 3 photos | Pass |
| `?from>to` | 400 | Pass |
| Invalid date (`from=notadate`) | 400 | Pass |
| Past-only range | 0 results | Pass |
| Combined `?q=…&category=Nature` ANDs | 1 (`-A1`) | Pass |
| `?limit=101` | 400 (house rule, not clamp) | Pass |
| Garbage `?limit=abc` | 400 | Pass |
| Empty query → whole A library newest-first, only A's (3) | Pass | Pass |
| No session | 401 | Pass |

### Priority 4 — regression spot-checks (3 live assertions)
| Test | Expected | Result |
|---|---|---|
| Guest folder listing `GET /api/guest/folders` | 200 | Pass |
| Guest photos listing on a permitted folder | 200 w/ photos | Pass |
| Owner `GET /api/collections` | 200 | Pass |

The refused-access-writes-no-audit-row property (`403/404` guest bulk refusals → 0 `folder_downloaded` rows) is confirmed exactly by the P5 Z7 count assertion above (only the one success incremented). The full guest-access flow + audit-trail behavior is additionally covered by the 137-test suite (audit hooks, `photo_downloaded` no-row-on-403, leak-proof `GET /api/audit`).

---

## New Failures

None.

---

## Notes for Developer Agent

- **No bugs, no security holes.** The three load-bearing security items are all verified live and adversarially: the **F1 guard** blocks merge AND delete on a live-shared folder with a DB-confirmed no-mutation (and revocation re-enables both); the **P5 zip leaks no raw key** (full body + header scan clean) and its **Z1 `download_all` gate** correctly 403s `download`/`view` guests; **P6 search is leak-proof both directions** across `q`, `folderId`, and `category`.
- **Method note (no fabrication):** owners/guests/folders/photos/permissions/sessions were seeded DIRECTLY via Prisma + real MinIO `putObject` in a throwaway `.mjs` under the session scratchpad (now deleted); sessions minted by inserting the SHA-256 token hash and using the raw token as the cookie (matches `lib/session.ts`/`lib/guestSession.ts`). Zip validity checked at ZIP-format level (PK local-file signature + End-Of-Central-Directory record + central-directory entry parse) — no unzip dependency, per instruction. Every HTTP call hard-timed at 10s; nothing waited out a rate-limit window; no browser/Playwright/CDP touched (deliberate, to avoid the two prior harness stalls). All seed data cleaned up — **0 residual `regr7test` rows confirmed post-run**.
- **Not a bug — informational:** the dev DB still holds 52 `folder_merged`/`folder_deleted` audit rows plus prior `@example.com` seed accounts from earlier Master/Tester cycles (unrelated to this pass — my seed footprint is fully gone). A clean-slate wipe remains available on request (already flagged in STATUS).
- **Coverage gap (honest):** this was a pure-backend pass — the P4/P6 UIs are wireframe-blocked and P5 is an unbuilt thin button, so there was nothing frontend to exercise. When those UIs land they'll need a browser-DOM pass. The `Developer-verified` acceptance items (merge runs inside `serializableTransaction()` with P2034 retry; zip is streamed not buffered) were confirmed by code review of `folders.ts`/`folderZip.ts`, consistent with the spec's stance that a concurrency race is impractical to force reliably black-box.
