# Test Report — 2026-07-03 0050

**Run type:** manual (evening run dispatched 2026-07-02; interrupted mid-run by a session usage limit and resumed after midnight — all live traffic in this report was generated post-resume on 2026-07-03 between ~00:30 and ~00:50 local, against the identical commit and running stack)
**Dev server:** localhost:3000 (frontend) — reachable: yes (200) | localhost:4000 (backend) — reachable: yes (`/health` → `{"status":"ok"}`)
**Docker Compose:** postgres / redis / minio all `Up (healthy)`. Worker confirmed alive (`tsx watch src/worker.ts` in process list, and every upload progressed `pending → done/duplicate/failed` within seconds without intervention).
**Tested against commit:** `27afa75` — branch `feature/ai-classification` (commits `8f1f065`, `5b8d304`, `97b69a1`, `0395096`, `27afa75` on top of the previously tested `74d1f4b`)
**Features in scope:** Full regression of auth (`specs/auth.md`) and upload pipeline (`specs/upload-pipeline.md`), adapted for the new split auth rate-limit buckets; every **[Tester-live]** acceptance criterion in `specs/ai-classification.md`. Reclassification UI was NOT built (blocked on wireframe pick, by design) — no new frontend page exists or was expected; `/upload` re-verified unchanged.
**Method:** headless Playwright (Chromium 1.61.1) for all browser flows + curl/node-fetch for API-level tests, per `agents/TESTER.md` (this is a VS Code session — no Cowork live browser control). Test images: the committed `backend/test/fixtures/` set (verified byte-for-byte against the README's label table through the real `classify()` + `mapToCategory()` code path before any upload), plus companion images generated per the README/MR recipe (seeded noise, flat rectangles, sharp-`withExif` tagged), each pre-verified offline through the same real code path.

---

## Summary

Passed: 52 / Failed: 0 (0 critical, 0 high, 0 medium, 0 low)

Every [Tester-live] AC in `specs/ai-classification.md` passed. Full auth + upload regression passed, including the two items upgraded from last run's "code-reviewed only" caveats: **EXIF is now live-verified** (new `exif` object on `GET /api/photos/:id`) and **exact `attempts=3` + `errorMessage` on forced failure is now live-verified** (new `job` object on the status endpoint). The flat-image pHash false positive found on 2026-07-02 is **confirmed fixed** via the exact red/blue repro. Two items remain not independently live-verifiable (storage quota, worker kill/restart durability — same environment constraints as both prior runs, detailed below). Items marked [Developer-verified / code-review-only] in the spec's legend were confirmed by code review only and are listed explicitly — none were live-tested, none fabricated.

---

## Regression Suite Results

### Auth (specs/auth.md — regression, adapted for split buckets)

| # | Test | Steps | Expected | Actual | Result |
|---|---|---|---|---|---|
| 1 | Unauthenticated `/dashboard` | Fresh browser context → `/dashboard` | Redirect to `/login` | Redirected | Pass |
| 2 | Signup via UI | Fill `/signup` form (fresh account A), submit | Land on `/dashboard`, welcome shown | "Welcome, Classification Tester A" | Pass |
| 3 | Session cookie flags | Inspect `document.cookie` + context cookies | HttpOnly (invisible to JS), SameSite=Lax, no Secure in dev, opaque non-JWT value | All confirmed (`photosphere_session`, 64-hex opaque token) | Pass |
| 4 | Auto-redirect while authed | Visit `/login` and `/signup` with valid session | Both redirect to `/dashboard` | Both redirected | Pass |
| 5 | Logout | Click Log out; revisit `/dashboard` | Back on `/login`; `/dashboard` blocked | Exactly as expected | Pass |
| 6 | Server-side revocation | Replay pre-logout cookie against `/api/auth/me` | 401 | 401 | Pass |
| 7 | Duplicate signup (UI) | Re-submit account A's email on `/signup` | "already exists" error, stays on page | Error shown, stayed on `/signup` | Pass |
| 8 | Wrong password | Login UI with bad password | Generic "Invalid email or password" | As expected | Pass |
| 9 | Unknown email (no enumeration) | Login UI with nonexistent email | Byte-identical error message to #8 | Identical | Pass |
| 10 | Correct login | Login UI with account A creds | `/dashboard` | As expected | Pass |
| 11 | Invalid signup payload | POST bad email/short password/empty name | 400 Zod details, before DB | 400 with field errors | Pass |
| 12 | **Split buckets: signup limit 5/15min/IP** | Burst duplicate-email signups | 429 on the 6th in-window request | 409 ×5 then 429 exactly at the 6th in-window request | Pass |
| 13 | **Split buckets: login unaffected by signup 429** | Valid login immediately after #12's 429 | 200 | 200 | Pass |
| 14 | **Login's own bucket 10/15min/IP** | Wrong-password burst | 429 at the 11th in-window login request | 401s through the 10th, 429 exactly at the 11th (counting all in-window login calls incl. earlier UI/API logins) | Pass |
| 15 | Upload unaffected by both exhausted auth buckets | Upload as A right after #12+#14 | 202, not 429 | 202 (resolved `duplicate`/sha256 — expected, bytes were a re-upload) | Pass |

### Upload pipeline (specs/upload-pipeline.md — regression)

| # | Test | Steps | Expected | Actual | Result |
|---|---|---|---|---|---|
| 16 | Happy path timing | POST `fixture-food.jpg`, time response | 202 with `photoId`/`jobId`, well under 1s | 202 in 73ms (all 20 uploads this run: 33–117ms) | Pass |
| 17 | 50MB size limit | 51MB file with JPEG magic bytes | 413 before storage write | 413 in 81ms | Pass |
| 18 | Content sniffing | PE-header executable renamed `.jpg` | 400 by magic bytes | 400 "Unsupported or unrecognized file type" | Pass |
| 19 | Async worker processing | Poll `/status` after each upload | `pending → done/duplicate/failed` within seconds | All terminal within ~2–6s | Pass |
| 20 | Thumbnails, 3 sizes | Fetch all pre-signed thumbnail URLs | 150/400/1200 all exist, real bytes | All 200 with real bytes | Pass |
| 21 | Pre-signed URLs only | Inspect every URL in `GET /:id` + folder-photos responses | Signed URLs only, never a raw key | All carry `X-Amz-Signature`; no raw keys anywhere | Pass |
| 22 | 60s TTL is real | Re-fetch a signed URL after 65s | Rejected | 200 at t=0, 403 at t=65s | Pass |
| 23 | Dedup: byte-identical re-upload | Re-upload `fixture-food.jpg` bytes, same user | `duplicate`, no classification | `duplicate`, correct `duplicateOfPhotoId`, empty `aiLabels`, null `aiConfidence` — and now provably `dedupMethod: "sha256"` | Pass |
| 24 | Dedup: true negatives | 7 distinct fixtures + 5 companions, same user | All `done`, no false duplicates | Confirmed | Pass |
| 25 | Dedup: per-user scope | User B uploads A's exact `fixture-food.jpg` bytes | NOT duplicate; B gets own classification/folders | `done`, own Food folder in B's own "My Photos" | Pass |
| 26 | Dedup: pHash near-dup phase still fires | Re-encode `fixture-vehicles.jpg` at q70 (different bytes, offline-verified hamming=2), upload as A | `duplicate` via pHash pointing at the original | `duplicate`, `dedupMethod: "phash"`, `duplicateOfPhotoId` = original vehicles photo | Pass |
| 27 | Cross-user 404 (photo endpoints) | B requests A's photo via `GET /:id` and `/:id/status` | 404 both, never 403 | 404 both | Pass |
| 28 | Forced failure path | Upload `FORCE_FAIL_animals.jpg` | Retries then `failed`; attempts=3, error populated | `failed`; `job: {type:"pipeline", status:"failed", attempts:3, errorMessage:"Forced failure…"}` — **exact attempts/error now live-verified for the first time** (was a caveat in both prior reports) | Pass |
| 29 | Upload limiter independence | Uploads during/after auth-bucket exhaustion; code review | Separate per-user bucket, no cross-contamination | Confirmed functionally (#15) and in code (`uploadRateLimiter.ts`, own instance, user-id-keyed, 30/15min) | Pass |
| 30 | Upload UI round-trip | Playwright: `/upload` → choose `ui-people.jpg` → click Upload → poll | Status `done`, labels shown, thumbnail `<img>` renders real pixels | All confirmed (`naturalWidth > 0`); People folder count incremented 2→3 afterward | Pass |
| 31 | EXIF fields | `GET /api/photos/:id` for EXIF-tagged and EXIF-less uploads | Populated vs all-null (no error) | Tagged: `cameraMake:"PhotoSphereTest", cameraModel:"TesterCam 3000"`, nulls elsewhere; fixture: all-null — **live-verified for the first time** via carry-over c | Pass |
| — | Storage quota 413 | — | — | **Not tested live** (unchanged constraint: 5GB default quota impractical to exceed, no DB access to shrink it). Code review: check still present in `routes/photos.ts` before any MinIO write, `storageUsedBytes` incremented transactionally | Not tested (code-reviewed only) |
| — | Worker kill/restart durability | — | — | **Not tested** (unchanged constraint: cannot manage Abhishek's local processes; `tsx watch` would auto-restart anyway). Indirect evidence unchanged: sub-120ms 202s on every upload | Not tested |

### AI classification — [Tester-live] ACs (specs/ai-classification.md)

| # | Test | Steps | Expected | Actual | Result |
|---|---|---|---|---|---|
| 32 | Fixture set integrity (pre-flight) | Run all 7 committed fixtures through the real `classify()`+`mapToCategory()` offline | Labels/confidence/folder match the README table exactly | All 7 match; all dHashes non-degenerate | Pass |
| 33 | Mappable fixture → auto-created folder | Upload `fixture-food.jpg`, poll, then `GET /api/collections` + `/folders` | `done`, non-null `folderId`+`collectionId`, exactly one default "My Photos", Food folder `ai_generated` | All confirmed | Pass |
| 34 | Folder reuse + photoCount | Upload `second-food.jpg` (different bytes, same category) | Same Food folder id, `photoCount: 2` | Same id `a7cea870…`, count 2 | Pass |
| 35 | All category mappings | Upload people/nature/animals/vehicles/documents fixtures | Each lands in its named folder | People, Nature (via aliases), Animals, Vehicles, Documents — all correct | Pass |
| 36 | Unmappable fixture | Upload `fixture-unmappable.jpg` | Uncategorized (auto-created, ai_generated) despite 0.94 confidence | Confirmed | Pass |
| 37 | FORCE_LOWCONF_ threshold override | Upload fresh mappable bytes named `FORCE_LOWCONF_vehicles.jpg` | `aiConfidence` < 0.6, lands in Uncategorized, labels kept | `aiConfidence: 0.42`, labels `["Car","Truck"]` kept, folder Uncategorized | Pass |
| 38 | Concurrent new-category race | `fixture-documents.jpg` + `second-documents.jpg` uploaded in parallel (brand-new Documents category, worker concurrency 2) | Exactly one folder row, both jobs complete | Both `done`, identical `folderId`, one "My Photos" created race-safely, both `attempts: 1` | Pass |
| 39 | Duplicates get no folder | Check both sha256 duplicates | `folderId` null | Null on both | Pass |
| 40 | Multi-category priority | `fixture-people.jpg` (`["Person","Outdoor"]` = People+Nature) | People (higher priority) | People | Pass |
| 41 | 401 on all six new endpoints | No-cookie requests to collections list/folders GET/folders POST/folder-photos/PATCH photo/reclassify | 401 all six | 401 all six (+ upload/GET/status regression 401s) | Pass |
| 42 | Cross-user 404 on every id-taking endpoint | B → A's collection (GET+POST folders), A's folder photos, A's photo (PATCH + reclassify); A → own photo into B's folder | 404 everywhere, never 403; no side effects | 404 ×6; A's photo folder confirmed unchanged after B's PATCH attempt; `GET /api/collections` shows only own collection for each user | Pass |
| 43 | Folder photos pagination | Documents folder (4 photos): `limit=2` pages, past-end offset | Correct slices, `total: 4`, newest first | 2+2+0 items, correct order, `total` 4 | Pass |
| 44 | Pagination validation | `limit=101`, `offset=-1`, `limit=abc` | 400 via Zod each | 400 ×3 | Pass |
| 45 | Folder photos thumbnails | Fetch `thumbnailUrl` from listing | Real bytes, signed, expires | 200 real bytes at t=0, 403 at t=65s, signed-only | Pass |
| 46 | Manual folder creation | POST `{name:"Tester Custom"}`; duplicate; empty; 256-char | 201 `custom` / 409 / 400 / 400 | Exactly that | Pass |
| 47 | PATCH move reconciles counts | Move flat-blue Documents → Tester Custom | 200; Documents 4→3, Custom 0→1; status/labels/confidence untouched | All confirmed | Pass |
| 48 | PATCH validation | Missing / non-uuid `folderId` | 400 both | 400 both | Pass |
| 49 | Reclassify rescues `failed` | `POST /:id/reclassify` on the FORCE_FAIL photo | 202 → `done` with folder (FORCE_FAIL must not fire on reclassify) | `done`, Animals folder, `job: {type:"reclassify", status:"completed", attempts:1}` | Pass |
| 50 | Reclassify rescues `duplicate` | Reclassify the sha256 duplicate | 202 → `done`, `duplicateOfPhotoId` and `dedupMethod` cleared, folder assigned | Both cleared (null), Food folder, count 2→3 | Pass |
| 51 | Reclassify FORCE_LOWCONF determinism | Reclassify the FORCE_LOWCONF photo | 202 → re-resolves to Uncategorized (hook fires on both job types) | Confidence 0.42 again, Uncategorized, reclassify job completed | Pass |
| 52 | Reclassify 409 while in progress | Two rapid reclassify POSTs on one photo | 202 then 409 | 202 then 409 "Classification already in progress" | Pass |
| 53 | Reclassify own rate-limit bucket | B bursts reclassify requests | 429 at the 31st in-window request; uploads/auth/other users unaffected | 429 exactly at #31; B upload 202, B login 200, A reclassify 202 (per-user keying) — all immediately after | Pass |
| 54 | Job observability + bookkeeping isolation | `job` object across pipeline/reclassify lifecycles; check an untouched photo's job mid-reclassify | Latest row only, correct type/status/attempts/error per photo | Pipeline rows stay `completed` on untouched photos while others reclassify; reclassify rows tracked independently (Tester-verifiable half of the section-4 re-key) | Pass |
| 55 | Flat-image repro (2026-07-02 finding) | Upload flat red + flat blue rectangles (exact prior repro); then byte-identical red re-upload | Both `done` independently, never pHash-deduped; re-upload → `duplicate` via sha256 | **Fixed as claimed:** both `done` in Documents; re-upload `duplicate` with `dedupMethod:"sha256"` | Pass |
| 56 | Migration landed via `prisma migrate dev` | Code review of `backend/prisma/migrations/` | New migration dir extending history | `20260702090917_add_collections_folders_classification` present; SQL matches spec schema (unique constraints, FKs, `file_sha256`/`dedup_method`, indexes) | Pass |
| 57 | No real Vision API (Tester-verifiable portion) | Dependency + source grep + live behavior | No GCP SDK, no Vision hostnames, mock-only provider, every classification succeeds via mock | `backend/package.json` clean; no `googleapis`/`google-cloud`/vision hosts in `backend/src`; `classification/index.ts` sole provider; 20+ live classifications all via deterministic mock (also: no JWT anywhere, regression) | Pass |

---

## Out of scope by the spec's verification legend — code-reviewed only, NOT live-tested (listed, not fabricated)

Per `specs/ai-classification.md`'s legend these are [Developer-verified / code-review-only]; Developer's MR draft (`reports/mr-drafts/feature-ai-classification.md`) records their verification. My code review found each implemented as described:

1. **Threshold boundary (exactly 0.60 is categorized)** — `mapToCategory` uses strict `<` (`categoryMapping.ts`); unit-level, not huntable via fixtures.
2. **`photos.file_sha256` populated on new uploads / pre-existing rows keep null** — set in the upload handler; no endpoint exposes it; DB-access half untestable here.
3. **Mock call-counter proof of dedup short-circuit** — `mockClassifyCallCount` exists; live proxy (empty labels/null confidence on duplicates) verified in #23.
4. **`npm test -w backend` twice back-to-back without 429 flakes** — `NODE_ENV=test` limit-1000 relaxation confirmed in `auth.ts`; MR draft reports 47/47 twice.
5. **`prisma migrate dev` replays cleanly on a fresh DB** — migration history reviewed only.
6. **Classification succeeds with networking unavailable** — cannot alter host network state; committed `classification.offline.test.ts` covers it per MR draft.
7. **Bookkeeping re-key, DB-level half** (original `pipeline` row still `completed` after a *failed* reclassify) — the API-observable half passed live (#54).
8. **Reclassification UI ACs** — correctly not built; wireframe options A–C exist in `design/wireframes/proposals/`, pending Abhishek's pick. Verified no new page shipped (`frontend/src/app/` unchanged: dashboard/login/signup/upload only).

---

## New Failures

None. Zero product bugs found in any tested path.

---

## Methodology / process notes

- **Interrupted run:** dispatched 2026-07-02 evening; a session usage limit paused it after environment checks + code review, before any live traffic. Resumed after midnight — commit (`27afa75`) and running stack unchanged across the pause, re-verified before resuming. All live results above are from the resumed session.
- **Signup budget discipline worked as designed.** Planned 4 signup requests up front (UI signup, UI duplicate, curl invalid-payload, curl user B), leaving 1 + the 429 for the burst test. Observed counts matched the production numbers exactly (signup 429 at the 6th in-window request, login at the 11th) — the whole run fit inside one 15-minute window with zero mid-suite friction, unlike the two prior runs that needed backend-restart workarounds. The split-bucket fix does its job.
- **Two `tsx watch src/server.ts` processes are running** (plus one worker). Only one can bind :4000, so the second is likely a stale leftover — benign, but in-memory rate-limit state lives only in the bound process, and a stale sibling could cause confusion if it ever wins a restart race. Worth a cleanup whoever owns the dev processes.
- **Companion-image determinism caveat for future runs:** my sharp-`withExif` gray image hashes to `["Person","Outdoor"]` → People, not the Nature outcome the fixtures README lists for the developer's own in-test gray image — EXIF payload bytes differ, and the mock is bytes-deterministic. Not a bug; future Testers should derive expectations from their own bytes via the real `classify()` (as done here), not from the README's in-test table.
- **Known accepted edge (duplicate→duplicate chain under a concurrent re-encode+copy race):** not observed. Every `duplicateOfPhotoId` this run pointed at a canonical, non-duplicate, strictly-older original — consistent with the `lib/dedup.ts` invariant. Nothing to add to the pending decision.
- **MinIO console login skipped again on principle** (no credential entry into forms); storage proof was pre-signed-URL fetches returning real bytes, same as last run.
- **Test residue left behind (by role, no cleanup):** accounts `clsf.a.20260703@example.com`, `clsf.b.20260703@example.com`; ~22 photos for A, 2 for B; one custom folder "Tester Custom"; assorted thumbnails in MinIO. B's reclassify bucket and the IP's signup/login buckets were exhausted at ~00:45 and self-reset within 15 minutes.
- Evidence screenshots (pass-path) kept in the session scratchpad; no failure screenshots exist because nothing failed.

---

## Notes for Developer Agent

- Nothing to fix. Every [Tester-live] AC passed, both prior "code-reviewed only" observability gaps (EXIF, job attempts/errorMessage) are now closed live, and the flat-image dedup false positive is confirmed fixed by the exact original repro.
- The pHash near-dup phase was verified with a genuinely different-bytes near-duplicate (q70 re-encode, hamming 2) → `dedupMethod: "phash"` with the correct canonical original — the sha256 fast path did not mask it.
- Consider killing the stale duplicate `tsx watch src/server.ts` process (see process note).
- For whoever builds the reclassification UI after Abhishek's wireframe pick: everything the blocked section needs from the backend (folders list with counts, folder photos with thumbnails, move, create-folder, reclassify + poll) is verified working at the API level in this report.
