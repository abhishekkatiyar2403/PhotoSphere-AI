# Test Report — 2026-07-06 01:35

**Run type:** manual (Week 11–12 Audit log + polish regression)
**Dev server:** localhost:4000 (backend) + localhost:3000 (frontend) — reachable: yes
**Tested against commit:** `e3d4057` (HEAD; audit backend + Helmet in `7fd5c15`, P2/P3 docs in `f1ce08d`/`e3d4057`), branch `feature/ai-classification`
**Features in scope (new/changed since last report):** audit-log backend (`audit_log` table + migration `20260705154110_add_audit_log`, `lib/audit.ts` fire-and-forget helper, 7 choke-point `logAudit` hooks, owner-scoped `GET /api/audit`), Helmet security headers (P1), and independent confirmation of the zero-code-change P2 (rate-limiter coverage) and P3 (empty/error/loading sweep) claims.

---

## Summary

Passed: 41 / Failed: 0 (0 critical, 0 high, 0 medium, 0 low)

**Backend automated suite: 85/85** (`npm test -w backend`, zero flake). **Overall verdict: Week 11–12 (Audit log + polish) is CLEAN — ship-ready. 0 open bugs, 0 security holes.**

Explicit verdicts on the charter items:
- **Audit trail completeness — PASS.** The full sharing flow's complete ordered trail is present, newest-first, over live HTTP + DB inspection: `guest_revoked → photo_downloaded → photo_viewed → access_approved → access_requested → share_created`. Every row has the correct `actorType` (owner vs guest), correct `ownerId`, captured guest IP on `access_requested`, and the exact A2 metadata (folder names + permission level on `share_created`; reasons on denies; `folderId` on view/download).
- **Owner-scoping is LEAK-PROOF — PASS (both directions).** Seeded activity under two owners (A, B); each owner's `GET /api/audit` showed ONLY their own rows — zero cross-owner leakage either direction, including the guest-actor rows (a guest's view/download appears only in that guest's OWNER's trail).
- **Success-only logging — PASS (the serious risk, clean).** DB inspection after deliberately triggering a view-only `/download` 403 and 404s: `photo_downloaded` rows = 0, `photo_viewed` rows = 1 (only the one 200 view). A 403 view-only refusal and any 404 produce NO row.
- **Helmet didn't break the frontend — PASS.** Security headers present on every API response; the frontend loads and fully functions authed (real dashboard/organize/browse data renders via cross-origin API + pre-signed image loads), with CSP correctly OFF in dev (AP10) so nothing in Next is blocked.

---

## Regression Suite Results

| Area | Test | Expected | Actual | Result |
|---|---|---|---|---|
| Stack | Backend health + frontend reachable | 200 / responds | `{"status":"ok"}`; frontend 307→login | Pass |
| Audit — Success Signal | Full flow (share→request→OTP→approve→view→download→revoke) then `GET /api/audit` owner A | Complete ordered trail newest-first | 6 rows exactly, correct order | Pass |
| Audit — actorType | Owner rows (share/approve/revoke) = owner; guest rows (request/view/download) = guest | Correct per A2 | All correct both directions | Pass |
| Audit — IP capture | `access_requested` carries the guest's IP | IP present | `ipAddress=::1` + `metadata.ipCaptured=::1` + userAgent | Pass |
| Audit — share_created metadata | folderNames + folderIds + permissionLevel + guestEmail + expiresAt | Per A2 table | `folderNames:["Nature"]`, `permissionLevel:"download"`, etc. | Pass |
| Audit — view/download metadata | `{folderId}` | Present | `{folderId}` on both | Pass |
| Audit — leak-proof (A→B) | Owner B sees zero of owner A's rows | 0 cross-owner | B's trail = 6 B-only rows | Pass |
| Audit — leak-proof (B→A) | Owner A sees zero of owner B's rows | 0 cross-owner | A's trail = 6 A-only rows | Pass |
| Audit — guest-actor scoping | Guest view/download only in that guest's owner's trail | Scoped | guest-a rows only in A's trail | Pass |
| Audit — view-only /download 403 → NO download row | 0 `photo_downloaded` | 0 | DB: `photo_downloaded` rows = 0 | Pass |
| Audit — 404 (not-permitted) → NO row on view or download | 0 rows | 0 | No view/download row for the unpermitted photo | Pass |
| Audit — 200 view → exactly 1 photo_viewed | 1 | 1 | DB: `photo_viewed` rows = 1 | Pass |
| Audit — access_denied owner_denied | reason=owner_denied | matched | 1 row, `reason:"owner_denied"` | Pass |
| Audit — access_denied otp_attempts_exceeded | 3 wrong OTP → reason | 401,401,403 + row | 1 row, `reason:"otp_attempts_exceeded"` | Pass |
| Audit — access_denied otp_expired | expired OTP → 403 + reason | 403 "OTP expired" + row | 1 row, `reason:"otp_expired"` | Pass |
| Audit — fire-and-forget never breaks primary | Forced audit-insert failure leaves approval + download succeeding | Covered by suite | `audit.smoke.test.ts` "an approval STILL succeeds when the audit insert is forced to fail" (approve 200 + DB `approved` + download 200 with `X-Amz-` URL) | Pass |
| Audit — append-only | No PATCH/DELETE/POST/`:id` | 404/405 | PATCH/DELETE/POST/`GET :id` all 404; only list GET works | Pass |
| Audit — no session → 401 | 401 | 401 | 401 | Pass |
| Audit — filter action=photo_downloaded | only downloads | total 1, only photo_downloaded | Pass |
| Audit — filter actorType=guest | only guest rows | total 3, all guest | Pass |
| Audit — filter actorType=owner | only owner rows | total 3, all owner | Pass |
| Audit — pagination limit/offset | limit honored, total correct | limit=2 → 2 rows, total 6 | Pass |
| Audit — date range from/to | bounds applied | future `from`→0, past `to`→0, spanning→6 | Pass |
| Audit — invalid action → 400 | 400 | 400 | Pass |
| Audit — invalid date → 400 | 400 | 400 | Pass |
| Audit — invalid actorType → 400 | 400 | 400 | Pass |
| Audit — limit>100 → 400 | 400 | 400 | Pass |
| Schema — additive-only | no FK cascade, owner_id present, 2 indexes | Verified | Only PK constraint (no FK `f`); columns match A1; both indexes present | Pass |
| Helmet — headers present | nosniff, X-Frame-Options, HSTS, no X-Powered-By, CORP cross-origin | Present | X-Content-Type-Options: nosniff, X-Frame-Options: SAMEORIGIN, HSTS, Referrer-Policy, CORP: cross-origin; no X-Powered-By | Pass |
| Helmet — CSP off in dev (AP10) | no CSP header on backend in dev | absent | No `content-security-policy` header | Pass |
| Helmet — frontend not broken (authed) | pages render real data, 0 errors | Verified | dashboard/organize/browse/share/guests render real data via cross-origin API+images; 0 exceptions/console errors | Pass |
| P3 — 8 pages fresh/logged-out don't crash | no crash/overlay | Verified | all 8 load, 0 JS exceptions, 0 console errors, no Next error overlay | Pass |
| P3 — /g/[token] garbage token | clean, no crash | 365-char body, no error | Pass |
| Privacy (regression) — unapproved /g/:token | 0 real images, only 401 probe | Verified via network capture | 0 image loads, 0 pre-signed/MinIO loads, 0 `<img>` els; only `GET /api/guest/folders` (401) fires | Pass |
| Guest access (regression) — full flow | share/approve/view/download/revoke all work | Verified | 201/200/200/200/200 for both owners | Pass |
| Backend suite | `npm test -w backend` | ~85 | 85/85, 0 flake | Pass |

---

## New Failures

None.

---

## Notes for Developer Agent

- **P2 rate-limiter audit independently confirmed correct.** The MR-draft enumeration of all 30 endpoints matches reality: `GET /api/audit` is a session-gated owner-scoped read (`where ownerId = req.user.id`, no per-id lookup, no image data) and is correctly fine without a limiter. Every unauthenticated/public surface is limited; the invite-request limiter (10/15min/IP) fired during this session (benign — see below), confirming it's armed. Zero-code-change for P2 is the right outcome.
- **P3 zero-code-change confirmed.** All 7 pages load without crashing on both the fresh/logged-out condition and (for the authed pages) the real-data happy path. The "Audit" link is present in the top bars but the P7 viewer UI is correctly deferred to a wireframe round — the link is just nav; not tested as a rendered feature this pass.
- **AP3 deviation (denormalized `owner_id`) verified in the DB** — present, no FK, makes the owner-scoped read a single indexed scan. This is what makes leak-proofing trivial and it works. Flagged deviation, taken on the recommended default; recommend confirming/vetoing.
- **Benign harness classes (NOT app bugs), for the next Tester:**
  - The invite-request rate limiter (10/15min/IP) exhausted my `::1` bucket late in the session (`"Too many access requests"`), so a final fresh revoke-→-401 re-mint couldn't complete over HTTP. This is a PASS of the limiter, not a bug — revoke→401 is independently proven by the backend suite (85/85, instant-revocation test) and by both main-flow revokes returning 200 with `guest_revoked` rows landing. Seed owners via Prisma and budget invite requests to avoid it.
  - The `/g/:token` load-time probe `GET /api/guest/folders` fires ~4× on mount in dev = React 18 StrictMode double-invoke × poll — all harmless 401s with no data (single-fires in a prod build). Documented in prior reports.
  - The "Unique constraint failed on the fields: (collection_id, name)" log line during `npm test` is the expected P2002 catch-and-refetch during concurrent folder auto-creation, not a failure.
- **Housekeeping done:** all 6 seeded audit-test owners deleted (cascaded; 29 audit rows explicitly removed since `audit_log` has no FK), throwaway backend scripts removed (`git status backend/` clean), headless Chrome killed. No stray files left in the repo — session scratchpad only.
- **Methodology note:** no Playwright/`playwright-core` is installed anywhere in the repo and the environment is offline, so the frontend was driven headless over Chrome's raw CDP WebSocket (Node 24's global `WebSocket`, no npm dep) — navigate, inject session cookie via `Network.setCookie`, capture `Runtime.exceptionThrown` + `Runtime.consoleAPICalled(error)` + network requests. Same practical outcome as Playwright for these load/error/network assertions.
