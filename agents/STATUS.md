# PhotoSphere AI — Live Status

> Single source of truth for what's happening right now. Every agent reads this first and updates it last. Master Agent owns this file — Planner, Developer, and Tester propose updates, Master reconciles conflicts.

**Last updated:** 2026-07-03 by Master Agent (fourth orchestration cycle of the day: Planner scoped the rest of Week 7–8 → Developer built the read-only Folder browser (`/browse`) + a shared fullscreen Photo viewer, both unblocked without a new wireframe → Tester ran a deliberately adversarial regression pass and found **zero new bugs**, 53/54 with the one failure a test-fixture mistake, root-caused and cleared. **The only thing left in Week 7–8 is the Dashboard page**, gated on a wireframe pick — 3 options below.)

---

## Current Phase
Phase 1 — MVP. Week 1–2 (Auth), Week 3–4 (Upload Pipeline), and **Week 5–6 (AI Classification, including its UI) are fully shipped and regression-tested clean.** Week 7–8 (Core UI) is now mostly done too: `GET /api/dashboard`, upload drag-and-drop/progress, the read-only Folder browser (`/browse`), and a shared fullscreen Photo viewer (used by both `/browse` and `/organize`) are all shipped and tested clean. **Only the Dashboard page itself remains**, blocked on Abhishek's wireframe pick — see Pending Decisions.

## Current Sprint Goal
Week 5–6 is done end to end. Week 7–8 is down to one piece: the Dashboard home screen (storage usage + stats, from the already-shipped `GET /api/dashboard`). Everything else in the roadmap's Week 7–8 checklist — Folder browser, Photo viewer, drag-and-drop upload — is built and tested. Three wireframe options are proposed and waiting on Abhishek below; once picked, this closes out Week 7–8 entirely.

## Ready Spec (from Planner)
- `specs/ai-classification.md` — **built and shipped.**
- `specs/dashboard-stats-and-upload-polish.md` — **built and shipped.**
- `specs/week7-8-dashboard-browser-viewer.md` — **new this cycle. Folder browser and Photo viewer built and shipped; Dashboard page explicitly separated out as blocked** on Abhishek's wireframe pick (3 options below, proposed by Planner this cycle, not yet built into any file).

All three specs are committed to git. Nothing left unbuilt in any shipped spec except the one blocked Dashboard page.

## Last Tester Run
**2026-07-03 16:47 — Folder browser (`/browse`) + shared Photo viewer regression, 53/54 passed, 0 app bugs.** Report: `reports/2026-07-03_1647.md`. The single failure was Tester's own test-fixture sizing mistake, root-caused and independently re-verified clean — not an app defect. Given the project's track record today (two real bugs found earlier via deliberate exploratory testing), Tester was explicitly asked to bring the same scrutiny here — and found nothing, a genuinely different outcome worth noting on its own: `/browse` confirmed read-only both by DOM absence AND by network monitoring (zero PATCH/POST fired during interaction, only GETs), the viewer's image confirmed to be the true full-resolution original (not a thumbnail), EXIF panel correctness on both populated and empty cases, Prev/Next correctly scoped to the loaded page with no wraparound (including the true single-photo-folder edge case), all three close mechanisms (X/Escape/backdrop-click — the backdrop-click bug Developer found and fixed on itself was re-verified genuinely fixed, not just trusted), and cross-surface integration confirmed (`/organize`'s pre-existing Move/Reclassify unaffected by the new shared viewer). Adversarial/break-it testing (rapid double-click and 10-click Next/Prev spam, browser back button with viewer open, narrow-viewport layout) surfaced nothing. Backend suite unaffected (56/56, zero backend changes this cycle). Housekeeping carried forward: the stale duplicate `tsx watch src/server.ts` process is now a fourth consecutive flag.

**2026-07-03 08:55 — fix verification, 30/30 passed, 0 bugs.** Report: `reports/2026-07-03_0855.md`. Focused re-test of the High-severity bug found earlier this cycle (below) after Developer's fix (commit `69363fb`): reproduced the exact original repro live and confirmed it's gone, confirmed the duplicate variant (using a stronger repro than Developer's own new test — both uploads `FORCE_FAIL_`-prefixed so the base photo genuinely never classifies, closing a gap noted for Developer's awareness), confirmed the second latent bug (sidebar not picking up a newly-created folder after a user's first-ever successful reclassify) is also fixed, and regression-spot-checked move/count-reconciliation and cross-user isolation since the fix required a refactor (moving shared helpers to new `lib/photoCard.ts`/`lib/storageKeys.ts` to avoid a circular import) — confirmed byte-for-byte identical logic, no behavioral drift. Backend suite: 56/56, run 5 times across the two verification sessions, zero flake.

**2026-07-03 07:31 — organize UI regression, 27/27 scripted passed, 0 bugs in scripted tests — but 1 High-severity bug found via deliberate exploratory testing beyond the task's suggested steps.** Report: `reports/2026-07-03_0731.md`. Every acceptance-criteria item explicitly in scope passed exactly as specced: end-to-end folder navigation, move with server-side count reconciliation (verified independently, not just trusting the UI), the "Unfiled" bucket's styling/labels/duplicate-filename resolution, reclassify and "not a duplicate?" recovery (verified via the status endpoint, not just visually), inline folder creation (incl. 409/empty-name), pagination across a genuine 13-photo folder, cross-user 404 isolation on the new endpoint, and the `folders.ts` refactor's regression path. **The bug:** a brand-new user whose very first photo fails or dedupes *before any collection has ever been created* had that photo permanently unreachable — the page's initial-load effect bailed out entirely on "no collections yet" without ever checking for unfiled photos independently. Found specifically because Tester deliberately sequenced uploads in an order the task's own suggested steps never would have (failure-before-any-success, rather than after populating real folders first) — flagged as exactly the kind of gap a reduced-review cycle (this Developer session was interrupted by a usage limit, see below) is most likely to miss. Root-caused precisely, screenshot captured, confirmed genuinely likely in practice (any real user whose first upload happens to fail or dedupe hits this), not a contrived edge case.

**2026-07-03 02:27 — dashboard-stats + upload-polish regression, 20/20 passed, 0 bugs** (plus 52/52 on the full backend automated suite, zero regressions). Report: `reports/2026-07-03_0227.md`. Scope was deliberately focused: a handful of load-bearing spot-checks (login, upload happy-path, cross-user 404) confirmed no regression from this small addition, then full live coverage of the two new pieces.

- **`GET /api/dashboard`:** live-verified end-to-end — 401 with no session, exact empty-state shape for a fresh user, correct aggregation after uploading across categories (a `FORCE_FAIL_` photo and a byte-identical duplicate both correctly counted in `totals.photoCount` per the spec's stated default, while correctly excluded from folder assignment), storage bytes cross-checked byte-for-byte against independently-computed upload sizes, and confirmed cross-user isolation with zero leakage either direction.
- **Upload page polish:** drag-and-drop genuinely populates file state and uploads successfully (verified via real `DataTransfer` construction, not a Playwright shortcut), visual drag-over feedback confirmed, a dropped bad-magic-bytes file is rejected identically to click-to-browse, and click-to-browse regression (both happy-path and rejection) still works unchanged. **The progress bar claim was verified, not just code-reviewed:** under throttled network conditions, Tester captured 3,521 samples with 181 distinct genuine intermediate progress values, monotonically increasing — definitive proof the `xhr.upload.onprogress` wiring is real (a plain local upload completes in under a second, too fast to observe intermediate values without throttling — Tester's methodology section explains why throttling was necessary rather than a shortcut).

**Prior run for comparison — 2026-07-03 00:50 — full regression (auth + upload pipeline + AI classification), 52/52 passed, 0 bugs.** Report: `reports/2026-07-03_0050.md`. Run was dispatched 2026-07-02 evening, interrupted mid-run by a session usage limit before any live traffic, and resumed after midnight against the identical commit/stack — nothing invalidated by the pause. Headless Playwright + curl/node-fetch (VS Code session, not Cowork live-browser control).

- **Auth regression (15/15):** full baseline re-verified, plus the new split rate-limit buckets confirmed live at exact production numbers — signup 429 at the 6th in-window request, login 429 at the 11th, fully independent of each other and of uploads. **This is the first run with zero rate-limit-induced workarounds**, after two consecutive prior runs needed a benign backend restart mid-suite.
- **Upload pipeline regression (16/16):** full baseline re-verified. Two items graduate from "code-reviewed only" to **live-verified for the first time**: EXIF fields (new `exif` object on `GET /api/photos/:id`) and exact `attempts=3`/`errorMessage` on forced failure (new `job` object on the status endpoint) — both were open testability gaps in the 2026-07-02 report, both closed by this build.
- **AI classification (21/21):** every `[Tester-live]` acceptance criterion in the spec passed — all 7 category mappings, folder reuse + photoCount reconciliation, Uncategorized via both unmappable labels and the new `FORCE_LOWCONF_` test hook, a genuine concurrency race (two simultaneous uploads into a brand-new category → exactly one folder row), pagination validation, PATCH move, all four reclassify paths (rescue a `failed` photo, rescue a `duplicate` photo, 409-while-in-progress, its own independent 30/15min rate bucket), cross-user 404 isolation and 401s across all six new endpoints, and the full job/dedup observability surface.
- **Flat-image false positive (2026-07-02 finding) — confirmed fixed.** Re-ran the exact red/blue flat-rectangle repro: both process independently to `done`; a byte-identical re-upload correctly resolves `duplicate` via `dedupMethod: "sha256"`.
- **Not independently live-verifiable (unchanged constraints, not failures):** storage-quota 413 (impractical without DB access to shrink the 5GB default), worker kill/restart durability (can't manage Abhishek's local processes from this session). Plus 8 items explicitly out-of-scope by the spec's own verification legend (`[Developer-verified]`/code-review-only: 0.60 threshold boundary, `file_sha256` DB state, mock call-counter, double `npm test` run, fresh-DB migration replay, offline classification, DB-level bookkeeping half, and — separately — the reclassification UI itself, correctly not built). Developer's MR draft (`reports/mr-drafts/feature-ai-classification.md`) records verification for all of these.
- **Two housekeeping notes from Tester:** a stale duplicate `tsx watch src/server.ts` process is running alongside the live one (benign, only one can bind :4000, worth killing next time someone's at the machine); and a methodology note for future Testers — derive companion test-image expectations from your own generated bytes via the real `classify()`, not from the fixtures README's table (the mock is byte-deterministic, so a differently-encoded "gray image" won't match another session's).

Prior baseline for comparison: 2026-07-02 04:50 run, 28/28, first to test the upload pipeline; found the flat-image pHash issue now fixed. 2026-07-01 run, 17/17, auth only.

## Last Developer Action
**2026-07-03 (evening, later) — Developer Agent shipped the read-only Folder browser + shared Photo viewer, commit `243b338`.** New route `/browse` (sidebar + paginated grid, same visual pattern as `/organize` but zero mutation surface: no move dropdown, no reclassify buttons, no folder creation) and a new shared `PhotoViewer` component (fullscreen image + EXIF panel, prev/next navigation scoped to the current page, three independent close methods) wired into both `/browse` and the existing `/organize`. Deliberate judgment call, stated and reasoned: shared the viewer component and CSS wholesale, but duplicated the small read-only card markup rather than threading a `readOnly` flag through `/organize`'s tightly-coupled editable `PhotoCard` — kept `/browse` a genuinely separate, simpler route rather than a mode flag. Zero backend changes needed (confirmed by reading the actual route code before assuming, per the spec's own instruction). Found and fixed one real bug on itself during manual verification: backdrop-click-to-close initially never fired because the fullscreen overlay had no exposed backdrop margin to click — fixed and re-verified. Typecheck/lint clean (independently re-verified by Master), backend suite re-run as a sanity check despite being untouched (56/56, unchanged). MR draft: `reports/mr-drafts/browse-and-photo-viewer.md`.

**2026-07-03 (evening) — Developer Agent shipped the reclassification/organize UI (Option A) on `feature/ai-classification`, commit `9669dfa`, then fixed a High-severity bug Tester found in it, commit `69363fb`.**

Build (`9669dfa`): the `/organize` page per Abhishek's Option A pick — sidebar folder tree with live counts, inline folder creation, a paginated photo grid with three card states (normal/failed/duplicate) matching `design/wireframes/reclassify-ui.svg` exactly. While building, found that `failed`/`duplicate` photos are structurally invisible to any real folder (the dedup gate and failed-pipeline paths both short-circuit before folder assignment, so `folderId` stays null forever) — closed this with a new `GET /api/collections/:id/unfiled-photos` endpoint surfaced as a virtual "Unfiled" sidebar row, plus additive `duplicateOfPhotoId`/`dedupMethod`/`originalFilename` fields needed for the "duplicate of X.jpg" label. All backend changes additive, nothing renamed/removed. This build was interrupted mid-session by a platform usage limit before it could report back, and Anthropic's safety classifier was unavailable to review the final output — **Master independently re-verified everything before trusting it**: re-ran typecheck/lint on both workspaces, re-ran the full backend suite twice back-to-back (52/52 both times), and read the new page's source and both backend route diffs directly, before committing.

Bug fix (`69363fb`): Tester's exploratory testing (07:31 report) found a real High-severity gap this build introduced — see Open Bugs/Last Tester Run. Fixed via a new, genuinely user-scoped `GET /api/photos/unfiled` endpoint (kept additive alongside the old collection-scoped one, which nothing else calls) so the Unfiled bucket no longer depends on a collection existing first; required extracting `PHOTO_CARD_SELECT`/`toPhotoCard` into new `lib/photoCard.ts` and `thumbnailKey`/`originalKey` into new `lib/storageKeys.ts` to avoid a circular import (pure moves, no logic change — confirmed by Tester's follow-up diff review). Developer also caught and fixed a **second latent bug** during self-review: a user's first-ever successful reclassify left the sidebar's known collection id stuck at `null`, so a newly-created folder existed server-side but never appeared without a manual reload. Added regression test coverage for the original bug (there had been none — Playwright-only, uncommitted — exactly how it slipped through). 56/56 backend tests, confirmed independently by both Master and Tester across multiple re-runs, zero flake. MR draft addendum: `reports/mr-drafts/organize-reclassification-ui.md`.

**2026-07-03 (afternoon) — Developer Agent shipped the dashboard-stats endpoint + upload-flow polish on `feature/ai-classification`, commit `510cc30`.** `GET /api/dashboard` (per-user photo/folder/collection totals, per-collection breakdown, storage usage as BigInt-safe strings with a server-computed `usedPercent`) plus drag-and-drop and a real XHR-based progress bar on the existing `/upload` page. Self-reviewed (no separate review pass this cycle) and caught its own bug: the new test file's worker-availability probe was missing, silently skipping the worker-dependent aggregation test every run regardless of whether the worker was actually healthy — fixed by reusing the `beforeAll` probe pattern from `classification.smoke.test.ts`. Backend 52/52 (new `dashboard.smoke.test.ts`, 5 tests), frontend typecheck/lint clean, manually verified in a live browser (drag-over feedback, drop-to-upload, progress bar, click-to-browse and rejected-file regressions). MR draft: `reports/mr-drafts/dashboard-stats-and-upload-polish.md`. Full detail in Tester's independent 02:27 report above.

**2026-07-02/03 (earlier) — Developer Agent shipped the AI-classification organizing layer on `feature/ai-classification`** (branched off `feature/upload-pipeline` @ `74d1f4b`), through a build → adversarial-review → fix → re-verify loop before Tester ever touched it. Five commits: `8f1f065`, `5b8d304`, `97b69a1`, `0395096`, `27afa75`.

**What was built** (full detail in `reports/mr-drafts/feature-ai-classification.md`):
- Schema: `Collection`/`Folder` Prisma models via a real migration (`20260702090917_add_collections_folders_classification`, extending the migration history that started with commit `74d1f4b`), plus `photos.file_sha256` and `photos.dedup_method`.
- Pure `categoryMapping.ts` module (sibling of the classification provider, not part of it — keeps the future real-Vision swap-in a one-file change) implementing the roadmap's label→folder priority table.
- Worker: race-safe folder auto-creation (lazy default "My Photos" collection, find-or-create with P2002 catch-and-refetch), confidence bucketing (<0.60 strict → Uncategorized), a new `reclassify` job type, and a `FORCE_LOWCONF_` test hook (mirrors the existing `FORCE_FAIL_` pattern) since the mock's real confidence range can never otherwise exercise the Uncategorized path.
- Six new endpoints (`GET /api/collections`, `GET`/`POST /api/collections/:id/folders`, `GET /api/folders/:id/photos`, `PATCH /api/photos/:id`, `POST /api/photos/:id/reclassify`) plus additive fields on the existing photo endpoints (`exif`, `folderId`, `collectionId`, `dedupMethod`, `job`), all under the existing `requireAuth`/`asyncHandler`/Zod/404-not-403 conventions.
- Carry-over hardening: `authRateLimiter` split into independent `signupRateLimiter` (5/15min/IP) and `loginRateLimiter` (10/15min/IP), both relaxed under `NODE_ENV=test`; SHA-256 exact-hash dedup pass ahead of pHash; EXIF exposed on `GET /api/photos/:id`.
- 3 SVG wireframe proposals for the reclassification UI at `design/wireframes/proposals/reclassify-ui-option-{a,b,c}.svg` — **not built**, correctly left blocked per the UI-decision protocol.
- A genuinely pre-existing bug found and fixed along the way: `auth.smoke.test.ts`'s duplicate-signup test used an email address invalid under the current Zod regex, so both signups 400'd and the 409 path was never actually exercised — confirmed pre-existing at `74d1f4b` before fixing.

**Two real concurrency bugs were caught before they ever reached Tester**, via an adversarial code-review pass (3 lenses: correctness, security/ownership, testability) followed by independent skeptic verification of every finding, then a second gap-closing round after the first fix proved incomplete:
1. **Circular/mutual duplicate-marking.** The new SHA-256 exact-dedup pass matched *any* same-user same-hash row regardless of whether it had finished processing — two quick uploads of the same file could each mark the other as the duplicate (A→B and B→A simultaneously), permanently losing both copies from the organized library. First fix (constrain the SHA-256 pass to strictly-older, non-duplicate candidates) closed that path but a second adversarial pass found the *same* hole survived through the pHash near-dup phase, which had none of the new exclusions. Final fix applies the same strictly-older-and-non-duplicate constraint to both phases, extracts both candidate queries into a single `lib/dedup.ts` module with the invariant documented at the top, and a max-effort skeptic then tried, and failed, to construct any interleaving (serial, concurrent, retried, reclassify-mixed) that produces a cycle. **One narrower, non-cyclic edge case remains and is accepted, not fixed:** under a specific concurrent race (uploading a re-encoded copy and a byte-identical copy of that re-encode at nearly the same time), a duplicate can end up pointing at another duplicate rather than the canonical original — chains stay finite and acyclic (no data loss, no stuck state), and reclassify still rescues either photo. Not reproduced in Tester's live run. Flagged below as a pending decision rather than spending a fourth fix round on it.
2. **Reclassify double-enqueue race.** `POST /api/photos/:id/reclassify`'s terminal-state check and its enqueue were four non-atomic steps, so concurrent requests (e.g. a double-clicked button) could all pass the guard and create duplicate jobs, corrupting folder photo-counts. Fixed with a single atomic conditional `UPDATE ... WHERE status NOT IN (pending, processing)` as the sole arbiter — confirmed closed by independent re-verification, including compensation logic so a Redis hiccup during enqueue can't strand a photo in `pending` forever.

Also hardened along the way: folder `photoCount` reconciliation was vulnerable to stale reads under concurrent moves/reclassifies (now Serializable-isolated with automatic retry), and 7 additional test coverage gaps the review surfaced (multi-category label priority, the exact flat-image repro as a permanent regression test, concurrent new-category folder creation, EXIF assertions, reclassify rate-limiter independence, and a from-scratch offline-classification test proving no code path can reach the network). Final suite: **47/47, twice back-to-back**, plus Tester's independent 52/52.

**Process note:** the session hit its usage limit three separate times mid-cycle (once during planning, twice during the developer build/fix/test chain). Each time, work already completed was preserved — either because the underlying agent had simply paused and could be resumed with full context, or because a capacity probe confirmed the limit had cleared before relaunching. No work was lost or duplicated; flagging only because it added real wall-clock time to this cycle and may recur.

## Open Bugs
None currently open. Today's five Tester runs: 2026-07-03 16:47 (53/54, one test-fixture mistake not an app bug, zero real bugs even under deliberate adversarial testing), 2026-07-03 08:55 (30/30, fix verification — clean), 2026-07-03 07:31 (27/27 scripted clean, but found 1 High bug — see below, fixed same cycle), 2026-07-03 02:27 (20/20 + 52/52 backend suite, dashboard-stats + upload-polish), 2026-07-03 00:50 (52/52, AI classification). Prior runs also clean (28/28 on 2026-07-02, 17/17 on 2026-07-01).

**Found and fixed this cycle:** a brand-new user whose very first photo failed classification or resolved as a duplicate — before any collection had ever been created — had that photo permanently unreachable in the new Organize UI (High severity, genuinely likely in practice, not a contrived edge case). Found via Tester's deliberate exploratory testing beyond the task's suggested steps, fixed via a new user-scoped `GET /api/photos/unfiled` endpoint, confirmed fixed live by Tester in a follow-up pass, with new regression test coverage added. A second latent bug (sidebar not refreshing after a user's first-ever successful reclassify) was found and fixed in the same pass. Both closed, not reopening here — see Last Developer Action for full detail.

Non-bug items flagged for attention (not blocking):
- **Duplicate-of-a-duplicate chain edge case** (AI-classification cycle). Accepted trade-off, not reproduced live, no data loss; revisit only if it shows up in practice or someone wants a fourth fix layer built.
- **Stale duplicate `tsx watch src/server.ts` process** (flagged twice now, both Tester reports today) — benign, only one can bind `:4000`, but worth killing next time someone's at the machine so it stops showing up in every report.
- Signup/login shared rate-limit bucket, pHash flat-image false positive, and the EXIF testability gap — all **resolved** in the AI-classification cycle, confirmed clean by two subsequent Tester runs. No longer open items.
- `npm install` still flags Next.js 14.2.13's known security advisory — still deferred to a dedicated dependency-upgrade pass (carried forward, unchanged).
- Both `specs/ai-classification.md` and `specs/dashboard-stats-and-upload-polish.md` are now committed to git — resolved the earlier "should specs be tracked" open question by just doing it; no objection raised, no longer flagging.

## Pending Decisions Awaiting User Input

**Carried forward, now resolved (for the record):** auth spec decisions 1–5 confirmed 2026-07-01; auth visual layout (Option B) resolved 2026-07-01, shipped; upload-pipeline's 7 flagged assumptions (file types, thumbnail layout, mock classification shape, pre-signed URL delivery, upload rate-limit numbers, storage quota scope, duplicate-detection scope) were all built on stated defaults with no veto received — treating as implicitly accepted since two full regression cycles have passed on them without objection.

**Resolved this cycle:**

0. **Reclassification UI wireframe — RESOLVED and SHIPPED 2026-07-03: Abhishek picked Option A** (sidebar folder tree + thumbnail grid, per-card "Move to…" dropdown, inline folder creation). SVG committed at `design/wireframes/reclassify-ui.svg` as the lasting design record. The `/organize` page is now built, tested, and clean (one High-severity bug found and fixed along the way — see Open Bugs/Last Developer Action). This also resolves the Option-B inheritance wrinkle flagged earlier — Option A was already Developer's recommendation specifically because it "scales naturally into the Week 7–8 folder browser," so the folder browser inheriting this pattern (minus move/reclassify actions, plus the same "Unfiled" concept this cycle discovered is necessary) stays the plan with no tension. **Week 5–6 is fully closed.**

0.5. **Dashboard page — THE ONLY REMAINING BLOCKER, wireframe pick needed to fully close Week 7–8.** Three options proposed by Planner this cycle (not yet built into any SVG file — describing them here per protocol, matching how Option A/B/C were first proposed for the reclassification UI):
   - **Option A — Stat cards + collection list.** Familiar SaaS-overview pattern, low design risk. Con: with only one collection in practice today, the collection-list section is currently a single sparse row.
   - **Option B — Storage-meter hero + folder shortcut tiles.** Gives the storage number (the stat users most concretely care about) the most visual weight, with folder-tile shortcuts more useful than a one-row collection list given today's one-collection/many-folders reality. Con: slightly more assembly (pulls from two endpoints' worth of data, not one). **Planner's slight lean, not a strong steer.**
   - **Option C — Sidebar nav shell with stats as the default pane.** Would solve the app's real lack of persistent navigation across pages. Con: scope creep — every existing page (`/upload`, `/organize`, `/browse`) would need the shell to make it feel real, turning a small dashboard spec into a mini redesign. **Flagged for completeness, not recommended for this pass.**
   
   Everything this page needs from the backend (`GET /api/dashboard`) is already built and live-verified. Once picked, Developer builds directly against it — small, self-contained, should be fast.

**New this cycle — 10 flagged assumptions from `specs/ai-classification.md`, none blocking (Developer proceeded on stated defaults, all shipped and tested), worth explicit confirmation or veto:**

1. **Default collection semantics.** One per-user default "My Photos" collection, created lazily by the worker at first folder assignment, `@@unique([ownerId, name])`. Side effect: user collections can't duplicate names later.
2. **Mapping-table aliases.** Added Landscape/Nature/Outdoor as Nature aliases beyond the roadmap's literal table (fixture-only change, provider untouched). Veto if the mapping table must stay roadmap-verbatim.
3. **"Secondary tags" scope.** Primary folder is real; secondary tags remain the existing raw `aiLabels` array, no separate tags table yet.
4. **Reclassify clears the duplicate verdict.** Reclassifying a `duplicate` photo clears `duplicateOfPhotoId`/`dedupMethod` — the deliberate escape hatch for pHash/sha256 false positives. Veto if duplicates should stay locked until a delete/merge flow exists.
5. **Reclassify rate limiter numbers.** Own per-user bucket, 30/15min, independent of upload's and auth's. Numbers open to veto.
6. **Auth rate-limit split numbers.** Signup 5/15min/IP, login 10/15min/IP, both relaxed under `NODE_ENV=test`. Confirmed working by two clean Tester runs — numbers open to veto if you'd prefer different values.
7. **pHash flat-image fix approach.** SHA-256 exact-hash first pass + skip pHash comparison when either hash is the degenerate all-zeros value. Accepted residual: two near-identical-but-not-byte-identical flat photos (e.g. two shots of the same blank wall) will no longer dedup against each other. Confirmed working by Tester's exact repro.
8. **Manual folder creation pulled into this pass.** `POST /api/collections/:id/folders` (create-only) shipped now since the reclassification UI needs move targets; rename/merge/delete stay deferred to Week 7–8.
9. **Low-confidence test hook.** `FORCE_LOWCONF_` filename hook (forces confidence to 0.42) rather than widening the mock's real confidence range, to avoid destabilizing every other classification test.
10. **Job/dedup observability fields.** `GET /api/photos/:id/status` now exposes internal job bookkeeping (`type`, `status`, `attempts`, `errorMessage`) and `dedupMethod` to the client — added mid-cycle so these paths could be Tester-verified at all, rather than staying permanent "code-review only" gaps. Veto if internal job state shouldn't be client-visible.

None of these block further work — all ten defaults are already built, shipped, and passed a full regression cycle. Flagging per protocol; treat silence past this point as implicit acceptance, same as the upload-pipeline batch above.

**New this cycle — 4 flagged assumptions from `specs/dashboard-stats-and-upload-polish.md`, none blocking (already built and tested), worth explicit confirmation or veto:**

11. **`totals.photoCount` scope.** Counts every photo regardless of status (pending/processing/done/duplicate/failed), treated as a storage-consumption stat rather than an "organized library" stat. Veto if it should narrow to `done`-only or exclude `failed`.
12. **Dashboard-page wireframe sequencing.** Default: after the reclassification-UI pick, not in parallel (see Pending Decision 0.5 above). Veto if you'd rather see both proposed now.
13. **Folder browser inheriting the reclassification UI's pattern — RESOLVED and SHIPPED this cycle.** Built as a genuinely separate route (`/browse`), not a mode flag inside `/organize` — Planner's stated reasoning: `/organize`'s state model is tightly coupled to editable-grid concerns (per-card mutation state, poll timers) that a read-only mode would need to thread a flag through everywhere, leaving untested dead paths live. `/browse` reuses the same visual pattern and the "Unfiled" concept, with zero new backend work needed. No veto received.
15. **Photo viewer — judged not to need its own wireframe round, built directly.** Planner's reasoning: fullscreen-image-plus-info-panel is one of the most standardized patterns in photo-product UI, with no real competing layout options the way `/organize` or the Dashboard page have — same test the upload-page polish passed (elaboration of an existing surface, not a new information-architecture decision). Shipped as a shared component used by both `/browse` and `/organize`. Veto if you'd have preferred options presented first.
14. **Upload progress bar implementation.** Plain `XMLHttpRequest`, zero new dependencies (chosen specifically because `fetch` cannot report upload progress). Veto if you'd rather standardize on a progress-capable fetch library now, in anticipation of future multi-file upload work.

## Next Scheduled Actions
- 06:00 daily — Tester Agent regression pass (Docker/dev-server prerequisites unchanged, see Notes/Risks — still not automated via cron in this environment).
- 18:00 daily — Tester Agent regression pass.
- **Next up: Abhishek picks a Dashboard-page wireframe (Pending Decision 0.5, options A/B/C above).** This is the only remaining item in Week 7–8. Once picked, Developer builds it against the already-shipped `GET /api/dashboard` — small, should be fast, closes out Week 7–8 entirely.
- If no pick arrives first: nothing else is currently buildable without a new UI or roadmap decision — Week 1–8 of the roadmap's local-first slice is otherwise complete. The next cycle should either wait, or Master should propose scoping Week 9–10 (Guest Access + OTP, per the roadmap) as a genuinely new planning conversation rather than manufacture more speculative work.

## Notes / Risks
- **GitHub remote is connected; `feature/upload-pipeline` and `feature/ai-classification` (through commit `3435f8c` only) are pushed**, per Abhishek's explicit go-ahead earlier today — `master` untouched throughout, no PRs opened. **Abhishek has said the next push will happen later tonight — seven more commits are local-only in the meantime:** `510cc30`, `e45aa1d`, `9669dfa`, `69363fb`, `37c6e8e`, `bd23c17`, `243b338` (dashboard-stats/upload-polish, two STATUS.md reconciliations, the organize UI build + bug fix, the Week 7-8 remainder spec, and the browse+viewer build). Still needs a fresh explicit go-ahead when the time comes — this note is tracking state, not standing permission.
- **A separate `feature/local-dashboard` branch exists and is pushed**, containing 4 files (`CLAUDE.md`, `agents/MASTER.md`, `agents/PLANNER.md`, `.gitignore`) that wire a personal local-only "insights dashboard" tool into the coordination loop (an untracked `dashboard/` folder Abhishek runs separately, gitignored by design). **Not merged anywhere**, does not apply to `feature/ai-classification` or any other branch — don't act on `dashboard/FEATURE_QUEUE.md` mentions from that branch's `MASTER.md` unless it's actually checked out or merged.
- Both dev servers (backend `:4000`, frontend `:3000`) plus the worker (`npm run worker -w backend`) have been running continuously since at least 2026-07-02 and were confirmed healthy throughout the entire day across four Tester runs (40+ hours Docker uptime). They will not survive a machine reboot or VS Code restart — check `curl localhost:4000/health` before assuming they're up in a future session. **Stale duplicate `tsx watch` processes now confirmed for BOTH `src/worker.ts` and `src/server.ts`** (not just `server.ts` as flagged before) — three extra idle processes total per the 07:31 report's `ps aux` check. All benign (only one process per port can bind, and only one worker instance is actually connected/consuming), but worth a `pkill`-and-restart-clean pass next time someone's at the machine, and worth confirming the stale ones aren't silently double-processing BullMQ jobs rather than just idling.
- Docker Compose (Postgres/Redis/MinIO) has been stable and healthy across the entire day — no recurrence of the earlier credential-helper blocker.
- Real AWS/GCP services (S3, Vision API, EKS, Terraform) remain fully deferred — the AI-classification cycle went to considerable lengths to keep that boundary airtight (dependency-graph checks, stubbed-network tests) precisely because "swap in real Vision" is meant to stay a one-file change whenever it's approved.
- UI design decisions stay SVG-only, no Figma. The reclassification-UI round is fully closed (Option A picked, built, shipped) and the Folder browser/Photo viewer were judged not to need their own rounds (reasoning in Pending Decisions 13/15) — no veto received on either judgment call. **Exactly one wireframe round remains for the entire local-first MVP roadmap: the Dashboard page** (Pending Decision 0.5, options A/B/C above).
- **On reduced-review builds:** three Developer sessions today were interrupted by platform usage limits before reporting back (this cycle's earlier organize-UI build, twice during the AI-classification cycle). Each time the underlying work was still completed and recoverable — by resuming the same agent, or (when the safety classifier was also unavailable) by Master independently re-running typecheck/lint/tests and reading the actual diffs before trusting and committing. This cycle's browse+viewer build completed normally with no interruption, for comparison. The pattern has now repeated enough times across the day to note as a real, recurring operating condition rather than a one-off, but it hasn't cost correctness so far — the real bugs that did slip through were caught by Tester's deliberate exploratory testing, not by the interruptions themselves.
- MR drafts: `reports/mr-drafts/feature-ai-classification.md`, `reports/mr-drafts/dashboard-stats-and-upload-polish.md`, `reports/mr-drafts/organize-reclassification-ui.md` (incl. bug-fix addendum), `reports/mr-drafts/browse-and-photo-viewer.md`.
