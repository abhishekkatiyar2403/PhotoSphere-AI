# PhotoSphere AI — Live Status

> Single source of truth for what's happening right now. Every agent reads this first and updates it last. Master Agent owns this file — Planner, Developer, and Tester propose updates, Master reconciles conflicts.

**Last updated:** 2026-07-04 (later) by Developer Agent (built the three Guest Access frontend pages — `/share`, `/guests`, `/g/[token]` — commit `a2cfb3f`, against the already-built/tested backend and the already-picked wireframes; verified live via Playwright incl. the no-real-image-before-approval privacy constraint; see Last Developer Action + `reports/mr-drafts/guest-access-ui.md`). Prior: full orchestration cycle — Abhishek picked "scope Week 9–10 (Guest Access + OTP)" → Planner scoped `specs/guest-access-otp.md` → Master reviewed the security model → Developer built the backend/API/data-model core (commit `dad0b74`, 71/71) → Master restarted the backend with OTP-exposure on for live testing → Tester ran an adversarial security pass: **68/68 live assertions + backend suite 71/71, 0 bugs, 0 security holes — the guest-access backend is verified SAFE and CORRECT.** Guest-UI wireframes PICKED 2026-07-04 (U1→B dedicated /share page, U2→C prioritized feed, U3→B preview-first — records in `design/wireframes/`). G1–G12 remain open for veto (built-on + tested). Nothing pushed since 2026-07-03 (needs go-ahead — push deferred to "later today"). Prior cycle: Next.js hardened → 14.2.35, stale process cleaned, backlog pushed with Claude trailers stripped — see Notes/Risks.)

---

## Current Phase
Phase 1 — MVP, **Week 1–8 local-first slice COMPLETE** (Auth, Upload Pipeline, AI Classification + reclassification UI, Core UI — all shipped and regression-tested clean, zero open bugs). **Week 9–10 (Guest Access + OTP) backend is BUILT and TESTED CLEAN** (`specs/guest-access-otp.md`, commit `dad0b74`, 68/68 live security assertions + 71/71 backend suite), and **the three guest frontend pages are now BUILT** (commit `a2cfb3f`, wireframes U1–U3 already picked): `/share` (Option B), `/guests` (Option C), `/g/[token]` (Option B). Not yet regression-tested by the Tester Agent. G1–G12 (backend) remain open for Abhishek's veto, same as last cycle.

## Current Sprint Goal
**Week 9–10: Guest Access + OTP — backend + data-model core.** An owner shares specific folders with a guest via an invite link; the guest requests access; a 6-digit OTP (mocked delivery, no real email/SMS) goes to the OWNER for real-time approval; on approval the guest gets an opaque, revocable guest session scoped to exactly the shared folders, with every image still served as a 60s pre-signed URL. Owner can revoke instantly. Build target: 5 additive Prisma models, a guest-session helper + `requireGuest` middleware with a single `getPermittedFolderIds()` scope choke point, a swappable mock-OTP notification module, and four routers (owner share-management, owner approval, guest-public invite entry, guest portal) — all on the existing opaque-token / 404-not-403 / Zod / pre-signed-URL conventions. **No UI in this build.**

## Ready Spec (from Planner)
- `specs/ai-classification.md` — **built and shipped.**
- `specs/dashboard-stats-and-upload-polish.md` — **built and shipped.**
- `specs/week7-8-dashboard-browser-viewer.md` — **built and shipped, fully.** Folder browser + Photo viewer shipped last cycle; the Dashboard page (its one remaining blocked-on-wireframe section) is now built too, after Abhishek's Option B pick.

- **`specs/guest-access-otp.md` — backend core BUILT (commit `dad0b74`, local only) and TESTED CLEAN 2026-07-04 (68/68 live security assertions + backend suite 71/71, 0 bugs, 0 security holes).** 5 additive models + migration `20260703215312_add_guest_access_otp`, `lib/guestSession.ts` + `middleware/requireGuest.ts` (+ `getPermittedFolderIds()` choke point), `lib/notifications/index.ts` (swappable mock OTP), `lib/otp.ts`, `middleware/inviteRateLimiter.ts`, and the four routers — all on the G1–G12 defaults. **The frontend is now also BUILT** (commit `a2cfb3f`, local only): `/share`, `/guests`, `/g/[token]` — see Last Developer Action below and `reports/mr-drafts/guest-access-ui.md`. **Week 9–10 is now feature-complete pending a Tester regression pass.** G1–G12 remain open for veto (built-on and tested, no objection received).

The earlier Week 1–8 specs are all committed to git and shipped. `specs/guest-access-otp.md` is the one active spec.

## Last Tester Run
**2026-07-04 03:58 — Guest Access + OTP backend security-focused regression (commit `dad0b74`), 68/68 live assertions clean + backend suite 71/71, 0 bugs, 0 security holes.** Report: `reports/2026-07-04_0358.md`. First test pass on the Week 9–10 Guest Access + OTP backend core. **Verdict: the guest-access backend is SAFE and CORRECT.** Reproduced the spec's full Success Signal live end-to-end (owner shares 2 of 4 folders download-level → anon invite request → OTP read via the flag-gated dev endpoint → owner approve → G7 poll-claim sets the httpOnly guest cookie → guest lists EXACTLY the 2 shared folders, views a photo via a real 60s `X-Amz-Expires=60` pre-signed MinIO URL, downloads it, is 404'd on an unshared folder → owner revokes → guest's next request 401). Every security-critical assertion passed adversarially and is called out explicitly in the report: OTP single-use (2nd approve→409, hash NULLed), 3-wrong→auto-deny (DB `otp_attempts`/`status` verified, correct code after→403), 5-min expiry (DB-time method→403 `expired`), cross-scope leakage (guest 404s on owned-but-unshared folder AND on a different owner's folder/photo/download; unshared folder ABSENT from the list, not just denied), cross-owner isolation (B approve/deny/DELETE of A's guest all →404-not-403, zero side effect), no/invalid/revoked/expired guest session →401, opaque-not-JWT with hash-only persistence (invite token, guest token, OTP — no plaintext anywhere in `invite_tokens`/`guest_sessions`/`access_requests`), G5 view-only download→403 while download_all→200, token enumeration (garbage/expired/exhausted all →byte-identical generic 404), and invite-request rate limiting (429 past its own 10/15min IP bucket, independent of owner endpoints). Migration confirmed additive-only (5 CREATE TABLE + FK-only ALTERs on the new tables; nothing touched on users/photos/collections/folders/sessions). Offline-notification test 3/3 + zero email/SMS/JWT SDKs in deps + plaintext gated on `NOTIFICATIONS_EXPOSE_OTP`/`NODE_ENV=test`. **Priority-3 regression clean:** owner endpoints (`/collections`, `/folders`, `/photos/unfiled`, `/dashboard`) unaffected, guest routers mounted with route order intact, live login+upload happy-path + bad-magic-bytes rejection, and all four authed frontend pages (`/dashboard`/`/organize`/`/browse`/`/upload`) + `/login` load with no non-401 errors (Playwright). **Two harness artifacts, NOT app bugs (documented for the next Tester):** (1) live auth/invite rate limiters are armed since the server runs `NODE_ENV=development` — repeated setup signups/requests exhaust the IP buckets and cascade 429s; worked around by seeding owner accounts+sessions directly via Prisma (fixture setup only; the guest flow stayed black-box over HTTP) and by budgeting requests + waiting out the 15-min window; the limiter firing is itself a PASS; (2) a login-looks-broken red herring — seeded owners had a mixed-case email but `loginSchema` correctly `.toLowerCase()`s the input, so lookup missed; this VALIDATES email normalization (login→200 after lowercasing the seeded row). Housekeeping: process tree clean (one server.ts, one worker.ts, one next dev, single :4000 listener); test fixture data cleaned up post-run.

**2026-07-03 23:40 — post-dependency-upgrade regression (Next.js 14.2.13 → 14.2.35, commit `ad441da`), 53/53 app-clean, 0 bugs.** Report: `reports/2026-07-03_2340.md`. The ONLY change since the last report was the frontend framework patch bump (verified: `git show --stat` touches only `frontend/package.json` + `package-lock.json`; running binary confirmed `next-server (v14.2.35)`, hoisted `node_modules/next` = 14.2.35). Re-exercised the entire app surface because a Next patch bump can plausibly affect client routing, hydration, `useSearchParams`/Suspense, static-vs-dynamic rendering, and headers — all verified intact. **Verdict: clean, regression-free upgrade.** Covered live: auth (gate→UI signup→logout→login), upload (happy-path + throttled XHR progress-bar intermediate values + real-DataTransfer drag-and-drop + bad-magic-bytes rejection), organize (sidebar tree/counts, move/reclassify/inline-create controls, Unfiled bucket, **and the fresh-user empty state re-verified NOT to repeat the earlier crash**), browse (read-only confirmed by DOM absence AND zero PATCH/POST during the session, the **`?folder=` deep-link — the Suspense+`useSearchParams` surface a Next bump is most likely to break — lands on the right non-first folder and falls back safely on garbage input**, viewer full-res original + EXIF panel with real fields + prev/next + all three close methods), and dashboard (storage-meter no-NaN width cross-checked against a LIVE `GET /api/dashboard` oracle, folder/Unfiled/viewall tiles, at-a-glance totals, both populated and fresh-empty states). One assertion initially tripped and was root-caused to a **test-classifier scope gap, not an app defect** — three pre-login auth-gate 401s (`/api/dashboard`, `/api/collections`, `/api/photos/unfiled`), the same benign class prior reports warned against misattributing; re-verified clean two ways (isolated authed-first-paint load → 0 401s / 0 pageerrors / 0 hydration warnings). Backend automated suite **56/56** (unaffected — zero backend changes). Frontend typecheck clean, `next lint` (through `eslint-config-next@14.2.35`) → no warnings/errors. **Housekeeping: the stale `tsx watch src/server.ts` process — flagged five consecutive reports — is now GONE** (exactly one server.ts + one worker.ts process, single :4000 listener); dropping it from the standing list unless it reappears.

**2026-07-03 19:02 — Dashboard page regression, 69/69 passed, 0 bugs.** Report: `reports/2026-07-03_1902.md`. Tested the new Option B Dashboard page against commit `fcff301` with the same adversarial scrutiny the last few cycles established, and specifically targeted the fresh-user empty state (where two real bugs hid earlier this week) — it renders cleanly: "0 B of 5.0 GB used", 0% meter with no NaN/broken width, no folder/Unfiled tiles, zero console errors on its own render, and crucially does NOT repeat the `/organize` fresh-user crash from earlier. Populated state verified too: storage meter fill cross-checked against the raw `GET /api/dashboard` `usedPercent`, folder tiles with correct singular/plural counts, Unfiled tile appearing only when count > 0, "At a glance" matching the API totals verbatim. Deep-links confirmed: folder tiles land on the right `/browse` folder (not the first), garbage/empty `?folder=` values fall back safely, and a user's sidebar click still wins over the deep-link (param doesn't lock selection). Two initially-tripped automated assertions were both run down and confirmed test-harness artifacts, not app bugs (CSS-float serialization on the meter width; pre-login `auth/me` 401s misattributed as dashboard errors). Regression spot-checks clean: `/browse` still read-only (0 mutation controls, 0 PATCH/POST), `/organize` and `/upload` still load despite the shared `api.ts`/`globals.css` touches. Backend suite 56/56 (unaffected — zero backend changes this cycle). Stale `tsx watch src/server.ts` process now a fifth consecutive flag.

**2026-07-03 16:47 — Folder browser (`/browse`) + shared Photo viewer regression, 53/54 passed, 0 app bugs.** Report: `reports/2026-07-03_1647.md`. The single failure was Tester's own test-fixture sizing mistake, root-caused and independently re-verified clean — not an app defect. Given the project's track record today (two real bugs found earlier via deliberate exploratory testing), Tester was explicitly asked to bring the same scrutiny here — and found nothing, a genuinely different outcome worth noting on its own: `/browse` confirmed read-only both by DOM absence AND by network monitoring (zero PATCH/POST fired during interaction, only GETs), the viewer's image confirmed to be the true full-resolution original (not a thumbnail), EXIF panel correctness on both populated and empty cases, Prev/Next correctly scoped to the loaded page with no wraparound (including the true single-photo-folder edge case), all three close mechanisms (X/Escape/backdrop-click — the backdrop-click bug Developer found and fixed on itself was re-verified genuinely fixed, not just trusted), and cross-surface integration confirmed (`/organize`'s pre-existing Move/Reclassify unaffected by the new shared viewer). Adversarial/break-it testing (rapid double-click and 10-click Next/Prev spam, browser back button with viewer open, narrow-viewport layout) surfaced nothing. Backend suite unaffected (56/56, zero backend changes this cycle). Housekeeping carried forward: the stale duplicate `tsx watch src/server.ts` process is now a fourth consecutive flag.

**2026-07-03 08:55 — fix verification, 30/30 passed, 0 bugs.** Report: `reports/2026-07-03_0855.md`. Focused re-test of the High-severity bug found earlier this cycle (below) after Developer's fix (commit `69363fb`): reproduced the exact original repro live and confirmed it's gone, confirmed the duplicate variant (using a stronger repro than Developer's own new test — both uploads `FORCE_FAIL_`-prefixed so the base photo genuinely never classifies, closing a gap noted for Developer's awareness), confirmed the second latent bug (sidebar not picking up a newly-created folder after a user's first-ever successful reclassify) is also fixed, and regression-spot-checked move/count-reconciliation and cross-user isolation since the fix required a refactor (moving shared helpers to new `lib/photoCard.ts`/`lib/storageKeys.ts` to avoid a circular import) — confirmed byte-for-byte identical logic, no behavioral drift. Backend suite: 56/56, run 5 times across the two verification sessions, zero flake.

**2026-07-03 07:31 — organize UI regression, 27/27 scripted passed, 0 bugs in scripted tests — but 1 High-severity bug found via deliberate exploratory testing beyond the task's suggested steps.** Report: `reports/2026-07-03_0731.md`. Every acceptance-criteria item explicitly in scope passed exactly as specced: end-to-end folder navigation, move with server-side count reconciliation (verified independently, not just trusting the UI), the "Unfiled" bucket's styling/labels/duplicate-filename resolution, reclassify and "not a duplicate?" recovery (verified via the status endpoint, not just visually), inline folder creation (incl. 409/empty-name), pagination across a genuine 13-photo folder, cross-user 404 isolation on the new endpoint, and the `folders.ts` refactor's regression path. **The bug:** a brand-new user whose very first photo fails or dedupes *before any collection has ever been created* had that photo permanently unreachable — the page's initial-load effect bailed out entirely on "no collections yet" without ever checking for unfiled photos independently. Found specifically because Tester deliberately sequenced uploads in an order the task's own suggested steps never would have (failure-before-any-success, rather than after populating real folders first) — flagged as exactly the kind of gap a reduced-review cycle (this Developer session was interrupted by a usage limit, see below) is most likely to miss. Root-caused precisely, screenshot captured, confirmed genuinely likely in practice (any real user whose first upload happens to fail or dedupe hits this), not a contrived edge case.

**2026-07-03 02:27 — dashboard-stats + upload-polish regression, 20/20 passed, 0 bugs** (plus 52/52 on the full backend automated suite, zero regressions). Report: `reports/2026-07-03_0227.md`. Scope was deliberately focused: a handful of load-bearing spot-checks (login, upload happy-path, cross-user 404) confirmed no regression from this small addition, then full live coverage of the two new pieces.

- **`GET /api/dashboard`:** live-verified end-to-end — 401 with no session, exact empty-state shape for a fresh user, correct aggregation after uploading across categories (a `FORCE_FAIL_` photo and a byte-identical duplicate both correctly counted in `totals.photoCount` per the spec's stated default, while correctly excluded from folder assignment), storage bytes cross-checked byte-for-byte against independently-computed upload sizes, and confirmed cross-user isolation with zero leakage either direction.
- **Upload page polish:** drag-and-drop genuinely populates file state and uploads successfully (verified via real `DataTransfer` construction, not a Playwright shortcut), visual drag-over feedback confirmed, a dropped bad-magic-bytes file is rejected identically to click-to-browse, and click-to-browse regression (both happy-path and rejection) still works unchanged. **The progress bar claim was verified, not just code-reviewed:** under throttled network conditions, Tester captured 3,521 samples with 181 distinct genuine intermediate progress values, monotonically increasing — definitive proof the `xhr.upload.onprogress` wiring is real (a plain local upload completes in under a second, too fast to observe intermediate values without throttling — Tester's methodology section explains why throttling was necessary rather than a shortcut).

**Prior run for comparison — 2026-07-03 00:50 — full regression (auth + upload pipeline + AI classification), 52/52 passed, 0 bugs.** Report: `reports/2026-07-03_0050.md`. Run was dispatched 2026-07-02 evening, interrupted mid-run by a session usage limit before any live traffic, and resumed after midnight against the identical commit/stack — nothing invalidated by the pause. Headless Playwright + curl/node-fetch (VS Code session, not Cowork live-browser control).

- **Auth regression (15/15):** full baseline re-verified, plus the new split rate-limit buckets confirmed live at exact production numbers — signup 429 at the 6th in-window request, login 429 at the 11th, fully independent of each other and of uploads. **This is the first run with zero rate-limit-induced workarounds**, after two consecutive prior runs needed a benign backend restart mid-suite.
- **Upload pipeline regression (16/16):** full baseline re-verified. Two items graduate from "code-reviewed only" to **live-verified for the first time**: EXIF fields (new `exif` object on `GET /api/photos/:id`) and exact `attempts=3`/`errorMessage` on forced failure (new `job` object on the status endpoint) — both were open testability gaps in the 2026-07-02 report, both closed by this build.
- **AI classification (21/21):** every `[Tester-live]` acceptance criterion in the spec passed — all 7 category mappings, folder reuse + photoCount reconciliation, Uncategorized via both unmappable labels and the new `FORCE_LOWCONF_` test hook, a genuine concurrency race (two simultaneous uploads into a brand-new category → exactly one folder row), pagination validation, PATCH move, all four reclassify paths (rescue a `failed` photo, rescue a `duplicate` photo, 409-while-in-progress, its own independent 30/15min rate bucket), cross-user 404 isolation and 401s across all six new endpoints, and the full job/dedup observability surface.
- **Flat-image false positive (2026-07-02 finding) — confirmed fixed.** Re-ran the exact red/blue flat-rectangle repro: both process independently to `done`; a byte-identical re-upload correctly resolves `duplicate` via `dedupMethod: "sha256"`.
- **Not independently live-verifiable (unchanged constraints, not failures):** storage-quota 413 (impractical without DB access to shrink the 5GB default), worker kill/restart durability (can't manage Abhishek's local processes from this session). Plus 8 items explicitly out-of-scope by the spec's own verification legend (`[Developer-verified]`/code-review-only: 0.60 threshold boundary, `file_sha256` DB state, mock call-counter, double `npm test` run, fresh-DB migration replay, offline classification, DB-level bookkeeping half, and — separately — the reclassification UI itself, correctly not built). Developer's MR draft (`reports/mr-drafts/feature-ai-classification.md`) records verification for all of these.
- **Two housekeeping notes from Tester:** a stale duplicate `tsx watch src/server.ts` process is running alongside the live one (benign, only one can bind :4000, worth killing next time someone's at the machine); and a methodology note for future Testers — derive companion test-image expectations from your own generated bytes via the real `classify()`, not from the fixtures README's table (the mock is byte-deterministic, so a differently-encoded "gray image" won't match another session's).

Prior baseline for comparison: 2026-07-02 04:50 run, 28/28, first to test the upload pipeline; found the flat-image pHash issue now fixed. 2026-07-01 run, 17/17, auth only.

## Last Developer Action
**2026-07-04 (later) — Developer Agent built the three Guest Access frontend pages, commit `a2cfb3f` on `feature/ai-classification` (local only, NOT pushed; no `Co-Authored-By: Claude` trailer, author Abhishek).** Built against the already-tested backend (`dad0b74`) and the three already-picked wireframes — no wireframe re-proposal, no backend changes. Shipped: `frontend/src/app/share/page.tsx` (owner, Option B two-column — folder multi-select + tally, guest email/permission/expiry, generated invite link with Copy), `frontend/src/app/guests/page.tsx` (owner, Option C single prioritized feed — pending-request cards with inline OTP entry float above the guest roster with one-tap Revoke), `frontend/src/app/g/[token]/page.tsx` (guest, PUBLIC — no owner auth-gate; one page, local `'landing'|'waiting'|'unlocked'` state machine, 5s status-poll interval). Extended `frontend/src/lib/api.ts` with `guestsApi`/`accessRequestsApi` (owner) and `invitesApi`/`guestPortalApi` (guest), same `apiFetch`/`ApiError`/`credentials:"include"` conventions as every existing API group. Entry point: a "Guests" link folded into `/dashboard`'s top bar next to Log out (same precedent as logout itself, no new nav shell). **PhotoViewer was NOT reused for the guest photo detail view** — a minimal guest-scoped equivalent was built inline instead, because `PhotoViewer`'s fetch/prop contract (`photosApi.get()`, owner-shaped `PhotoDetail`/duplicate-status labels) doesn't fit a guest's `done`-only, permission-scoped world; reasoning and the exact judgment call are in the MR draft. **The privacy constraint (no real image/pre-signed URL/API call before approval) was verified live with Playwright, not just code-reviewed:** a guest sitting on the landing page fires zero `/api/*` requests, the locked-preview grid contains zero `<img>` elements (6 static decorative tiles only), and `/api/guest/*` calls were confirmed to only start after the state machine reaches `unlocked`. Also verified live via real browser clicks in separate owner/guest contexts: the full share→request→OTP-approve→poll-unlock→scoped-browse→download flow, a `view`-only guest correctly getting zero download buttons (absent, not disabled), both owner pages redirecting to `/login` when logged out, and the guest portal NOT redirecting (and handling a garbage token cleanly) since it's intentionally unauthenticated. Verifications: `npm run typecheck -w frontend` clean, `npm run lint -w frontend` clean, `npm run build -w frontend` (real production build) clean with all three new routes present and no Suspense issue. No backend files touched (confirmed via `git status --short backend/` before committing). MR draft: `reports/mr-drafts/guest-access-ui.md`. **`specs/guest-access-otp.md` is now fully shipped (backend + frontend), pending only a Tester regression pass on the new pages.**

**2026-07-04 — Developer Agent built the Guest Access + OTP backend/API/data-model core, commit `dad0b74` on `feature/ai-classification` (local only, NOT pushed; no `Co-Authored-By: Claude` trailer, author Abhishek).** Built against `specs/guest-access-otp.md` §1–7 on the G1–G12 defaults. **No UI** (the 3 guest UIs U1–U3 stay wireframe-blocked). Shipped: additive migration `20260703215312_add_guest_access_otp` (5 models `GuestUser`/`InviteToken`/`FolderPermission`/`AccessRequest`/`GuestSession` + `User.guestsInvited`/`Folder.permissions` back-relations; geo omitted G8, audit deferred G9 — verified additive-only, no ALTER/DROP on existing tables); `lib/guestSession.ts` (opaque guest tokens mirroring `session.ts`, distinct `photosphere_guest_session` cookie, TTL capped at earliest permission expiry, G2); `middleware/requireGuest.ts` with the `getPermittedFolderIds()` single scope choke point; `lib/notifications/index.ts` swappable mock OTP provider (no real email/SMS/JWT/cloud, plaintext gated on test flag, one-file swap-in like the classifier); `lib/otp.ts` (6-digit crypto OTP, SHA-256, constant-time verify); `middleware/inviteRateLimiter.ts` (own IP buckets, G11); four routers `/api/guests`, `/api/access-requests`, `/api/invites`, `/api/guest` wired in `app.ts` (route order per §7). **G7 handoff built as a flagged variant:** session minted at the guest's status-poll *claim* (not at approval) via a `session_claimed_at` claim-once latch, because a raw token can't be recovered from a hash — net behavior matches the spec intent exactly (raw token never persisted, never touches the owner, single-use, durable), contained to `routes/invites.ts` + one nullable column. **Two other small deviations flagged in the MR draft:** a test/dev-only gated `GET /api/invites/requests/:requestId/otp` (so Tester can read the OTP per the spec's own success signal) and two documented `.env` vars (`GUEST_SESSION_TTL_HOURS`, `NOTIFICATIONS_EXPOSE_OTP`; `.env` is gitignored). Verifications: backend typecheck + lint clean; **full backend suite 71/71** (was 56/56; +12 guest security-integration tests, +3 offline notification tests), run twice with zero flake; migration applied cleanly and also replayed from scratch via `migrate reset` during dev. Security-critical AC all covered live: OTP 3-attempt cap→auto-deny, single-use, 5-min expiry, cross-scope 404 leakage (unshared-but-owned folder + other owner's photo), instant revocation→401, 404-not-403 on cross-owner approve/deny/delete, guest 401s, view-only download→403, and an offline test proving the notification module can't reach the network. MR draft: `reports/mr-drafts/guest-access-otp.md`. **`specs/guest-access-otp.md` backend core — SHIPPED.**

**2026-07-03 (later) — Developer Agent ran the deferred Next.js dependency-hardening pass, commit `ad441da` on `feature/ai-classification` (local only, NOT pushed).** Bumped `next` and `eslint-config-next` from `14.2.13` → `14.2.35` (latest patch on the `next-14` dist-tag), kept in lockstep, updated `package-lock.json` via a root `npm install` (npm workspaces). **No app source touched** — only `frontend/package.json` + `package-lock.json`. All three verifications clean under 14.2.35 with zero fixes required: `next build` (real production build — all 10 routes incl. `/browse`'s Suspense boundary + RSC/static generation), `tsc --noEmit`, and `next lint` (`✔ No ESLint warnings or errors`). **Audit delta:** the sole **critical** advisory is eliminated and 10 `next` CVEs cleared (auth-bypass ×2, SSRF, dev-server info exposure, image-optimization content injection, several DoS/cache-poisoning); `next` advisory count 24 → 14. **14 `next` advisories remain, plus the transitive `glob` (high, build-time-only dev dep, non-reachable) and `postcss` (moderate)** — all of these only clear via a **Next 15→16 major migration**, which is a breaking change and deliberately NOT done here (`npm audit fix --force` explicitly avoided — it installs next@16). **A full audit-clear requires a Next major migration — flagged as a separate decision for Abhishek, not actioned.** MR draft: `reports/mr-drafts/nextjs-hardening-bump.md`.

**2026-07-03 (night) — Developer Agent shipped the Dashboard page (Option B), commit `fcff301` — the last Week 7–8 screen.** Storage-meter hero (human-readable used/limit formatted client-side from the API's string byte values, progress bar filled to `usedPercent`), a "Jump to a folder" tile grid (per-folder name/count, a stand-out Unfiled tile shown only when count > 0, a dashed "View all folders" tile), and an "At a glance" totals recap. Replaced the Week 1–2 auth-proof placeholder that lived at `/dashboard`. **Data-assembly choice (Option 1, no backend change):** `GET /api/dashboard` supplies storage + totals, but its rollup is per-collection, not the per-folder breakdown the Option B tiles need — so tiles are assembled from the already-shipped `GET /api/collections` → `/api/collections/:id/folders` + `GET /api/photos/unfiled`, the same multi-fetch-on-mount pattern `/organize` and `/browse` use. Two deviations, both flagged and reviewed by Master: (1) a minimal, well-reasoned `?folder=<id>` deep-link param added to `/browse` (Suspense-wrapped for Next 14's `useSearchParams`, honored once on initial load, never overrides a later user click) so tiles can land on the right folder per the spec's AC — dashboard is its only caller; (2) committed `design/wireframes/dashboard.svg` (Abhishek's Option B pick) unmodified as the version-controlled design record. Master independently re-verified before trusting: frontend typecheck/lint clean, and confirmed the `unfiledPhotosApi` signature was NOT changed this cycle (it was already collection-independent from the `69363fb` bug fix — no hidden shared-code change). MR draft: `reports/mr-drafts/dashboard-page.md`.

**2026-07-03 (evening, later) — Developer Agent shipped the read-only Folder browser + shared Photo viewer, commit `243b338`.** New route `/browse` (sidebar + paginated grid, same visual pattern as `/organize` but zero mutation surface: no move dropdown, no reclassify buttons, no folder creation) and a new shared `PhotoViewer` component (fullscreen image + EXIF panel, prev/next navigation scoped to the current page, three independent close methods) wired into both `/browse` and the existing `/organize`. Deliberate judgment call, stated and reasoned: shared the viewer component and CSS wholesale, but duplicated the small read-only card markup rather than threading a `readOnly` flag through `/organize`'s tightly-coupled editable `PhotoCard` — kept `/browse` a genuinely separate, simpler route rather than a mode flag. Zero backend changes needed (confirmed by reading the actual route code before assuming, per the spec's own instruction). Found and fixed one real bug on itself during manual verification: backdrop-click-to-close initially never fired because the fullscreen overlay had no exposed backdrop margin to click — fixed and re-verified. Typecheck/lint clean (independently re-verified by Master), backend suite re-run as a sanity check despite being untouched (56/56, unchanged). MR draft: `reports/mr-drafts/browse-and-photo-viewer.md`.

**2026-07-03 (evening) — Developer Agent shipped the reclassification/organize UI (Option A) on `feature/ai-classification`, commit `9669dfa`, then fixed a High-severity bug Tester found in it, commit `69363fb`.**

Build (`9669dfa`): the `/organize` page per Abhishek's Option A pick — sidebar folder tree with live counts, inline folder creation, a paginated photo grid with three card states (normal/failed/duplicate) matching `design/wireframes/reclassify-ui.svg` exactly. While building, found that `failed`/`duplicate` photos are structurally invisible to any real folder (the dedup gate and failed-pipeline paths both short-circuit before folder assignment, so `folderId` stays null forever) — closed this with a new `GET /api/collections/:id/unfiled-photos` endpoint surfaced as a virtual "Unfiled" sidebar row, plus additive `duplicateOfPhotoId`/`dedupMethod`/`originalFilename` fields needed for the "duplicate of X.jpg" label. All backend changes additive, nothing renamed/removed. This build was interrupted mid-session by a platform usage limit before it could report back, and Anthropic's safety classifier was unavailable to review the final output — **Master independently re-verified everything before trusting it**: re-ran typecheck/lint on both workspaces, re-ran the full backend suite twice back-to-back (52/52 both times), and read the new page's source and both backend route diffs directly, before committing.

Bug fix (`69363fb`): Tester's exploratory testing (07:31 report) found a real High-severity gap this build introduced — see Open Bugs/Last Tester Run. Fixed via a new, genuinely user-scoped `GET /api/photos/unfiled` endpoint (kept additive alongside the old collection-scoped one, which nothing else calls) so the Unfiled bucket no longer depends on a collection existing first; required extracting `PHOTO_CARD_SELECT`/`toPhotoCard` into new `lib/photoCard.ts` and `thumbnailKey`/`originalKey` into new `lib/storageKeys.ts` to avoid a circular import (pure moves, no logic change — confirmed by Tester's follow-up diff review). Developer also caught and fixed a **second latent bug** during self-review: a user's first-ever successful reclassify left the sidebar's known collection id stuck at `null`, so a newly-created folder existed server-side but never appeared without a manual reload. Added regression test coverage for the original bug (there had been none — Playwright-only, uncommitted — exactly how it slipped through). 56/56 backend tests, confirmed independently by both Master and Tester across multiple re-runs, zero flake. MR draft addendum: `reports/mr-drafts/organize-reclassification-ui.md`.

**2026-07-03 (afternoon) — Developer Agent shipped the dashboard-stats endpoint + upload-flow polish on `feature/ai-classification`, commit `510cc30`.** `GET /api/dashboard` (per-user photo/folder/collection totals, per-collection breakdown, storage usage as BigInt-safe strings with a server-computed `usedPercent`) plus drag-and-drop and a real XHR-based progress bar on the existing `/upload` page. Self-reviewed (no separate review pass this cycle) and caught its own bug: the new test file's worker-availability probe was missing, silently skipping the worker-dependent aggregation test every run regardless of whether the worker was actually healthy — fixed by reusing the `beforeAll` probe pattern from `classification.smoke.test.ts`. Backend 52/52 (new `dashboard.smoke.test.ts`, 5 tests), frontend typecheck/lint clean, manually verified in a live browser (drag-over feedback, drop-to-upload, progress bar, click-to-browse and rejected-file regressions). MR draft: `reports/mr-drafts/dashboard-stats-and-upload-polish.md`. Full detail in Tester's independent 02:27 report above.

**2026-07-02/03 (earlier) — Developer Agent shipped the AI-classification organizing layer on `feature/ai-classification`** (branched off `feature/upload-pipeline` @ `74d1f4b`), through a build → adversarial-review → fix → re-verify loop before Tester ever touched it. Five commits: `8f1f065`, `5b8d304`, `97b69a1`, `0395096`, `27afa75`.

**What was built** (full detail in `reports/mr-drafts/feature-ai-classification.md`):
- Schema: `Collection`/`Folder` Prisma models via a real migration (`20260702090917_add_collections_folders_classification`, extending the migration history that started with commit `74d1f4b`), plus `photos.file_sha256` and `photos.dedup_method`.
- Pure `categoryMapping.ts` module (sibling of the classification provider, not part of it — keeps the future real-Vision swap-in a one-file change) implementing the roadmap's label→folder priority table.
- Worker: race-safe folder auto-creation (lazy default "My Photos" collection, find-or-create with P2002 catch-and-refetch), confidence bucketing (<0.60 strict → Uncategorized), a new `reclassify` job type, and a `FORCE_LOWCONF_` test hook (mirrors the existing `FORCE_FAIL_` pattern) since the mock's real confidence range can never otherwise exercise the Uncategorized path.
- Six new endpoints (`GET /api/collections`, `GET`/`POST /api/collections/:id/folders`, `GET /api/folders/:id/photos`, `PATCH /api/photos/:id`, `POST /api/photos/:id/reclassify`) plus additive fields on the existing photo endpoints (`exif`, `folderId`, `collectionId`, `dedupMethod`, `job`), all under the existing `requireAuth`/`asyncHandler`/Zod/404-not-403 conventions.
- Carry-over hardening: `authRateLimiter` split into independent `signupRateLimiter` (5/15min/IP) and `loginRateLimiter` (10/15min/IP), both relaxed under `NODE_ENV=test`; SHA-256 exact-hash dedup pass ahead of pHash; EXIF exposed on `GET /api/photos/:id`.
- 3 SVG wireframe proposals for the reclassification UI at `design/wireframes/proposals/reclassify-ui-option-{a,b,c}.svg` — **not built**, correctly left blocked per the UI-decision protocol.
- A genuinely pre-existing bug found and fixed along the way: `auth.smoke.test.ts`'s duplicate-signup test used an email address invalid under the current Zod regex, so both signups 400'd and the 409 path was never actually exercised — confirmed pre-existing at `74d1f4b` before fixing.

**Two real concurrency bugs were caught before they ever reached Tester**, via an adversarial code-review pass (3 lenses: correctness, security/ownership, testability) followed by independent skeptic verification of every finding, then a second gap-closing round after the first fix proved incomplete:
1. **Circular/mutual duplicate-marking.** The new SHA-256 exact-dedup pass matched *any* same-user same-hash row regardless of whether it had finished processing — two quick uploads of the same file could each mark the other as the duplicate (A→B and B→A simultaneously), permanently losing both copies from the organized library. First fix (constrain the SHA-256 pass to strictly-older, non-duplicate candidates) closed that path but a second adversarial pass found the *same* hole survived through the pHash near-dup phase, which had none of the new exclusions. Final fix applies the same strictly-older-and-non-duplicate constraint to both phases, extracts both candidate queries into a single `lib/dedup.ts` module with the invariant documented at the top, and a max-effort skeptic then tried, and failed, to construct any interleaving (serial, concurrent, retried, reclassify-mixed) that produces a cycle. **One narrower, non-cyclic edge case remains and is accepted, not fixed:** under a specific concurrent race (uploading a re-encoded copy and a byte-identical copy of that re-encode at nearly the same time), a duplicate can end up pointing at another duplicate rather than the canonical original — chains stay finite and acyclic (no data loss, no stuck state), and reclassify still rescues either photo. Not reproduced in Tester's live run. Flagged below as a pending decision rather than spending a fourth fix round on it.
2. **Reclassify double-enqueue race.** `POST /api/photos/:id/reclassify`'s terminal-state check and its enqueue were four non-atomic steps, so concurrent requests (e.g. a double-clicked button) could all pass the guard and create duplicate jobs, corrupting folder photo-counts. Fixed with a single atomic conditional `UPDATE ... WHERE status NOT IN (pending, processing)` as the sole arbiter — confirmed closed by independent re-verification, including compensation logic so a Redis hiccup during enqueue can't strand a photo in `pending` forever.

Also hardened along the way: folder `photoCount` reconciliation was vulnerable to stale reads under concurrent moves/reclassifies (now Serializable-isolated with automatic retry), and 7 additional test coverage gaps the review surfaced (multi-category label priority, the exact flat-image repro as a permanent regression test, concurrent new-category folder creation, EXIF assertions, reclassify rate-limiter independence, and a from-scratch offline-classification test proving no code path can reach the network). Final suite: **47/47, twice back-to-back**, plus Tester's independent 52/52.

**Process note:** the session hit its usage limit three separate times mid-cycle (once during planning, twice during the developer build/fix/test chain). Each time, work already completed was preserved — either because the underlying agent had simply paused and could be resumed with full context, or because a capacity probe confirmed the limit had cleared before relaunching. No work was lost or duplicated; flagging only because it added real wall-clock time to this cycle and may recur.

## Open Bugs
None currently open. **2026-07-04 03:58 — Guest Access + OTP backend security-focused regression, 68/68 live assertions + backend 71/71, 0 bugs, 0 security holes; verdict SAFE and CORRECT (report `reports/2026-07-04_0358.md`).** Prior Tester runs: 2026-07-03 23:40 (53/53 app-clean, post-Next-14.2.35-upgrade regression — zero regressions, backend 56/56 unaffected), 19:02 (69/69, Dashboard page — clean incl. the fresh-user empty state), 16:47 (53/54, one test-fixture mistake not an app bug, zero real bugs under adversarial testing), 08:55 (30/30, fix verification), 07:31 (27/27 scripted clean, found 1 High bug — fixed same cycle), 02:27 (20/20 + 52/52 backend suite), 00:50 (52/52). Prior runs also clean (28/28 on 2026-07-02, 17/17 on 2026-07-01). **The full Week 1–8 slice has now been through the full plan→build→test loop with zero open bugs.**

**Found and fixed this cycle:** a brand-new user whose very first photo failed classification or resolved as a duplicate — before any collection had ever been created — had that photo permanently unreachable in the new Organize UI (High severity, genuinely likely in practice, not a contrived edge case). Found via Tester's deliberate exploratory testing beyond the task's suggested steps, fixed via a new user-scoped `GET /api/photos/unfiled` endpoint, confirmed fixed live by Tester in a follow-up pass, with new regression test coverage added. A second latent bug (sidebar not refreshing after a user's first-ever successful reclassify) was found and fixed in the same pass. Both closed, not reopening here — see Last Developer Action for full detail.

Non-bug items flagged for attention (not blocking):
- **Duplicate-of-a-duplicate chain edge case** (AI-classification cycle). Accepted trade-off, not reproduced live, no data loss; revisit only if it shows up in practice or someone wants a fourth fix layer built.
- ~~**Stale duplicate `tsx watch src/server.ts` process**~~ — **RESOLVED 2026-07-03, deliberately, by Master** (correcting Tester's "likely cleaned up by the frontend restart" guess — it was not a side effect). Master traced the full process tree by parent-PID and port ownership: exactly one genuinely stale process existed, a Jul-1 `tsx watch src/server.ts` orphan (PID 15469) with no live child and nothing bound — killed surgically **before** the frontend restart, with the live backend/worker verified untouched (`:4000` health OK throughout). Tester's 23:40 run independently confirmed the clean end state: one `server.ts` + one `worker.ts` watcher, single `:4000` listener. The earlier "stale worker.ts too" note from the 07:31 report did not reproduce — only the one orphan existed by tonight. Dropped from the standing list unless it reappears.
- Signup/login shared rate-limit bucket, pHash flat-image false positive, and the EXIF testability gap — all **resolved** in the AI-classification cycle, confirmed clean by two subsequent Tester runs. No longer open items.
- ~~`npm install` still flags Next.js 14.2.13's known security advisory — still deferred to a dedicated dependency-upgrade pass~~ — **DONE this cycle (partial, by design).** Bumped to `14.2.35` (commit `ad441da`): the critical advisory + 9 more `next` CVEs cleared, build/typecheck/lint all clean. **14 `next` advisories + transitive `glob`/`postcss` remain** — they only clear via a Next 15→16 major migration, which is a breaking change now flagged as a separate decision for Abhishek (see Pending Decisions). Not a bug; risk materially reduced within the 14.2.x line.
- Both `specs/ai-classification.md` and `specs/dashboard-stats-and-upload-polish.md` are now committed to git — resolved the earlier "should specs be tracked" open question by just doing it; no objection raised, no longer flagging.

## Pending Decisions Awaiting User Input

**Carried forward, now resolved (for the record):** auth spec decisions 1–5 confirmed 2026-07-01; auth visual layout (Option B) resolved 2026-07-01, shipped; upload-pipeline's 7 flagged assumptions (file types, thumbnail layout, mock classification shape, pre-signed URL delivery, upload rate-limit numbers, storage quota scope, duplicate-detection scope) were all built on stated defaults with no veto received — treating as implicitly accepted since two full regression cycles have passed on them without objection.

**Resolved this cycle:**

0. **Reclassification UI wireframe — RESOLVED and SHIPPED 2026-07-03: Abhishek picked Option A** (sidebar folder tree + thumbnail grid, per-card "Move to…" dropdown, inline folder creation). SVG committed at `design/wireframes/reclassify-ui.svg` as the lasting design record. The `/organize` page is now built, tested, and clean (one High-severity bug found and fixed along the way — see Open Bugs/Last Developer Action). This also resolves the Option-B inheritance wrinkle flagged earlier — Option A was already Developer's recommendation specifically because it "scales naturally into the Week 7–8 folder browser," so the folder browser inheriting this pattern (minus move/reclassify actions, plus the same "Unfiled" concept this cycle discovered is necessary) stays the plan with no tension. **Week 5–6 is fully closed.**

0.5. **Dashboard page wireframe — RESOLVED and SHIPPED 2026-07-03: Abhishek picked Option B** (storage-meter hero + folder shortcut tiles + at-a-glance totals). SVG committed at `design/wireframes/dashboard.svg` as the lasting design record; the `/dashboard` page is built, tested clean (69/69, both populated and empty states), and closes out Week 7–8. Options A (stat cards + collection list) and C (sidebar nav shell — was flagged as scope creep, not recommended) were not taken. **This was the last open UI-wireframe decision in the entire Week 1–8 local-first MVP — there are no more pending UI picks.**

**New this cycle — 10 flagged assumptions from `specs/ai-classification.md`, none blocking (Developer proceeded on stated defaults, all shipped and tested), worth explicit confirmation or veto:**

1. **Default collection semantics.** One per-user default "My Photos" collection, created lazily by the worker at first folder assignment, `@@unique([ownerId, name])`. Side effect: user collections can't duplicate names later.
2. **Mapping-table aliases.** Added Landscape/Nature/Outdoor as Nature aliases beyond the roadmap's literal table (fixture-only change, provider untouched). Veto if the mapping table must stay roadmap-verbatim.
3. **"Secondary tags" scope.** Primary folder is real; secondary tags remain the existing raw `aiLabels` array, no separate tags table yet.
4. **Reclassify clears the duplicate verdict.** Reclassifying a `duplicate` photo clears `duplicateOfPhotoId`/`dedupMethod` — the deliberate escape hatch for pHash/sha256 false positives. Veto if duplicates should stay locked until a delete/merge flow exists.
5. **Reclassify rate limiter numbers.** Own per-user bucket, 30/15min, independent of upload's and auth's. Numbers open to veto.
6. **Auth rate-limit split numbers.** Signup 5/15min/IP, login 10/15min/IP, both relaxed under `NODE_ENV=test`. Confirmed working by two clean Tester runs — numbers open to veto if you'd prefer different values.
7. **pHash flat-image fix approach.** SHA-256 exact-hash first pass + skip pHash comparison when either hash is the degenerate all-zeros value. Accepted residual: two near-identical-but-not-byte-identical flat photos (e.g. two shots of the same blank wall) will no longer dedup against each other. Confirmed working by Tester's exact repro.
8. **Manual folder creation pulled into this pass.** `POST /api/collections/:id/folders` (create-only) shipped now since the reclassification UI needs move targets; rename/merge/delete stay deferred to Week 7–8.
9. **Low-confidence test hook.** `FORCE_LOWCONF_` filename hook (forces confidence to 0.42) rather than widening the mock's real confidence range, to avoid destabilizing every other classification test.
10. **Job/dedup observability fields.** `GET /api/photos/:id/status` now exposes internal job bookkeeping (`type`, `status`, `attempts`, `errorMessage`) and `dedupMethod` to the client — added mid-cycle so these paths could be Tester-verified at all, rather than staying permanent "code-review only" gaps. Veto if internal job state shouldn't be client-visible.

None of these block further work — all ten defaults are already built, shipped, and passed a full regression cycle. Flagging per protocol; treat silence past this point as implicit acceptance, same as the upload-pipeline batch above.

**New this cycle — Next.js major-migration decision (dependency-hardening pass, commit `ad441da`):**

16. **Next 15→16 major migration for a full `npm audit` clear.** The patch bump to `14.2.35` cleared the critical advisory and 10 `next` CVEs total, but **14 `next` advisories** (XSS via CSP nonces / beforeInteractive, RSC + Middleware cache-poisoning, HTTP request smuggling in rewrites, request-deserialization DoS, WebSocket SSRF, several image-optimization DoS items) plus the transitive **`glob`** (high; build-time-only dev dep, non-reachable in our usage) and **`postcss`** (moderate) advisories can only be resolved by moving off the 14.2.x line entirely — i.e. a **Next 15→16 major migration**, which is a breaking change (App Router API surface, `useSearchParams`/RSC semantics, config format, ESLint config all shift). This pass deliberately did NOT attempt it and did NOT run `npm audit fix --force` (which installs next@16). **Decision for Abhishek:** whether/when to schedule a dedicated Next major-migration cycle. Not blocking — the current 14.2.35 build is clean and the highest-risk (critical + auth-bypass) items are already closed.

**New this cycle — 4 flagged assumptions from `specs/dashboard-stats-and-upload-polish.md`, none blocking (already built and tested), worth explicit confirmation or veto:**

11. **`totals.photoCount` scope.** Counts every photo regardless of status (pending/processing/done/duplicate/failed), treated as a storage-consumption stat rather than an "organized library" stat. Veto if it should narrow to `done`-only or exclude `failed`.
12. **Dashboard-page wireframe sequencing — moot/resolved.** It was sequenced after the reclassification-UI pick as planned; the Dashboard pick (Option B) has now happened and shipped. Nothing outstanding here.
13. **Folder browser inheriting the reclassification UI's pattern — RESOLVED and SHIPPED this cycle.** Built as a genuinely separate route (`/browse`), not a mode flag inside `/organize` — Planner's stated reasoning: `/organize`'s state model is tightly coupled to editable-grid concerns (per-card mutation state, poll timers) that a read-only mode would need to thread a flag through everywhere, leaving untested dead paths live. `/browse` reuses the same visual pattern and the "Unfiled" concept, with zero new backend work needed. No veto received.
15. **Photo viewer — judged not to need its own wireframe round, built directly.** Planner's reasoning: fullscreen-image-plus-info-panel is one of the most standardized patterns in photo-product UI, with no real competing layout options the way `/organize` or the Dashboard page have — same test the upload-page polish passed (elaboration of an existing surface, not a new information-architecture decision). Shipped as a shared component used by both `/browse` and `/organize`. Veto if you'd have preferred options presented first.
14. **Upload progress bar implementation.** Plain `XMLHttpRequest`, zero new dependencies (chosen specifically because `fetch` cannot report upload progress). Veto if you'd rather standardize on a progress-capable fetch library now, in anticipation of future multi-file upload work.

**NEW 2026-07-04 — 12 decisions from `specs/guest-access-otp.md` (Week 9–10), namespaced G1–G12 to avoid collision with the batches above. The Developer is building the backend core on these recommended defaults NOW; per the established pattern (see the AI-classification batch), treat silence as implicit acceptance, but any of these is vetoable before the branch is pushed/merged. G3 and G7 are the load-bearing ones worth an explicit look:**

- **G1. OTP length/TTL/attempts — ROADMAP-SPECIFIED, no decision needed.** 6 digits, SHA-256 hash, 5-min TTL, single-use, 3 wrong attempts → auto-deny (roadmap § 12). Override only if you want different numbers.
- **G3 (load-bearing). Share unit = FOLDER(s).** Grounded in roadmap § 6 (`folder_permissions` on `folder_id`) + § 14 flow. No single-photo or whole-collection sharing this pass. **Veto if you want photo-level or collection-level sharing** — this shapes the schema, so a change is cheapest now.
- **G7 (load-bearing). Guest-token handoff after approval:** the guest's status-poll endpoint, on detecting `approved`, sets the httpOnly guest-session cookie directly on the polling browser (raw token never shown to the owner or copy-pasted). Clean and secure; Developer builds it so a veto is a contained change. **Veto if you'd prefer a different handoff.**
- **G2. Guest session TTL:** add `expires_at` (roadmap omits it), cap at min(24h, earliest permission expiry) — a session can't outlive the grant.
- **G4. Guest email vs. link:** owner supplies guest email as a label; since real email is mocked, the raw invite URL is returned to the owner to share manually. OTP goes to the owner, not the guest.
- **G5. `view`-only download refusal = 403** (not 404) — the guest legitimately knows the photo exists; 404 stays for folders/photos they can't see at all.
- **G6. Invite `max_uses` = 1** — one named guest, no link-forwarding.
- **G8. Geo enrichment omitted** — capture IP + user-agent only (IP→geo needs an external service); add in Week 11–12 if wanted.
- **G9. Audit log deferred to Week 11–12** (roadmap sequences it there); choke-point hooks left in place so it's a non-refactor add later.
- **G10. Re-click behavior:** live session → `already_approved` (no new OTP); unexpired pending request → reuse it; expired → fresh OTP. Don't spam the owner per click.
- **G11. Invite rate-limit numbers:** request 10/15min/IP, status-poll 120/15min/IP, both relaxed under test. Its own bucket.
- **G12. `download_all` stored but no bulk-zip endpoint this pass** — treated as ≥ `download`; per-photo download only.

**RESOLVED 2026-07-04 — the 3 Guest Access UI wireframe rounds are picked (8 SVG options presented via an Artifact; Abhishek chose per surface). Chosen SVGs promoted to `design/wireframes/{share-panel,guest-management,guest-portal}.svg` as the design records (proposals retained under `proposals/`). The frontend build for Week 9–10 is now BUILT (commit `a2cfb3f`) — see Last Developer Action.**
- **U1. Owner "Share" panel — PICKED: Option B, dedicated `/share` page** (two-column: scrollable folder picker + Select-all + running tally on the left; permission/expiry/email + generated invite link on the right). Record: `design/wireframes/share-panel.svg`. (Matched Master's recommendation.)
- **U2. Owner approval / guest-management — PICKED: Option C, single prioritized feed** (pending requests float to the top under an attention banner with inline OTP entry + IP/device; guest roster with one-tap Revoke flows below in the same stream). Record: `design/wireframes/guest-management.svg`. (Master had leaned A/two-column; Abhishek chose C.)
- **U3. Guest portal — PICKED: Option B, preview-first single page** (locked/blurred thumbnails shown up front; a status bar swaps request→waiting→unlocked in place, no page hop; download button per-photo, gated by permission level). Record: `design/wireframes/guest-portal.svg`. (Master had leaned A/sequential for the stricter gate; Abhishek chose B.) **⚠ ACCEPTED PRIVACY TRADE-OFF — build constraint:** B reveals a preview of the shared set *before* the owner approves, so the frontend MUST render only blurred/locked placeholder thumbnails pre-approval — never a real, un-blurred, or full-resolution image, and never a working pre-signed URL — until the guest session exists. The backend already enforces this (no guest session → no `/api/guest/*` access → no pre-signed URLs), so the "preview" pre-approval is purely decorative locked tiles; the build must not fetch or leak any real image before approval. Flagged so this doesn't silently regress into exposing private photos.

## Next Scheduled Actions
- 06:00 daily — Tester Agent regression pass (Docker/dev-server prerequisites unchanged, see Notes/Risks — still not automated via cron in this environment).
- 18:00 daily — Tester Agent regression pass.
- **Week 9–10 (Guest Access + OTP) frontend is now BUILT (commit `a2cfb3f`), pending a Tester regression pass.** Immediate next step: Tester runs a full pass against the three new pages (`/share`, `/guests`, `/g/[token]`) — the Developer's own live Playwright verification covered the happy path + the privacy constraint + auth-gate behavior, but a dedicated adversarial Tester pass (rapid double-click on Approve/Deny/Revoke, cookie-blocked edge case, cross-owner isolation through the new UI) hasn't happened yet. See `reports/mr-drafts/guest-access-ui.md`'s "Testing notes for the Tester Agent" section for specific angles flagged by Developer.
- Other open items, each needing explicit go-ahead: the **Next 15→16 major migration** (Pending Decision 16, breaking-change hardening cycle); and **whether/when to push** the Week 9–10 work (nothing new pushed since the 2026-07-03 backlog push — needs a fresh go-ahead, and remember: no `Co-Authored-By: Claude` trailer, feature branch only, never master).
- 06:00 / 18:00 daily Tester regression passes remain the standing cadence (still not cron-automated in this environment).

## Notes / Risks
- **GitHub remote is connected; `feature/upload-pipeline` and `feature/ai-classification` (through commit `3435f8c` only) are pushed**, per Abhishek's explicit go-ahead earlier today — `master` untouched throughout, no PRs opened. **PUSHED 2026-07-03 (late night) on Abhishek's explicit go-ahead** — the full 13-commit backlog on `feature/ai-classification` went up in one fast-forward push (`3435f8c..5ca5ed9`), feature branch only, no PR opened. **Two standing rules from that go-ahead:** (1) never push to master — trivially satisfied: the remote has NO master/main branch at all (only the three feature branches; GitHub's default is `feature/upload-pipeline`); (2) **no Claude co-author attribution in commits** — the three commits carrying a `Co-Authored-By: Claude` trailer were rewritten before pushing (local-only history, verified byte-identical trees, all authorship Abhishek's). That rewrite remapped four hashes: dashboard-page build `064cc26`→`fcff301`, Week 7–8 STATUS reconciliation `25d8dd6`→`870f024`, Next.js bump `9295354`→`ad441da`, housekeeping reconciliation `b05a1ec`→`5ca5ed9` — all markdown records in this repo have been updated to the new hashes; any *old* hash in an external note refers to the same content. A local safety ref `backup/pre-strip-ai-classification` preserves the pre-rewrite history (never push it). **Going forward: no `Co-Authored-By: Claude` trailers in any commit.** Also observed during the push (informational, not actioned): `origin/feature/upload-pipeline` moved remotely `74d1f4b`→`e53ca1e` — not from this session; presumably Abhishek's own activity.
- **NOT YET PUSHED — 2026-07-04 Week 9–10 work is local-only; Abhishek said "we will push later today" (2026-07-04) — hold until he gives the explicit word, do NOT push proactively.** Four commits now sit on `feature/ai-classification` past the pushed tip `035ed24` — the spec+STATUS scoping commit `8d21be7`, the Developer's backend build `dad0b74`, the STATUS/report reconciliation `7c04383`, plus any further coordination commits. One branch push covers them all when he says go (feature branch only, never master, no `Co-Authored-By: Claude` trailer — same rules as last time).
- **A separate `feature/local-dashboard` branch exists and is pushed**, containing 4 files (`CLAUDE.md`, `agents/MASTER.md`, `agents/PLANNER.md`, `.gitignore`) that wire a personal local-only "insights dashboard" tool into the coordination loop (an untracked `dashboard/` folder Abhishek runs separately, gitignored by design). **Not merged anywhere**, does not apply to `feature/ai-classification` or any other branch — don't act on `dashboard/FEATURE_QUEUE.md` mentions from that branch's `MASTER.md` unless it's actually checked out or merged.
- **Runtime state (updated 2026-07-04):** the **backend `:4000` was restarted by Master 2026-07-04** (detached via `nohup`) to pick up two `.env` changes for the guest-access build — it now runs `NODE_ENV=development` with **`NOTIFICATIONS_EXPOSE_OTP="true"`** so the mock OTP is readable via the flag-gated dev endpoint (`GET /api/invites/requests/:id/otp`) for local testing. **`.env` is gitignored** — on a fresh checkout set `GUEST_SESSION_TTL_HOURS` (default 24) and `NOTIFICATIONS_EXPOSE_OTP` (keep "false" outside local dev/test; it was flipped true only for the Tester pass and is a local-only convenience, never for a real deployment). **The frontend was restarted again 2026-07-04 (later) by Developer** (killed the stale `next dev`/`next-server` pair bound to `:3000`, relaunched detached via `nohup npm run dev -- -p 3000`) — needed to pick up the three new route folders (`/share`, `/guests`, `/g/[token]`), same as any Next dev-server route addition. Confirmed clean afterward: single `:3000` listener, all app routes (old + new) returning non-error status codes. The **worker is the same continuously-running process from 2026-07-02 — deliberately never restarted.** All three launched detached from the Claude session (won't survive a machine reboot; `curl localhost:4000/health` before assuming they're up). Process tree clean: one `server.ts` watcher, one `worker.ts` watcher, one `next dev`. The old stale-duplicate-watchers / double-BullMQ-processing concern stays closed — one worker instance only. **Also note: this cycle's live verification exhausted the signup/login rate-limit buckets again** (same benign harness artifact every prior Tester report has documented) — expect a Tester pass right after this to need the same seed-via-Prisma workaround for its own setup, or to simply wait out the 15-minute window.
- Docker Compose (Postgres/Redis/MinIO) has been stable and healthy across the entire day — no recurrence of the earlier credential-helper blocker.
- Real AWS/GCP services (S3, Vision API, EKS, Terraform) remain fully deferred — the AI-classification cycle went to considerable lengths to keep that boundary airtight (dependency-graph checks, stubbed-network tests) precisely because "swap in real Vision" is meant to stay a one-file change whenever it's approved.
- UI design decisions stay SVG-only, no Figma. **All UI-wireframe rounds for the Week 1–8 local-first MVP are now closed:** auth layout (Option B, Week 1–2), reclassification/organize UI (Option A), and the Dashboard page (Option B) all went through the propose→pick→build→record loop; the Folder browser and Photo viewer were judged not to need their own rounds (reasoning in Pending Decisions 13/15), no veto received. Committed design records: `design/wireframes/auth.svg`, `reclassify-ui.svg`, `dashboard.svg` (proposals for each retained under `design/wireframes/proposals/`).
- **On reduced-review builds:** three Developer sessions today were interrupted by platform usage limits before reporting back (this cycle's earlier organize-UI build, twice during the AI-classification cycle). Each time the underlying work was still completed and recoverable — by resuming the same agent, or (when the safety classifier was also unavailable) by Master independently re-running typecheck/lint/tests and reading the actual diffs before trusting and committing. This cycle's browse+viewer build completed normally with no interruption, for comparison. The pattern has now repeated enough times across the day to note as a real, recurring operating condition rather than a one-off, but it hasn't cost correctness so far — the real bugs that did slip through were caught by Tester's deliberate exploratory testing, not by the interruptions themselves.
- MR drafts: `reports/mr-drafts/feature-ai-classification.md`, `reports/mr-drafts/dashboard-stats-and-upload-polish.md`, `reports/mr-drafts/organize-reclassification-ui.md` (incl. bug-fix addendum), `reports/mr-drafts/browse-and-photo-viewer.md`, `reports/mr-drafts/dashboard-page.md`, `reports/mr-drafts/guest-access-otp.md` (backend), `reports/mr-drafts/guest-access-ui.md` (frontend — new this cycle).
