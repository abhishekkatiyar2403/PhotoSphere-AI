# PhotoSphere AI — Live Status

> Single source of truth for what's happening right now. Every agent reads this first and updates it last. Master Agent owns this file — Planner, Developer, and Tester propose updates, Master reconciles conflicts.

**Last updated:** 2026-07-18 — **Rekognition request-parameter accuracy pass on `backend/src/lib/classification/` (direct request from Abhishek): two changes, everything else confirmed already at its optimum.** (1) `DetectLabels` `MaxLabels` 30 → 100 — billed per image so extra labels are free evidence, and a busy scene can carry MORE than 30 labels above categoryMapping's 75% voting floor, silently starving the dominance scoring/taxonomy fallback; `MinConfidence` deliberately stays 50 (the 50-75% tail never votes, but photo cards surface it per-label, Abhishek's 2026-07-11 request). (2) `SearchFacesByImage` gains `QualityFilter: "AUTO"`, matching the bar `IndexFaces` already enforces at enrollment — the AWS default is NONE, so a face too blurry to ENROLL could still MATCH, and low-quality query faces are exactly where spurious ≥85-similarity wrong-person matches come from; the filter's rejection signal (`InvalidParameterException`) is handled as a quiet flat-"People" fallback, not an error. Confirmed-optimal-as-is: `FaceMatchThreshold` 85 (2026-07-12 audit), `IndexFaces` `MaxFaces:1`/`QualityFilter:AUTO`, the 1600px/q82 pre-resize (sits under Rekognition's 1920×1080 min-face-size scaling knee — raising it buys ~nothing). `tsc --noEmit` clean; golden-set + screenshot suites 52/52 (mock-forced under Vitest by design, so the suite can't exercise these params). **Honest gap: neither change is observable without `CLASSIFICATION_PROVIDER=rekognition` against live AWS — worth an eyeball pass on the next real-library run, same caveat as the FaceMatchThreshold tuning. NOT committed** (working tree also carries unrelated pre-existing edits: `app.ts`, a new csrf test).

Prior update (2026-07-14 by Master) — **RESOLVED: `specs/plan-tiered-upload.md` + `specs/production-upload-batch.md` both fully live-verified end-to-end in a real browser, including the S3 CORS gap flagged earlier this same session.** Abhishek applied a bucket CORS rule to `photosphere-ai-prod` (`AllowedMethods: [PUT, GET, HEAD]`, origins covering `localhost:3000`/the LAN mobile-dev origin/the Vercel frontend URL, `ExposeHeaders: [ETag]`) — Master's first drafted rule from earlier this session had omitted `PUT` (the one method the browser's direct-to-S3 part-upload actually needs; `GET`/`HEAD` alone only cover reads), caught and corrected before applying. Re-ran the exact same browser upload that failed before: file selected → part `PUT` against the real bucket succeeded (no CORS error) → `/complete` assembled it → worker processed it → **status: done**, real AI labels, live thumbnail rendered via presigned URL. Zero console errors. **Both upload specs are now fully built AND live-verified with no known gaps** — only a Tester Agent regression pass remains as a formality.
- **RESOLVED 2026-07-14 — the Vercel deployment (`https://photo-sphere-ai-frontend.vercel.app`) is real and confirmed by Abhishek: "basically working," the production/live version.** The standing "no public hosting exists for this app yet" note in Notes/Risks was stale and has been corrected there. Current local work (this session, `feature/ai-classification`) is upgrading/testing AHEAD of that deployment — production-upload-batch + plan-tiered-upload exist only locally/unpushed, not yet on Vercel. Which backend the Vercel frontend actually talks to is still unconfirmed — not assumed, flagged in Notes/Risks for whenever it matters.

Prior update (2026-07-14 by Master, live-testing finding, superseded above) — **Live-tested `specs/plan-tiered-upload.md` + `specs/production-upload-batch.md` end-to-end in a real browser (Chrome DevTools MCP, not the repo's own tooling — this session had it, subagents don't) against the actual running dev stack. Mixed result: plan-tier logic is fully correct; the NEW batch-upload architecture is blocked in this environment by a real S3 CORS gap neither spec caught.**
- **Fixed in passing: a stale `frontend/.next` build cache was silently breaking EVERY form submission app-wide** (`main-app.js` 404ing → zero React hydration → forms fell back to native GET, login/signup totally broken). Killed the stale process, `rm -rf .next`, clean restart fixed it — unrelated to any of this session's feature work, but worth knowing: if forms ever silently stop working again, check for a 404 on `main-app.js` before assuming a code regression.
- **Plan-tier enforcement — confirmed correct via real HTTP against the running backend (not just the test suite):** `POST /api/upload/initiate` with 60 files on `free` → `400 {error:"batch_limit_exceeded", plan:"free", limit:50, requested:60}`; switched to `studio` via `PATCH /api/auth/plan` (confirmed `storageLimitBytes` synced to `536870912000` = 500GB in the same response) → the identical 60-file batch → `201`, all 60 issued part URLs, zero duplicates. `/settings` page itself verified live in-browser: segmented control renders exactly per the picked A+B design, click-to-switch is instant, active tier highlights correctly, success message shows, backend `PATCH /api/auth/plan` confirmed 200 in the network panel.
- **BullMQ priority — confirmed correct by inspecting the actual Redis-backed job, not just trusting the `.add()` call:** uploaded a real photo via the (CORS-unaffected, server-side-`putObject`) single-file route as `studio` → job's `opts.priority === 1`; same route as `free` → `opts.priority === 10`. Both photos fully processed to `status:"done"` by the real worker (thumbnails/classification/folder assignment all completed) — the plan-tiered pipeline is genuinely live and correct end-to-end for the single-file path.
- **NEW GAP — blocks the batch flow's browser-driven part-upload step in THIS environment, not caught by either spec or either Developer build:** `backend/.env`'s `AWS_S3_BUCKET` points at a REAL AWS S3 bucket (`photosphere-ai-prod`, `eu-north-1` — from the already-committed `0f9a732` "real AWS integrations" work, not something this session touched), not local MinIO. The single-file route's server-side `putObject` works fine against it (server→S3, no browser involved). But the NEW batch flow's whole point is the browser PUTing parts DIRECTLY to S3 — and that bucket has **no CORS policy** allowing `PUT`/`OPTIONS` from `http://localhost:3000`. Every part upload fails: `Access to XMLHttpRequest ... blocked by CORS policy: ... No 'Access-Control-Allow-Origin' header`. Uppy retries 5×, then the file fails. This is invisible to the backend test suite (VITEST env forces MinIO for all tests, per `lib/storage.ts`'s own documented seam) and was never exercised through a real browser until this pass. **Not something Master fixed or attempted to fix — modifying a real AWS bucket's CORS policy is exactly the kind of production-infrastructure change CLAUDE.md's "no real cloud credentials... without Abhishek's explicit go-ahead" rule exists for; a read-only `aws s3api get-bucket-cors` check was attempted and correctly auto-blocked by the permission classifier.** Needs Abhishek's decision: (a) add a CORS rule to the real bucket (`AllowedMethods: [PUT, GET, HEAD]`, `AllowedOrigins` covering the dev/prod frontend origins, `AllowedHeaders: ["*"]`) — a few-minute AWS console/CLI change Abhishek would need to authorize/perform himself; or (b) point local dev's `AWS_S3_BUCKET` back at MinIO for now (local MinIO's default config has no CORS restriction) and only worry about real-bucket CORS at actual deploy time. **The batch upload's server-side logic (`/initiate`'s cap/dedup/quota checks, `/complete`'s assembly+Photo-row-creation, the cleanup job) is unaffected and was verified correct independent of this** — only the middle "browser PUTs bytes to S3" step is blocked.

Prior update (2026-07-14 by Developer) — **`specs/plan-tiered-upload.md` is now BUILT END-TO-END, both layers. The `/settings` page + its topbar nav link are DONE, commit `f18d409` on `feature/ai-classification` (local, no push).** Unblocked by Abhishek's 2026-07-14 wireframe pick (see Pending Decisions below — "a merge of A + B," Master's synthesis read as A's instant-apply segmented control + B's inline per-tier stats). Built: `frontend/src/app/settings/page.tsx` (Free/Pro/Studio segmented control, click = immediate `authApi.updatePlan()` call, each segment shows its batch-limit/storage/priority numbers inline, "Plan (testing only — no billing)" copy, no pricing/CTA styling, transient success line + inline error surfacing on failure); "Settings" added to the shared topbar nav on all 8 authed pages (`activity`/`browse`/`dashboard`/`guests`/`organize`/`search`/`share`/`upload`), same one-line-per-page pattern `861ff25` used for "Trash"; `design/wireframes/settings.svg` saved as the picked design record. `tsc --noEmit`/`next lint`/`next build` all clean (`/settings` builds as a static route). **Honest gap, same standing category as every prior UI pass: no Playwright/browser-automation tooling exists in this repo, so no click-through was performed** — verified via typecheck/lint/build plus close reading against the spec/wireframe, resting on the already-backend-tested `PATCH /api/auth/plan` (12/12 in `plan-tiered-upload.smoke.test.ts`). Backend untouched this pass. Full detail + the A+B synthesis reconciliation note in `reports/mr-drafts/plan-tiered-upload.md`'s new addendum. **With this, `specs/plan-tiered-upload.md` needs only a Tester pass — nothing else is unbuilt in it.**

Git hygiene note: this branch's working tree carries substantial pre-existing uncommitted work unrelated to this task (backend audit fixes, a `/v2` design-system pass across most pages) — several of the 8 nav files and `globals.css` had *other* unrelated uncommitted hunks (a `UiV2Banner` import, large `.ps2` v2 styles) sitting in the working tree before this task touched them. Used hand-split hunks (`git apply --cached` on extracted per-file patches) to stage only the Settings-related additions, leaving every pre-existing hunk unstaged/untouched — commit `f18d409`'s diff is scoped to exactly the Settings work, no sweep-in this time.

Prior update (2026-07-13 (even later) by Developer) — **`specs/plan-tiered-upload.md` is BUILT end-to-end on the BACKEND, tests green; the FRONTEND `/settings` page is BLOCKED on Abhishek's wireframe pick (per CLAUDE.md's coordination protocol) — everything else that doesn't depend on it is done.** Branch: `feature/ai-classification`, local commits only (no push, no Claude trailer). Full breakdown:
- **Schema (PTU1 RESOLVED by Abhishek): `User.plan` promoted from a plain `String` to a real Prisma enum (`enum Plan { free pro studio }`, default `free`).** Confirmed via `psql` that all 67 existing rows were already exactly `"free"` before migrating — a clean, lossless promotion. `prisma migrate dev` refused to run non-interactively (it detects the String→Enum diff as a potential-data-loss drop+recreate), so the migration was hand-written (`20260713180000_promote_user_plan_to_enum/migration.sql`, same precedent as the trash-system's hand-edited partial-unique-index migration): `CREATE TYPE`, then `ALTER COLUMN ... TYPE "Plan" USING (...)`, applied via `prisma migrate deploy` — genuinely lossless (a cast, not a drop), verified live post-migration (still 67/67 rows, still all `free`).
- **`backend/src/lib/plans.ts`:** added `PlanTier` (aliased directly to the generated `$Enums.Plan`, not the plain-string type the spec's own literal code block showed — that block predated PTU1's resolution, reconciled deliberately per the task's own instruction), `BATCH_LIMITS`/`STORAGE_LIMITS_BYTES`/`PRIORITY_BY_PLAN` maps exactly per spec (free `{50, 5GB, 10}` / pro `{500, 100GB, 5}` / studio `{1500, 500GB, 1}`), `getBatchLimit`/`getStorageLimitBytes`/`getJobPriority`. `checkGuestLimit`'s `plan !== "free"` comparison is unchanged in behavior (enum members compare equal to their string literal), just now typo-proof.
- **`routes/upload.ts`:** `lib/validation.ts`'s `MAX_BATCH_FILES` renamed `ABSOLUTE_MAX_BATCH_FILES` (1000 → 1500, Studio's own cap, kept as a cross-tier Zod-schema ceiling). `/initiate` now does the REAL plan-aware batch-cap check right after `dbUser` is fetched, returning the exact `{ error: "batch_limit_exceeded", plan, limit, requested }` shape, ordered BEFORE the existing storage-quota check. `/complete` now does one `prisma.user.findUnique({select:{plan:true}})` for the session owner (it never fetched this before), computes priority once, threads it through `completeOneFile` into the existing `photoProcessingQueue.add(...)` call.
- **`routes/photos.ts`:** the single-file route's existing `photoProcessingQueue.add(...)` call gains `priority: getJobPriority(dbUser.plan)` — `dbUser` was already fully fetched for the quota check. The unrelated `reclassify` route's own `.add()` call is untouched (out of scope per the spec).
- **`PATCH /api/auth/plan`** (new, in `routes/auth.ts`): `updatePlanSchema` (Zod enum) in `lib/validation.ts`; one `prisma.user.update` writes `plan` + `storageLimitBytes` (via `getStorageLimitBytes`) together (PTU2's sync-at-switch decision); writes a `plan_changed` audit row (`fromPlan`/`toPlan` — new `AuditAction` member added to `lib/audit.ts`); no new rate limiter (PTU6, unchallenged default). `GET /api/auth/me` extended with its own small `prisma.user.findUnique({select:{plan:true}})` (⁠`requireAuth`'s `req.user` shape deliberately left untouched, per the spec).
- **PTU3 (RESOLVED by Abhishek) confirmed working exactly as decided:** a downgrade while over the new lower limit succeeds with no guard, and the very next upload attempt gets the SAME 413 quota check that already exists for anyone over-quota for any other reason — verified live in the new test suite, not just asserted.
- **PTU4/PTU5 built on the spec's own recommended defaults** (priority ints `studio:1/pro:5/free:10`; `plan_changed` audited) — not re-litigated, both still open for veto per the existing Pending Decisions entries below (left as-is, not re-worded, since nothing changed).
- **Frontend (non-UI parts only):** `lib/api.ts` gained `PlanTier`/`MeResponse`/`UpdatePlanResponse` types, `authApi.me()`'s return type now carries `plan`, `authApi.updatePlan(plan)`, and a `BatchLimitExceededBody`/`isBatchLimitExceeded` type guard (same pattern as the existing `isRestoreConflict`). `/upload`'s `/initiate` error handling now special-cases `batch_limit_exceeded` with a named plan/limit message ("Your free plan allows batches of up to 50 photos — 120 were selected. Split into smaller batches, or switch plans in Settings.") instead of falling through to the generic failure copy — this was explicit copy latitude in the spec, not a new UI surface, so it did not need a wireframe round.
- **BLOCKED: the `/settings` page itself + the "Settings" topbar nav link on all 8 authed pages.** Per CLAUDE.md's coordination protocol, this is genuinely new UI (no account/settings surface exists anywhere in this app) — 3 lightweight SVG options proposed below under Pending Decisions (`design/wireframes/proposals/settings-option-{a,b,c}.svg`), Developer's recommendation is Option A. **Stopped here rather than guessing** — everything else in the spec that doesn't depend on the page existing is done and tested.
- **Tests:** new `backend/src/__tests__/plan-tiered-upload.smoke.test.ts` (12 tests) covering every automatable Acceptance Criteria item — the exact tier constants + the priority-inversion check + a safe-default-on-garbage-input check (unit-level), the switcher's 401/400/audit-row/three-tier-switch-confirmed-via-me-and-dashboard, the downgrade-while-over-quota-then-413 flow, batch-cap enforcement at all three tiers (free 51→400, the same 51 on pro→201, studio 1501→400 at the Zod layer specifically, distinguished from the plan-aware 400 by asserting the error shape), and BullMQ priority correctness for BOTH the single-file and batch `/complete` code paths (via `photoProcessingQueue.getJob(id).opts.priority`, not just trusting the call was made) — confirming both paths produce the identical priority for the identical plan. **Fixed one pre-existing test fixture per the spec's own flagged expectation:** `upload-batch.smoke.test.ts`'s "over MAX_BATCH_FILES" test hardcoded the old flat 1000-file ceiling on a plain (now `free`-plan-default) test user — retargeted to assert the NEW `batch_limit_exceeded` shape at free's real 50-file cap instead (that file's own dedicated absolute-ceiling coverage moved to the new test file). `upload.smoke.test.ts`/`dashboard.smoke.test.ts`'s flat-5GB fixture resets needed NO change — `getStorageLimitBytes("free")` is still exactly 5GB, so the flat constant they restore to remains correct for a fresh/reset free-plan user. `checkGuestLimit`'s regression coverage (`plans.smoke.test.ts`) passes unmodified, confirming the enum promotion didn't change its behavior.
- **Full backend suite: 289/289 passing** (277 pre-existing + 12 new), `tsc --noEmit` and `eslint` both clean. Frontend `tsc --noEmit` and `next lint` both clean (build not run standalone this pass since the one new-UI surface — `/settings` — doesn't exist yet; will run `next build` again once that page is added).

Prior update (2026-07-13 (later still) by Planner) — **New spec written: `specs/plan-tiered-upload.md`, READY FOR DEVELOPER.** An addendum to `specs/production-upload-batch.md` (built, `e86f126`), reinstating the two items that spec's Non-goals deliberately trimmed ("BullMQ job priority by plan tier" and "per-plan batch/storage limit tiers") now that Abhishek has explicit numbers: Free 50 photos/batch + 5GB + low priority, Pro 500/100GB/normal, Studio 1500/500GB/high. Scoped against the REAL shipped code (`backend/src/routes/upload.ts`, `lib/queue.ts`, `lib/plans.ts`, `routes/photos.ts`, `routes/auth.ts`), not a guess. Key decisions:
- `lib/plans.ts` gains `BATCH_LIMITS`/`STORAGE_LIMITS_BYTES`/`PRIORITY_BY_PLAN` maps + `getBatchLimit`/`getStorageLimitBytes`/`getJobPriority` lookups, same file/pattern as the existing `FREE_PLAN_MAX_ACTIVE_GUESTS`/`checkGuestLimit`. **BullMQ priority direction stated explicitly (the easy inversion mistake): LOWER number = processed FIRST — Studio gets `1`, Pro `5`, Free `10`.**
- **The batch-cap check must move OUT of the static Zod schema** (`initiateUploadSchema`'s `.max(MAX_BATCH_FILES)` runs before the route knows the caller's plan) — kept as an absolute cross-tier ceiling (`1500`, Studio's own cap) at the schema layer for cheap pre-DB-lookup defense-in-depth, with the REAL per-plan check added in the route body after `dbUser` is fetched, returning a distinct `{ error: "batch_limit_exceeded", plan, limit, requested }` shape (not the generic Zod-failure shape) so the frontend can special-case an upgrade nudge.
- **Storage-limit decision: sync-at-switch, not compute-live.** `PATCH /api/auth/plan` writes `storageLimitBytes` to match the new plan's tier in the SAME update as `plan` — zero changes needed to either existing quota-check call site (`routes/upload.ts` `/initiate`, `routes/photos.ts`'s single-file route), since both already trust the stored column. **Confirmed via grep: no existing-user backfill is needed** — the ONLY writers of `storageLimitBytes` anywhere in `backend/src/` today are test fixtures; every real row still carries the schema default (5GB), which is already exactly Free's tier number.
- **Downgrade-while-over-the-new-limit is explicitly allowed, not blocked** — the existing 413 quota check naturally covers it on the next upload attempt, no new guard.
- New `PATCH /api/auth/plan` (free/pro/studio, no payment step, audited as `plan_changed`) + both `.add()` call sites (`upload.ts`'s `/complete` — which today never even fetches the session owner's plan, needs one added lookup — and `photos.ts`'s single-file route, which already has `dbUser` in hand) gain an explicit `priority` option. **Confirmed no new BullMQ queue is needed** — priority is a per-job option on the existing single `photoProcessingQueue`.
- A new owner-facing `/settings` page (no account/settings surface exists in this app today — confirmed via file search) will need Developer's usual propose→pick→build wireframe round per CLAUDE.md's coordination protocol before it's built — Planner deliberately did NOT pre-pick a layout, only the endpoint contract and copy discipline ("Plan (testing only)", no pricing/upgrade-CTA styling).
- **6 open questions posted below as PTU1–PTU6** (enum-vs-string promotion for `User.plan`, the sync-vs-compute storage decision flagged for veto, downgrade-block-or-allow, exact priority integers, whether to audit the switch, whether to rate-limit the switch endpoint) — none block Developer starting the `lib/plans.ts`/schema-adjacent work, all are called out explicitly rather than silently decided.

Prior update (2026-07-13 (later) by Developer) — **`specs/production-upload-batch.md` is BUILT end-to-end, commit `e86f126` on `feature/ai-classification` (local, no push, no Claude trailer).** All three resolved load-bearing decisions (PUB4 daily BullMQ cleanup job, PUB7 `UploadSessionFile` companion table, PUB10 Uppy frontend) built exactly as decided; every other open question (PUB1/2/3/5/6/8/9) built against Planner's own recommended defaults, no further guessing. Full breakdown:
- **Backend:** new `UploadSession`/`UploadSessionFile` Prisma models (2 migrations, real `prisma migrate dev`) + `User.uploadSessions` back-relation; `lib/storage.ts` gained `createMultipartUpload`/`getPresignedUploadPartUrl`/`completeMultipartUpload`/`abortMultipartUpload` (additive, same `usingRealS3` branch pattern, nothing existing touched); new `backend/src/routes/upload.ts` mounted at `/api/upload` (`POST /initiate`, `POST /complete`, `DELETE /abort`) with its own shared 10/15min/user rate limiter; a new daily BullMQ repeatable job (`registerUploadSessionCleanupJob`, mirrors `registerTrashPurgeJob` exactly) reconciles stale `in_progress` sessions past `expiresAt`. **The existing single-file `POST /api/photos/upload` route is completely untouched** — same file, same behavior, its own test file passes unmodified.
- **Frontend:** `/upload` reworked onto the batch flow — one `/initiate` call per selection (any file count, even 1, per Open Question #8's "no dual-wiring" default), client-side SHA-256 (Web Crypto), Uppy driving part-uploads directly to MinIO, a chunked completion batcher (every 50 files/30s per spec) flushing to `/complete`, unchanged `photosApi.status` polling once real `photoId`s come back. Batch-level summary line added (X/N uploaded, duplicates skipped, failures) alongside the existing per-item progress-bar UI.
- **One real deviation, flagged not buried:** the spec names `@uppy/aws-s3-multipart`; that package is marked "no longer supported" by its own maintainers on npm. Swapped for the actively-maintained `@uppy/aws-s3` (Uppy folded multipart support into it, identical hook contract) — a package substitution, not a design change. Full reasoning + 3 smaller schema/config choices (an added `UploadSessionFile.originalFilename` column the spec's literal block omitted, the Uppy-per-file vs. backend-per-batch reconciliation approach, the 24h session TTL) in `reports/mr-drafts/production-upload-batch.md`.
- **Testing:** 9 new tests in `backend/src/__tests__/upload-batch.smoke.test.ts` against REAL local MinIO/Postgres/Redis (no mocking) covering every item on the spec's Acceptance Criteria checklist that's automatable — 401, 413-with-nothing-created, duplicate-pre-check-excluding-trashed, over-`MAX_BATCH_FILES`, a real multi-file batch with genuine unauthenticated `PUT`s straight to MinIO, partial-success on a tampered ETag (+ storage-quota accounting only counting the real success), content-sniff failure with object cleanup, abort genuinely aborting in MinIO (verified via `ListMultipartUploadsCommand`, not just a DB flag), and the stale-session cleanup job's state transition. **Full backend suite: 277/277** (268 pre-existing + 9 new), rerun twice to rule out one transient flake as a regression (confirmed non-reproducible in isolation, same pre-existing flake class STATUS.md already documented 2026-07-08). Backend/frontend `typecheck`/`lint`/`build` all clean.
- **Not done / honest gaps:** no browser/Playwright click-through of the reworked `/upload` page (this repo still has no browser-automation tooling installed anywhere, same standing gap every prior UI pass has flagged) — the batch flow was verified via the automated backend suite (real HTTP + real MinIO) plus `typecheck`/`lint`/`next build`, not a live click-through. The spec's qualitative "Node process memory stays flat regardless of batch size" acceptance criterion was verified by architecture/code-review (no function in the new code path ever buffers a full file's bytes — `readLeadingBytes` explicitly caps at 4KB and destroys the stream early) rather than an automated RSS assertion (spec itself calls this a "spot-check," not a hard threshold).
- **Note on the working tree:** this branch already carried ~70 files of uncommitted work from an earlier session (real AWS integrations / audit fixes, apparently never committed despite `0f9a732` existing) when this task started. 7 files this feature needed to touch (`app.ts`, `worker.ts`, `queue.ts`, `storage.ts`, `validation.ts`, `schema.prisma`, `upload/page.tsx`) already had pre-existing uncommitted changes on them with no clean git-level way to separate hunks — this commit's diff on those 7 files necessarily includes that pre-existing content too. Every other unrelated file (~60+) was left untouched/unstaged. Full detail in the MR draft.

MR draft: `reports/mr-drafts/production-upload-batch.md`. Next: a Tester pass on this feature (the spec's own Success Signal — a 500+ file batch, network-inspection confirming direct-to-MinIO PUTs, a corrupted-file partial-success run, an abort-then-verify-MinIO-state run); everything else queued below remains open in parallel.

Prior update (2026-07-13 by Planner) — **New spec written: `specs/production-upload-batch.md` (presigned multipart batch upload, 500-1000 photos/batch), superseding/extending `specs/upload-pipeline.md`'s single-file flow.** Master reviewed the internal Feature 19 proposal with Abhishek and trimmed it hard for this codebase's actual stage before handing it to Planner — explicitly OUT this pass: BullMQ job priority by plan tier and per-plan batch/storage limit tiers (no billing exists yet to make either meaningful), Google Drive import, browser IndexedDB tab-close resume, and any change to the worker pipeline itself (thumbnails/EXIF/pHash/classification stay exactly as `upload-pipeline.md` built them). The existing single-file `POST /api/photos/upload` endpoint is KEPT, unmodified — this is an additive batch path alongside it, not a replacement.

Prior update (2026-07-09 by Developer) — **`POST /api/photos/:id/restore` REVISED per `specs/trash-system.md` FINAL DECISION 5 (REVISED 2026-07-09): restoring a single trashed photo no longer cascades into auto-restoring its ALSO-trashed folder (and every other photo in it).** Commit `6443d87` on `feature/ai-classification` (backend + tests only, local, no push, no Claude trailer). Abhishek found the old cascade confusing via live testing — restoring one photo shouldn't resurrect a whole folder. **New behavior:** the trashed folder is now NEVER touched by a photo restore — no restoring it, no touching `deletedAt`/`photoCount`/its other photos. The photo instead lands in a LIVE folder: an existing live same-named one (with a 409-then-`onConflict:"existing"` choice if one exists), a brand-new one (`onConflict:"new"`, auto-named `"<name> (recovered)"` with a capped numeric-suffix collision retry, or a caller-supplied `newName`), or a freshly auto-created folder reusing the original name if no live same-named folder exists at all. New shared helpers `findLiveFolderByName`/`generateNonCollidingRecoveredName` in `routes/folders.ts`; new `photoRestoreSchema` (`onConflict: "existing" | "new"`, deliberately a different vocabulary from folder-restore's `"merge"/"rename"` since this resolves "where does the ONE photo land," not "how does the folder itself restore"). Rewrote the two obsolete cascade tests and added 5 new ones covering auto-create-on-no-collision, 409-on-collision, both `onConflict` branches, and confirming the folder's own independent restore path still works afterward (including its own unrelated, expected T7 name-collision against the newly-auto-created recovered folder). **Backend suite: 173 → 176, all green.** `typecheck`/`lint` clean. Addendum in `reports/mr-drafts/trash-system.md`. **Known, explicitly-out-of-scope follow-up flagged: `frontend/src/app/trash/page.tsx`'s photo-restore-collision UI still calls the OLD `foldersApi.restore(...)`-based resolution path (merge/rename the trashed folder) and needs a matching update to call `photosApi.restore(photo.id, { onConflict, newName })` directly with "existing/new" copy before this ships to users** — the UI is currently wired against a contract that no longer exists for this flow.

Prior update (2026-07-08 (latest) by Developer) — **Trash page + `/organize` multi-select delete UI are BUILT, commit `861ff25`on `feature/ai-classification` (frontend-only, no backend file touched, no push, no Claude trailer).** Built against the picked wireframes (`design/wireframes/trash-page.svg`, `multi-select-delete.svg`) and the live 173/173 trash-system backend. New `frontend/src/app/trash/page.tsx`: Photos/Folders tabs, days-remaining badge (urgent red under 2 days), Recover, a genuinely-irreversible Delete-forever with a real in-app confirm, an Empty-trash button with its own stronger red-bordered confirm, and the restore-collision 409 handled inline exactly per the wireframe (Merge into existing / Rename & restore as, reused for both a folder row and a photo-restore auto-cascade hitting the same collision). "Trash" added to the top-bar nav on all 8 authed pages. `/organize` gained the full desktop-style multi-select Abhishek asked for: hover-reveal checkbox, plain click toggles one card (replacing the rest), Shift+click selects a contiguous range, Ctrl/Cmd+click toggles without clearing the rest, and a drag from empty grid space draws a marquee that REPLACES the selection on a plain drag but ADDS to it when Shift/Ctrl is held at drag-start (Finder/Explorer convention - the modifier-aware extension was implemented, not just the plain-replace minimum). "Select all on page" plus a "Select all N in this folder" link that fetches every page's ids when paginated (scoped honestly - never claims more than it fetched). A persistent bottom action bar drives `POST /api/photos/bulk-delete` with reversible "moved to Trash, 7 days" copy; the T2 partial-result case is shown as "N deleted, M could not be deleted (already gone or currently shared with a guest)" since the backend's uniform `not_found` reason genuinely can't be split further - not fabricated. Also added a previously-missing single-photo Delete action on `/organize` cards (same reversible copy, a specific message on the T2 409) and **fixed the stale P4 delete-folder confirm** that read the now-removed `photosOrphaned` field / "moves to Unfiled" copy - it now correctly says the folder and its photos move to Trash, recoverable for 7 days. `api.ts`: `ApiError` now carries the full parsed error body (needed for the restore-collision 409's `conflictingFolderId`/`conflictingFolderName`) - additive, no existing call site broken. **Verification:** `typecheck`/`lint`/`next build` all clean (isolated distDir since `next dev` was live); a throwaway live-HTTP pass (real signup + Prisma-seeded data, 0 residual rows) exercised single delete→trash-list→restore, folder delete→restore-collision→rename-resolve, bulk-delete's exact partial-success shape, permanent purge, and empty-trash - all matching the shapes the UI codes against. **No browser/CDP click-through this pass** (harness has repeatedly stalled on recent cycles per STATUS's own history; re-read the render logic post-edit as a substitute) - flagged as the one honest gap, same category as several prior UI passes. MR draft `reports/mr-drafts/trash-ui.md`. **Next: a Tester pass on this UI**, plus the outstanding P4/P5/P6 and P7 browser-DOM passes still queued from before.

Prior update (2026-07-08 (later still), by Master) — **Trash UI wireframes PICKED. Both Option A: Surface A (multi-select) → checkbox-on-hover + bottom bar; Surface B (Trash page) → dedicated `/trash` page.** Records promoted to `design/wireframes/{multi-select-delete,trash-page}.svg` (proposals kept under `proposals/`). **Abhishek added a requirement to Surface A: full desktop-style multi-select, not just one-by-one checkbox clicking** — click a card to select it, **Shift+click for a contiguous range**, **Ctrl/Cmd+click to toggle one item without losing the rest**, **click-and-drag a marquee/rubber-band box** over several cards, plus the existing "Select all" button (select-all scoped to the current page; if the folder is paginated, offer a secondary "select all N across all pages" action rather than silently guessing which — Developer's call on exact copy). **Master's scope call, flagged to Abhishek in the same turn, not yet confirmed: multi-select-delete ships on `/organize` ONLY, not `/browse`** — `/browse` was deliberately built read-only (no move/reclassify/create) per Day3.md's design; adding delete there would break that consistency. Revisit if Abhishek wants `/browse` included too. **Now building: the `/trash` page + the `/organize` multi-select/delete UI.**

Prior update (2026-07-08, by Developer) — **the entire trash system (specs/trash-system.md) is BUILT: schema + all endpoints + the daily auto-purge job + the full 21-path listing-query audit + 35 new tests. Backend suite 137 → 173, all green. NOT pushed (local commits only on `feature/ai-classification`).** See "Last Developer Action" for the full breakdown; MR draft `reports/mr-drafts/trash-system.md`. **One real deviation flagged, not buried:** `DELETE /api/folders/:id` now returns `{ deletedAt, purgeAt }` instead of the old `photosOrphaned` field (nothing is orphaned anymore under soft-delete) — the shipped P4 `/organize` delete-confirm modal still reads `photosOrphaned` for its "N photos move to Unfiled" copy, which is now factually stale until the Trash-page + delete-confirm UI round (queued below) ships. No other frontend was touched this pass (backend + tests only, per the task's explicit scope). **T5's veto option (spill folder-purge's photos to Unfiled instead of hard-deleting them) is logged here as a future feature request, not built, per the spec's own instruction.**

Prior update (2026-07-08 by Master Agent): **Abhishek made an explicit, FINAL product decision superseding `specs/photo-deletion.md`'s PD1: deletion must be a reversible 7-day TRASH, not immediate/permanent, for BOTH photos and folders — REVISING the already-shipped, already-PUSHED P4 folder-delete (`057605c`/`9664b6b`).** Planner wrote `specs/trash-system.md` (supersedes `photo-deletion.md` on PD1; that file kept, marked SUPERSEDED, for its still-valid PD3/PD4/PD5/PD7 analysis). Master + Abhishek then resolved every open sub-decision — **the spec is now 100% FINAL, ready for Developer, zero open questions:** `deletedAt` column on both `Photo`/`Folder`; a daily BullMQ repeatable-job auto-purges anything >7 days; **T5 = folder-purge cascades to hard-delete its photos too (Option A)** — the alternative (spill to Unfiled instead) is explicitly LOGGED AS A FUTURE FEATURE REQUEST, not built now; **T2 REVERSED from the Planner's original recommendation — a single photo now CANNOT be deleted if its folder is live-shared with a guest, same 409 posture as F1**; **T7 fixed via a Postgres partial unique index** (`WHERE deleted_at IS NULL`, raw-SQL migration, drops the plain `@@unique`) so a trashed folder never blocks a same-named new live folder; folder-restore collisions get a real choice (`onConflict: "merge"` or `"rename"`); restoring a photo whose folder is also trashed auto-restores the folder first. A 21-path listing-query audit is enumerated in the spec — the highest-leak-risk part of this build. **Two smaller, unrelated frontend bugs found+fixed earlier today: multi-file upload (`8d5b74a`) and the Unfiled manual-move dead-end (`4e33e25`) — both DONE.**

Prior update (2026-07-08, by Developer via Master reconciliation): fixed a real gap — `/upload` now supports multiple files at once (`8d5b74a`, frontend-only). See Last Developer Action.

Prior update (2026-07-07, by Master Agent): the deferred **P4/P5/P6 backlog is now BUILT END-TO-END — backends + UIs — and substantively verified; one browser-DOM pass is the only outstanding item**. Flow: Planner scoped `specs/folder-mgmt-download-search.md` → Master reviewed/accepted the F/Z/S safe defaults → Developer built the 3 backends (P4 `057605c`+`9664b6b`, P5 `3e7683f`, P6 `1c7ba12`), Master re-verified the suite each step → Tester backend-only pass **208 assertions + suite 137/137, 0 bugs/0 security holes** (`reports/2026-07-06_1810.md`) → Abhishek picked the UIs (P4→A kebab, P6→A `/search` page, P5→owner+guest button; `ac1da6a`) → Developer built all 3 UIs (`33731cb`: kebab rename/merge/delete on `/organize` w/ the F1 409 shared-block + Unfiled-confirm; "Download all" on `/organize`+`/browse`+`/g/[token]`-download_all; dedicated `/search` page + nav) → Master verified (frontend-only, no trailer, typecheck/lint/`next build` clean, all routes serve 200, Developer live-drove every API path over HTTP incl. the 409 block, delete→Unfiled, search filters, download_all gating). **The one gap: the pure browser-DOM Tester pass DID NOT complete — the browser/CDP harness failed a THIRD time (two 600s stalls, then a session-limit hit mid-run), NOT an app issue.** Given the build is clean, the backends are 208-verified, and the UI's API wiring was live-driven, P4/P5/P6 are judged **substantively ship-ready** with the browser-DOM pass (visual modal render, click interactions, a real zip save) as the sole outstanding verification — offered to Abhishek to retry, or he can click through locally. **With this, the entire Phase-1 local-first slice — roadmap through Week 12 + the three deferred features — is built end-to-end.** F/Z/S decisions open for veto. **PUSHED 2026-07-07 on Abhishek's "push" go-ahead** — the full 16-commit P7 + P4/P5/P6 backlog went up to `feature/ai-classification` (`af83a92..cafc1a1`, one fast-forward), feature branch only, no PR, no Claude trailer (verified pre-push); local/remote in sync at `cafc1a1`, `master`/`main` still absent on the remote. Note: dev DB holds ~26 `@example.com` Tester-seed accounts (accumulated across cycles, no real data) — clean-slate wipe available on request.)

Prior update (2026-07-06 by Master Agent): orchestration cycle COMPLETE — **Week 11–12 Audit log + polish is built and TESTED CLEAN — ship-ready.** Flow: Abhishek picked Week 11–12 → Planner scoped `specs/audit-and-polish.md` → Master reviewed → Developer built the audit backend + Helmet but **hit its session limit pre-commit with the safety classifier down**, so Master independently verified everything (additive migration, additive-only hooks in the security-tested guest routes, fire-and-forget helper, owner-scoped/append-only endpoint, 85/85 suite) before committing (`7fd5c15`) → a follow-up Developer completed polish P2/P3 (both **zero-code-change**) + the MR draft → Tester ran an adversarial pass: **41/41, 0 bugs, 0 security holes, backend 85/85** → P7 audit-viewer UI built (`238a3ee`/`78cff9d`/`c3db148`) and substantively Master-verified after two Tester harness stalls. **10 decisions (AP1–AP10) open for veto.**

---

## Current Phase
Phase 1 — MVP: the entire local-first slice through Week 12 + the three deferred features (P4/P5/P6) is BUILT END-TO-END (backends+UIs, Tester-verified/substantively-verified). Two small frontend dead-end bugs found+fixed 2026-07-08 (multi-file upload, Unfiled manual-move). **The trash system (specs/trash-system.md) is now BUILT end-to-end — backend (schema, soft-delete/restore/permanent-purge endpoints, the daily auto-purge job, the full 21-path listing-query audit, 173/173 suite) AND frontend (the `/trash` page + `/organize` multi-select delete, commit `861ff25`) — backend NOT yet tested by the Tester Agent, frontend NOT yet tested by the Tester Agent, neither pushed.** The P4/P5/P6 browser-DOM Tester pass also remains outstanding (harness-blocked 3×; low residual risk). **`specs/production-upload-batch.md` is now BUILT end-to-end (commit `e86f126`, not yet Tester-verified, not pushed) — see the top "Last updated" entry for the full breakdown.** **`specs/plan-tiered-upload.md` is a fresh, READY-FOR-DEVELOPER addendum (batch cap/storage/priority now vary by plan; a new dev-only plan switcher) — not yet built.** Nothing else in Phase 1 is unbuilt.

## Current Sprint Goal
**`specs/plan-tiered-upload.md` is now BUILT END-TO-END (both layers) — commit `f18d409`, `/settings` page + nav link done, unblocked by Abhishek's 2026-07-14 pick.** Nothing remains unbuilt in that spec; next step is a Tester pass on it (backend already 289/289, frontend never click-through-tested — no Playwright in this repo). In parallel, still open: a Tester pass on `specs/production-upload-batch.md` (backend 277/277 automated, no browser/network-inspection pass done yet — this repo still has no browser-automation tooling installed), a Tester regression pass on the trash system (backend + `/trash` UI + `/organize` multi-select, spec's own Success Signal walkthrough), the P4/P5/P6 browser-DOM UI pass (harness-blocked 3×; offered to retry or click through locally), the F/Z/S + AP/G decisions open for veto, optional dev-DB clean-slate, the deferred hardening majors (Next 15→16, vitest 2→4), and any further push go-ahead.

## Ready Spec (from Planner)
- **UPDATED 2026-07-14 — `specs/plan-tiered-upload.md` — BUILT end-to-end, BOTH layers (backend 289/289 suite green + commit `f18d409`'s `/settings` page + nav link). No longer blocked; ready for a Tester pass.** See the top "Last updated" entry for the full breakdown.
- Prior note (now superseded by the line above) — **UPDATED 2026-07-13 (even later) — `specs/plan-tiered-upload.md` — BACKEND BUILT end-to-end (289/289 suite green); FRONTEND partially built (upload-page error copy, api.ts types) — the `/settings` page itself is BLOCKED on Abhishek's wireframe pick (Pending Decisions above).** See the top "Last updated" entry for the full breakdown.
- Prior note (now superseded by the line above) — **NEW 2026-07-13 (later still) — `specs/plan-tiered-upload.md` — plan-tiered batch caps/storage/priority + dev-only plan switcher, READY FOR DEVELOPER, not yet built.** Addendum to `specs/production-upload-batch.md`, reinstating that spec's two deliberately-trimmed Non-goals now that Abhishek has concrete numbers (Free 50/5GB/low, Pro 500/100GB/normal, Studio 1500/500GB/high). Scope: `lib/plans.ts` gains `BATCH_LIMITS`/`STORAGE_LIMITS_BYTES`/`PRIORITY_BY_PLAN`; `/api/upload/initiate`'s batch-cap check moves out of the static Zod schema into a post-plan-lookup route check (with an absolute cross-tier ceiling kept at the schema layer); storage-limit sync-at-switch (no call-site changes needed at either quota check); both `photoProcessingQueue.add(...)` call sites (single-file + batch `/complete`) gain an explicit `priority`; new `PATCH /api/auth/plan` (no billing, audited, clearly labeled "testing only"); a new `/settings` page (needs Developer's usual wireframe round first, per CLAUDE.md). See top "Last updated" entry for full detail. 6 open questions (PTU1-PTU6) below — none block starting the `lib/plans.ts`/schema-adjacent work.
- **`specs/production-upload-batch.md` — BUILT end-to-end (commit `e86f126`), not yet Tester-verified, not pushed.** Supersedes/extends `specs/upload-pipeline.md`'s single-file flow (kept, unmodified, as the small-upload path) with a batch path: `POST /api/upload/initiate` (whole-batch quota check + `fileSha256` duplicate pre-check + per-file `createMultipartUpload` + presigned part URLs), browser uploads parts directly to MinIO/S3 (0 backend code, 0 bytes through the Node process), `POST /api/upload/complete` (assembles via `completeMultipartUpload`, post-assembly content-sniff, creates the SAME-shaped `Photo` row + enqueues the SAME `photoProcessingQueue` job the single-file route already uses — worker pipeline untouched), `DELETE /api/upload/abort`. New additive `UploadSession` + `UploadSessionFile` models. New multipart wrappers in `lib/storage.ts`, following the file's existing `usingRealS3` swap seam. Frontend `/upload` + `lib/api.ts` moved fully to the batch flow (even for 1 file). 10 open questions (PUB1-PUB10) — PUB4/PUB7/PUB10 resolved by Abhishek, PUB1/2/3/5/6/8/9 built on Planner's recommended defaults.
- **`specs/trash-system.md` — BUILT end-to-end (backend 2026-07-08 + frontend 2026-07-08, commit `861ff25`).** See "Last Developer Action" for the full breakdown. Not yet Tester-verified (either layer); not pushed. Supersedes `specs/photo-deletion.md` on PD1 (see below).
- **`specs/photo-deletion.md` — SUPERSEDED by `specs/trash-system.md`'s hard requirement (soft-delete, not hard-delete).** PD3 (duplicate-chain cascade-null), PD4 (photoCount reconciliation), PD5 (bulk endpoint partial-success shape, 100-item cap), and PD7 (honest confirm-copy discipline, now INVERTED to "moved to Trash, recoverable" language) were all REUSED as designed by `trash-system.md` and are now built. PD1 (hard delete) and PD2 (no guest-share block) as originally written were explicitly overridden by Abhishek's decision and `trash-system.md`'s T2 — NOT built as recommended; kept in the repo for the reasoning trail only, do not build against PD1/PD2 as written.

- `specs/ai-classification.md` — **built and shipped.**
- `specs/dashboard-stats-and-upload-polish.md` — **built and shipped.**
- `specs/week7-8-dashboard-browser-viewer.md` — **built and shipped, fully.**
- **`specs/guest-access-otp.md` — COMPLETE end-to-end** (backend `dad0b74` 68/68 + frontend `a2cfb3f`/`c27c09d` 30/30, pushed). G1–G12 open for veto.
- **`specs/audit-and-polish.md` — Part A + P1/P2/P3 + P7 audit-viewer UI BUILT and TESTED CLEAN 2026-07-06** (commits `7fd5c15` audit backend + Helmet, `f1ce08d`/`e3d4057` P2/P3, `238a3ee`/`78cff9d`/`c3db148` P7 `/activity` UI; Tester 41/41 on the backend, backend 85/85; P7 substantively Master-verified after two Tester harness stalls). audit_log table + fire-and-forget `lib/audit.ts` + 7 hooks + owner-scoped append-only `GET /api/audit`; Helmet headers (CSP dev-relaxed); `/activity` viewer page. On AP1–AP10 defaults.

- **`specs/folder-mgmt-download-search.md` — BUILT END-TO-END (backends + UIs) and substantively verified 2026-07-06.** Backends: P4 `057605c`+`9664b6b`, P5 `3e7683f`, P6 `1c7ba12` — Tester-cleared 208 assertions / 137/137. UIs: `33731cb` (P4 kebab, P5 button, P6 `/search`) on the picked wireframes (`ac1da6a`) — Master-verified clean; only the browser-DOM Tester pass is outstanding (harness-blocked 3×). F/Z/S decisions built-on the safe defaults, open for veto. Summary of the parts:
  - **P4 — folder rename/merge/delete.** `PATCH /api/folders/:id` (rename, 409-off-`@@unique`), `POST /api/folders/:id/merge` (move A's photos→B, both `photoCount`s reconciled via `serializableTransaction()`, delete A), `DELETE /api/folders/:id` (photos→Unfiled). NO schema change. Extends `routes/folders.ts`. **Load-bearing decision F1: a live guest share on a folder being merged-away/deleted → recommended BLOCK with 409 (don't silently migrate the grant to B [privilege escalation] or silently sever the guest).** Also F2 (delete→Unfiled not cascade), F4 (audit merge+delete, not rename), F5 (allow ops on AI folders too). Needs a wireframe round (`/organize`).
  - **P5 — bulk download-all (folder zip).** `GET /api/folders/:id/download-all` (owner) + `GET /api/guest/folders/:id/download-all` (guest), streaming a ZIP built on-the-fly from authorized MinIO reads via `archiver` (new dep, Z2) — no raw keys, no temp file. NO schema change. **Load-bearing decision Z3: inline-stream this pass with a 500-photo guard cap (409 if over) — async BullMQ/stored-zip DEFERRED; Z1: guest zip requires `download_all` (plain `download` → 403).** Thin UI (a "Download all" button on folder views incl. `/g/[token]`).
  - **P6 — basic search.** `GET /api/search` (owner-scoped) by filename (`ILIKE`), date range, folder, and category — plain SQL, no full-text infra (S4). NO schema change. New `routes/search.ts`. Paginated, pre-signed thumbnails. **S1: category filter = match folder name; raw-label search deferred. S2: filter on `createdAt` (EXIF-date deferred).** Needs a wireframe round (new search-bar + results surface).

All prior specs (through `specs/folder-mgmt-download-search.md`) are built and substantively tested. **`specs/photo-deletion.md` is superseded (see above); `specs/plan-tiered-upload.md` is the one unbuilt-but-ready spec now.**

## Last Tester Run
**2026-07-08 03:30 — Trash page + `/organize` multi-select delete UI regression (commit `861ff25`). 27/27 HTTP-level checks + backend 173/173 (2 of 3 runs clean, 1 transient flake self-healed on rerun). 0 bugs. Trash UI judged SHIP-READY, with one honest gap: no browser/Playwright tooling exists anywhere in this repo (not installed, not just flaky) so click-level DOM interaction was not observed.** Report: `reports/2026-07-08_0330.md`. Method: real signup blocked by an already-exhausted production rate limiter, so seeded 2 owners + folders/photos/guest-share directly via Prisma in a throwaway `backend/scratchpad_*.mjs` (deleted after, 0 residual rows) and minted session cookies the same way `createSession` does; verified every frontend/backend contract over live HTTP; code-reviewed the multi-select interaction logic in full.
- **All 6 Priority-1 API-contract checks PASS:** `GET /api/trash` shape exact; `POST /api/photos/bulk-delete`'s `{deleted,failed}` partial-result shape exact (guest-shared photo + a bogus id both correctly landed in `failed`); folder-restore collision triggered for real (409 conflict shape) and resolved BOTH ways (`onConflict:"merge"` and `"rename"`, both 200 with the exact shapes `FolderRestoreResult` expects); photo-restore-with-folder-also-trashed cascade verified both clean (auto-restores folder + photo in one call) and colliding (same 409 conflict shape, nothing restored) — using the actual reachable scenario (an individually-deleted photo, matching what the Trash page's Photos tab can ever list); guest-shared-folder photo delete → 409 confirmed; `DELETE /api/trash/:type/:id` and `DELETE /api/trash` (empty) shapes exact, cross-owner-scoping leak-proof.
- **Priority 3 code review of `/organize`'s multi-select logic — no bugs found, all correct on inspection:** Shift+click's contiguous range reads the live rendered grid order and doesn't move the anchor; Ctrl/Cmd+click toggles one id via a fresh Set copy, isolated from the rest; the marquee hit-test uses real `getBoundingClientRect()` per card (not an approximation) with a standard AABB overlap test, consistently viewport-relative coordinates throughout; the modifier-replace-vs-extend flag is captured at drag-START only (`mousedown`), not re-read at drag-end; "select all on page" only reads current-page state; "select all N across all pages" genuinely loops every page via real API calls, never fabricates.
- **Priority 4 regression — PASS:** kebab rename/merge still 200 over HTTP; the stale `photosOrphaned`/"Unfiled" delete-confirm copy is CONFIRMED FIXED — the modal now reads "will move to Trash — they'll stay recoverable for 7 days," no remaining stale references.
- **Priority 2 (browser) — NOT POSSIBLE, confirmed within ~2 minutes, not fought:** Playwright is not installed anywhere in this repo (front-end `node_modules/.bin`, root `node_modules/.bin`, and a full filename search all empty) — this is a tooling gap, not a flaky-harness-stall repeat of prior sessions. No rendered-DOM/console-error/click verification was performed; flagged as the one honest outstanding gap on an otherwise fully-verified feature.
- **Backend suite:** run 3× this session — 171/173 (2 transient parallel-load flakes, same class already documented 2026-07-08 02:45, non-reproducible), then 173/173, then 173/173 clean. Confirmed NOT a regression from the (frontend-only) Trash UI commit.

**2026-07-08 02:45 — Trash system BACKEND regression + Priority-0 flake investigation. 173/173 suite (8 of 9 runs this session clean), 0 new bugs. Trash-system backend judged SHIP-READY.** Report: `reports/2026-07-08_0245.md`. Method: backend-only, no browser/Playwright (per instruction) — `npm test -w backend` run 9 times total (7 full 13-file suites + 2 near-full 8-file subsets), plus direct `psql` schema inspection and direct reading of `trash-system.smoke.test.ts`'s 35 assertion bodies to confirm they test what their names claim.
- **Priority 0 (the reported intermittent guest-access 404) — INCONCLUSIVE, honestly reported, NOT reproduced.** 0/9 runs this session hit the specific `GET /api/guest/folders/:id/photos` 404 Abhishek saw 2/4 times. Read `getPermittedFolderIds` in full (single atomic query, no caching — cache-staleness race ruled out) and `createGuestSessionFromHash` (single atomic `create()` — ruled out as the culprit). **Confirmed a REAL, different pre-existing flake instead:** one run failed `classification.smoke.test.ts`'s rate-limit-carryover test on a plain 5000ms default-timeout (8 sequential bcrypt-gated logins under full-suite load) — already logged before trash-system existed (`38b5c2e`), now more likely to surface since the suite grew 137→173. Confirmed real write contention under full-suite load (`"Transaction failed due to a write conflict"` Prisma retries observed, self-healing) and a structural gap worth fixing regardless of root cause: **no `connection_limit` set on the test `DATABASE_URL`** (Prisma defaults to `2*cpus+1`=21 per client × up to 13 concurrent test-file processes on this 10-core machine vs. Postgres's `max_connections=100`) and **no `vitest.config.ts` raising the default 5000ms `testTimeout`**. Recommended to Developer as worth doing either way; did NOT fabricate a definitive root cause for the specific guest-access assertion since it never reproduced live this session.
- **T2 (reversed) — PASS.** Single-photo delete + each bulk-delete item on a live-shared folder → 409/not_found, nothing changed; revoke → succeeds. **F1 — PASS, unchanged regression.**
- **T7 partial-unique-index — PASS, confirmed live via `psql`:** `"folder_active_name_unique" UNIQUE, btree (collection_id, name) WHERE deleted_at IS NULL` exists exactly as decided; plain `@@unique` confirmed gone; live-vs-live collision still 409s.
- **Restore flows — PASS:** plain restore, 409-with-conflict-shape on no-onConflict collision, `onConflict:"merge"` (counts reconciled, source row hard-deleted), `onConflict:"rename"` (restore+rename atomic, re-collision handled), photo-restore auto-cascading its trashed folder (clean case + the same-409-shape collision case, photo stays trashed on unresolved collision).
- **Permanent delete / empty trash / purge job — PASS:** single-item purge (row+MinIO gone, folder cascades), empty-trash leak-proof both directions, purge job idempotent (8-day gone, 6-day survives, run-twice no double-effect, `photoCount` never double-touched on cascade).
- **21-path audit spot-checks — PASS, including #20 (the flagged leak):** download-all zip byte-scan confirms a soft-deleted photo (`status:done`, real `s3Key`) is genuinely absent from the zip; #1/#14/#15/#21 (unfiled, search, guest-folders, dashboard totals) all confirmed excluding trashed items.
- **Final suite: 173/173**, repeated to close out. **Verdict: Trash system backend SHIP-READY.** One Low, pre-existing (not trash-related) test-infra flake documented for Developer; the Priority-0 flake remains open/unresolved pending a live recurrence to instrument.

**2026-07-08 (later) — P4/P5/P6 UI browser pass: DID NOT COMPLETE (browser/CDP harness failed a 3rd time — session-limit hit mid-run after two prior 600s stalls; NOT an app issue). Master substantively verified the UIs instead (reduced-review recovery).** No report produced by the interrupted run; environment verified healthy afterward (backend/frontend/worker up, `/search` + all routes 200, process tree clean 1×each, working tree clean). **What Master verified directly on the UI build `33731cb`:** frontend-only commit (no backend touched), no Claude trailer, `typecheck`/`lint`/`next build` all clean, all routes serve 200. The Developer's own build-time live drive over HTTP had already exercised every API path the UIs call — rename 409/400/200, merge 200 + tree refresh, the F1 409 shared-block (merge AND delete of a live-shared folder → nothing moved), delete→Unfiled (photos reappear in `/api/photos/unfiled`), search q/limit-400/unfiled/whole-library, download-all empty→400 / not-owned→404 / unauth→401, and the guest `download_all` button gate (reads `GuestFolder.permissionLevel`). The underlying backends are Tester-cleared (208 assertions, 137/137). **NOT independently verified: the pure browser-DOM layer** — visual rendering of the kebab menu + the merge/delete modals + the red 409 shared-block, click interactions, the guest button's conditional visibility in the live DOM, and a real browser zip-save. Low residual risk (build-clean render code over already-verified endpoints/wiring), but it's the one honest gap — **the browser-DOM pass remains outstanding; retry later or Abhishek can click through `/organize` + `/search` + a guest link locally.**

**2026-07-06 18:10 — P4/P5/P6 deferred-feature BACKEND regression (backend-only, NO browser — deliberately, to avoid the two prior harness stalls). 208/208 (137 automated suite + 71 live black-box HTTP/DB/MinIO). 0 bugs, 0 security holes. ALL THREE BACKENDS SHIP-READY.** Report: `reports/2026-07-06_1810.md`. Tested `d3ce81e` (P4 `057605c`+`9664b6b`, P5 `3e7683f`, P6 `1c7ba12` all present). Method: owners/guests/folders/photos(+real MinIO objects)/permissions/sessions seeded DIRECTLY via Prisma in a throwaway scratchpad `.mjs` (now deleted); sessions minted by inserting the SHA-256 token hash; every fetch hard-timed 10s; zip validated at ZIP-format level (PK sig + EOCD central-dir parse, no unzip dep). All seed data cleaned — **0 residual `regr7test` rows confirmed post-run**.
- **Priority 0 — `npm test -w backend` → 137/137 green**, 0 flake (the expected count). The `prisma:error` unique-constraint log is the deliberate P2002 collision assertion.
- **(P4) F1 GUARD — PASS, adversarial + DB-confirmed:** merge AND delete of a live-shared folder both → 409 with NOTHING moved/deleted (2 photos + folder row intact in DB); revoking the guest perm cleanly re-enables both (merge→200 moving the photos, delete→200). The guard runs FIRST before any mutation.
- **(P4) merge count EXACT — PASS:** `B.photoCount == live COUNT(folderId=B) == 5`, no drift; A row deleted, no photo left pointing at A; one `folder_merged` audit row.
- **(P4) delete→Unfiled reachable — PASS:** a `done` orphan gets `folderId=null` AND appears in `GET /api/photos/unfiled` (the `9664b6b` reachability fix works live); photo row + MinIO original survive (not cascade-deleted); one `folder_deleted` row.
- **(P5) PASS:** valid zip (`application/zip` + `attachment`), exactly the 3 downloadable photos (`failed`/`duplicate` excluded), dup filename de-duped; **NO RAW KEY LEAK** (full body+header scan clean of `ownerId/`, `original.jpg`, `X-Amz-`, `minio`, `:9000`, `.amazonaws.com`); **Z1 `download_all` gate** 403s `download`- and `view`-only guests, 404s an unpermitted folder, 401s no-session; **Z7** exactly ONE `folder_downloaded` row on the successful guest zip, NONE on any 403/404/401.
- **(P6) LEAK-PROOF — PASS both directions:** owner A's `?q=` returns only A's photos, owner B's only B's; same owner-scoping holds for `?folderId=` (A→B's folder = 404) and `?category=` (A's Nature only, not B's). Filters all correct: case-insensitive `q` w/ pre-signed thumb (no raw key), `folderId=unfiled`→null photos, date range + `from>to`/invalid→400, unknown category→400, combined AND, `limit>101`/garbage→400, empty query→whole library, no-session→401.
- **Regression — PASS:** guest folder listing + guest photos-on-permitted-folder + owner `/api/collections` all 200; refused guest bulk accesses write NO `folder_downloaded` row (exact Z7 count).

**2026-07-06 (later) — P7 `/activity` audit-viewer: Tester pass STALLED TWICE (agent stream-watchdog, NOT app issues) → Master independently verified P7's substance (reduced-review recovery).** Both Tester dispatches died on the 600s no-progress watchdog inside the Tester's OWN browser-automation harness setup — first hanging on a Prisma seed script, then on a headless-Chrome CDP connection bootstrap (`Target.createTarget`). Neither reached app assertions; neither is an app defect. Rather than re-feed a flaky harness a third time, **Master verified P7 directly:**
- **Backend suite `npm test -w backend` → 85/85 green** (run this session) — audit hooks write correctly, guest-access regression intact.
- **`folderName` fix (`78cff9d`) confirmed LIVE end-to-end via DB inspection:** real `photo_viewed`/`photo_downloaded` rows in `audit_log` carry `metadata.folderName` ("People"/"Nature"), not just `folderId` — proving the backend captures the name on real 200s. The frontend reads it with an id fallback (`c3db148`, typecheck/lint clean).
- **`GET /api/audit` endpoint correctness was already Tester-verified 41/41 on 2026-07-06** (owner-scoping leak-proof both ways, filters, append-only, 401) and is UNCHANGED since except the additive `folderName` metadata — so the substantive data/scoping surface the `/activity` page renders is proven.
- **`/activity` serves 200; frontend `build`/`lint`/`typecheck` all clean.** The Developer's own build-time live check (pre-stall) confirmed the page's API calls return the correct `{entries,total,limit,offset}` shape, filters narrow, and no-session→401.
- **NOT independently re-verified this cycle (honest gap):** a full adversarial *browser-DOM* pass of `/activity` — rendered OWNER/GUEST badges, labels, "photo in Nature" display, in-browser filter/pagination clicking, empty-state DOM. Low residual risk (a read-only viewer over an already-verified endpoint, with build-clean render code + the Developer's serve/shape check), but the belt-and-suspenders browser pass remains outstanding — **offered to Abhishek to retry later.** P7 is judged substantively ship-ready with that caveat.
- **DB housekeeping:** the two stalled Testers left seed pollution; Master removed this cycle's `p7test-*` footprint (39 `audit_log` rows + 3 users + 2 guest_users + cascade), backend still healthy. **Flagged for Abhishek:** older test accumulation from prior Tester cycles remains in the dev DB (26 users, 28 guest_users, 138 audit_log rows, 22 access_requests — all `@example.com` seed accounts, no real data) — a clean-slate wipe is available on request, not done unilaterally.

**2026-07-06 01:35 — Week 11–12 Audit log + polish regression (audit backend + Helmet in `7fd5c15`, P2/P3 docs in `f1ce08d`/`e3d4057`; HEAD `e3d4057`). 41/41 live HTTP+DB+browser assertions + backend suite 85/85. 0 bugs, 0 security holes. WEEK 11–12 IS CLEAN — SHIP-READY.** Report: `reports/2026-07-06_0135.md`. **Audit trail Success Signal — PASS (live HTTP + DB):** ran the full sharing flow for two owners (share→request→OTP→approve→view→download→revoke) then `GET /api/audit`; owner A's trail is the exact complete ordered set newest-first — `guest_revoked → photo_downloaded → photo_viewed → access_approved → access_requested → share_created` — every row with correct `actorType` (owner vs guest), correct `ownerId`, the guest's captured IP on `access_requested`, and the exact A2 metadata (`folderNames:["Nature"]`+`permissionLevel:"download"` on share_created; `folderId` on view/download; reasons on denies). **Owner-scoping LEAK-PROOF — PASS both directions (the core risk):** seeded activity under two owners; each owner's `GET /api/audit` returned ONLY their own 6 rows, zero cross-owner leakage either way, including guest-actor rows (guest-a's view/download appear ONLY in owner A's trail). **Success-only logging — PASS (serious-risk item, clean):** DB inspection after deliberately triggering a view-only `/download`→403 and 404s shows `photo_downloaded` rows = 0 and `photo_viewed` rows = 1 (only the one 200 view); a 403 refusal and any 404 produce NO row. **access_denied reasons — all three correct:** `owner_denied` (explicit deny), `otp_attempts_exceeded` (3 wrong OTP → 401,401,403), `otp_expired` (expired→403), each exactly one row. **Fire-and-forget never breaks primary — covered by suite** (`audit.smoke.test.ts` "an approval STILL succeeds when the audit insert is forced to fail"). **Append-only — PASS:** PATCH/DELETE/POST/`GET :id` on `/api/audit` all 404; only the list GET exists; no session→401. **Filters+validation — PASS.** **Schema additive-only — PASS (DB-verified).** **Helmet (P1) — PASS.** **P2/P3 zero-change claims independently confirmed.** **Regression — PASS** (guest-access full flow + `/g/:token` privacy constraint still hold). **Backend `npm test -w backend` → 85/85**, 0 flake.

**2026-07-05 20:25 — Guest Access UI FIX-VERIFICATION pass. 30/30 + backend 71/71. 0 bugs. BOTH BUGS FIXED, PRIVACY CONSTRAINT STILL HOLDS — Guest Access UI is SHIP-READY.** Report: `reports/2026-07-05_2025.md`.

**2026-07-05 20:15 — Guest Access FRONTEND regression. 19/21 UI + backend 71/71. Two real bugs (1 High, 1 Medium) — both since fixed + verified.** Report: `reports/2026-07-05_2015.md`.

**2026-07-04 03:58 — Guest Access + OTP backend security regression (`dad0b74`), 68/68 live + backend 71/71, 0 bugs, 0 security holes; verdict SAFE and CORRECT.** Report: `reports/2026-07-04_0358.md`. Prior clean runs: 2026-07-03 23:40 (53/53 post-Next-14.2.35 bump), 19:02 (69/69 Dashboard), 16:47 (53/54, one fixture mistake), 08:55 (30/30 fix-verify), 07:31 (27/27 scripted + 1 High found/fixed), 02:27 (20/20 + 52/52), 00:50 (52/52); 2026-07-02 (28/28 — full-regression report `reports/2026-07-02_0450.md`, auth + upload pipeline, 0 bugs, superseded by all later work), 2026-07-01 (17/17).

## Last Developer Action
**2026-07-13 (even later) — Developer built `specs/plan-tiered-upload.md`'s backend end-to-end + the non-UI half of the frontend, on `feature/ai-classification` (local commits, no push, no Claude trailer), and produced the `/settings` wireframe round for the one remaining piece.** See the top "Last updated" entry and Pending Decisions above for full detail; short version: `User.plan` promoted to a real Prisma enum via a hand-written migration, `lib/plans.ts` gained the tier constants/lookups, `/api/upload/initiate` enforces the real plan-aware batch cap, both `photoProcessingQueue.add(...)` call sites (single-file + batch `/complete`) carry an explicit plan-derived `priority`, `PATCH /api/auth/plan` is a working no-billing switcher (audited, storage-synced), `GET /api/auth/me` returns `plan`. 12 new tests + 1 retargeted fixture, full backend suite 289/289, `tsc`/`eslint` clean both sides. **Stopped before building `/settings` itself** — proposed 3 SVG options instead, posted to Pending Decisions, recommends Option A. Next: build the page + nav link once picked.

**2026-07-08 (latest) — Developer produced the combined Trash-system UI wireframe round (SVG proposals only, NO React/Next/CSS, no frontend file touched, no commit needed).** Per `specs/trash-system.md`'s own closing note, Surface A (multi-select-delete on `/organize`/`/browse`) and Surface B (the Trash page) were proposed TOGETHER so confirm-dialog copy and Trash-page tone stay consistent. 6 SVGs in `design/wireframes/proposals/`: `multi-select-delete-option-{a,b,c}.svg`, `trash-page-option-{a,b,c}.svg`, all matching the established `viewBox="0 0 800 600"` style (`design/wireframes/folder-mgmt.svg`/`guest-management.svg` conventions — top bar, white cards, `#f3e2e2`/`#9a5555` attention color for irreversible actions, `#e8eefc`/`#1b3a9e` accent, two-line footnote). Built against the REAL shipped contracts (`DELETE /api/photos/:id` 409-on-live-share, `POST /api/photos/bulk-delete` partial-success shape, `GET /api/trash`, `.../restore` with `onConflict`, `DELETE /api/trash/:type/:id`, `DELETE /api/trash`) — every wireframe shows real data shapes (mixed photos+folders, varying `daysRemaining` incl. a "1 day left" urgency case, a restore-collision merge/rename choice, and Surface A's T2 partial-result case "2 deleted, 1 skipped — shared with a guest"). Confirm-dialog copy on Surface A deliberately uses the reversible "moved to Trash, recoverable for 7 days" language (PD7/T6); the Trash page's OWN Delete-forever/Empty-trash actions are the ones marked genuinely irreversible, with a stronger red-bordered confirm on Empty-trash. **Surface A — 3 options:** (A) checkbox-on-hover + persistent bottom bar, (B) explicit "Select photos" mode toggle (mirrors P4's kebab-menu mode-gate precedent), (C) long-press/right-click entry, same visual language as A. **Recommendation: A** — lowest friction for what's usually a small, opportunistic multi-delete (few extra clicks vs B's explicit mode entry/exit), and more discoverable than C's hidden gesture; B is the safer runner-up if Abhishek wants an unambiguous mode boundary. **Surface B — 3 options:** (A) dedicated `/trash` page with its own top-bar entry and Photos/Folders tabs (mirrors `/activity`/`/search`), (B) a virtual "Trash" row folded into `/organize`'s sidebar (reuses the existing shell, smallest build footprint, but cannot show a live-count badge from other pages), (C) a top-bar trash icon+badge opening a slide-over panel (badge visible everywhere, but the panel is cramped for showing both photos and folders plus a restore-collision choice with two full-width buttons). **Recommendation: A** — trash is exactly the kind of infrequent-but-needs-room surface (mixed types, collision resolution, empty-trash confirm) that benefits from a full page rather than a sidebar row or a narrow panel, and it matches the precedent already set by `/activity`/`/search` getting their own nav entries. Posted to Master for Abhishek's pick — did not choose on his behalf. Once picked, the chosen SVGs become `design/wireframes/trash-page.svg` and `design/wireframes/multi-select-delete.svg`, and P4's stale `photosOrphaned`-reading delete-confirm modal copy gets fixed as part of that same build pass (per the known deviation flagged when the trash backend shipped).

**2026-07-08 (later still) — Developer built the ENTIRE trash-system BACKEND per `specs/trash-system.md`, 5 commits on `feature/ai-classification` (backend + tests only, no push, no Claude trailer).** Commits: `9b8da10` (schema migration + storage/audit/purge foundations), `c52a3f4` (soft-delete/restore endpoints), `7410271` (daily auto-purge job), `99296f1` (21-path listing-query audit), `e9f9e81` (test coverage + retargeted P4 tests). Full breakdown in `reports/mr-drafts/trash-system.md`; summary:

- **Schema:** additive `deletedAt` on `Photo`/`Folder` + new indexes. **T7 fixed for real:** the plain `@@unique([collectionId, name])` on `Folder` is GONE, replaced by a Postgres **partial unique index** (`folder_active_name_unique ... WHERE deleted_at IS NULL`) added via a hand-edited, loudly-commented raw-SQL migration (`20260707201747_add_trash_system`) — Prisma's DSL can't express the `WHERE` clause, so `schema.prisma`'s own `@@index([collectionId, name])` is now deliberately non-unique with a comment warning against "fixing" it back. **Verified live via direct SQL:** a trashed folder + a new live same-named folder → both insert cleanly (no 409); two live same-named folders → still correctly collide.
- **Endpoints:** `DELETE /api/photos/:id` + `POST /api/photos/bulk-delete` now soft-delete (T2 FINAL/reversed: 409 if the photo's folder is live-shared, same posture as F1, checked per-item in bulk too). `DELETE /api/folders/:id` REVISED (F1 unchanged, runs first) to soft-delete instead of hard-delete-and-orphan-to-Unfiled — **response shape changed, `photosOrphaned` is gone, replaced by `deletedAt`/`purgeAt`** (see deviation flag below). New `POST /api/photos/:id/restore` (auto-cascades into restoring an also-trashed folder first, surfacing the SAME 409 conflict shape on an unresolved collision) and `POST /api/folders/:id/restore` (`onConflict: "merge"` or `"rename"` on a T7 name collision). New `routes/trash.ts` mounted at `/api/trash`: `GET /api/trash` (list), `DELETE /api/trash/:type/:id` (purge one now), `DELETE /api/trash` (empty everything).
- **Auto-purge job:** BullMQ `upsertJobScheduler` (available in the installed `bullmq@5.79.2`), daily, on the EXISTING `photo-processing` queue/worker (job name `trash-purge`). Idempotent registration confirmed live (registering twice → exactly one scheduler in Redis). New `lib/purge.ts` (shared `purgePhoto`/`purgeFolder`, used by BOTH the manual purge endpoint and the job — T5 Option A: folder purge cascades to hard-delete its photos) and `lib/trashPurgeJob.ts`. Idempotency (safe to run twice, no error/double-effect) verified via the test suite for both a standalone photo and a folder cascade.
- **21-path listing-query audit:** all 21 rows addressed — 19 required a code change, #10 (collections list) confirmed N/A, #12 (folder create) confirmed N/A for its own logic (its P2002 now comes off the new partial index, resolving the original friction as a T7 side-effect). **#20 (the flagged highest-risk leak)** fixed in `lib/folderDownload.ts`'s `queryDownloadablePhotos` — without it, a soft-deleted photo (still `status: done`, real `s3Key`) stayed zippable through the already-shipped download-all endpoint. **#14 (`GET /api/search`, the audit's own highest-leak-risk row) needed an ADDITIONAL fix beyond the spec's literal description** — the base query's `deletedAt: null` alone did NOT catch a photo whose FOLDER (not itself) is trashed in the no-folder-filter/default case; caught only by the new test suite, fixed with an `OR: [{folderId: null}, {folder: {deletedAt: null}}]` clause. `getPermittedFolderIds` (the guest single choke point) now excludes trashed folders so every downstream guest route inherits the fix for free.
- **Tests:** new `trash-system.smoke.test.ts` (35 tests) + 3 retargeted `folder-mgmt.smoke.test.ts` assertions (the old hard-delete-and-orphan behavior no longer exists) + 1 adapted reachability regression test. **Backend suite: 137 → 173, all green.** `typecheck`/`lint` clean. Migration applies cleanly.
- **Deviation flagged (not buried):** the shipped P4 `/organize` delete-confirm modal reads the now-removed `photosOrphaned` field and shows "N photos move to Unfiled" copy that is now factually stale (nothing is orphaned under soft-delete) — **not fixed this pass**, frontend was explicitly out of scope; queued into the combined Trash-page + multi-select-delete wireframe round below.
- **Backlog note (per the spec's own instruction):** T5's veto option — spilling a purging folder's photos to Unfiled instead of hard-deleting them — is logged here as a genuine future feature request, not built.

**Next step: a Tester regression pass** (not yet run) — the spec's own Success Signal is a ready-made walkthrough script. **UI wireframe round still queued, not started:** a combined Trash-page (list/Recover/Delete-forever/Empty-trash) + multi-select-delete round on `/organize`/`/browse`, per the spec's closing recommendation to design both together (consistent confirm-copy tone). Backend does not block on this.

**2026-07-08 (later) — Developer fixed a real gap: `/upload` now supports uploading multiple files at once, commit `8d5b74a` (frontend-only, no push, no Claude trailer).** Found via manual testing + direct user feedback: `/upload` tracked a single `file: File | null` (no `multiple` attribute, drag/drop only took `e.dataTransfer.files[0]`) — the backend's `upload.single("file")` one-file-per-request contract in `backend/src/routes/photos.ts` is by design and was NOT touched. **Fix (frontend-only):** reworked `frontend/src/app/upload/page.tsx` from the single-file proof-of-concept into a per-file `UploadItem[]` queue (key/file/queueStatus/progress/photoId/pollStatus/labels/duplicateOfPhotoId/thumbnailUrl/error per item — mirrors `/organize`'s `PhotoCard` per-card state-modeling idea, not the component itself). File input gained `multiple`; `handleDrop` now passes the whole `FileList`. Uploads run through a **capped-parallel queue (3 workers, `UPLOAD_CONCURRENCY`)** calling the existing unchanged `photosApi.uploadWithProgress` once per file — chosen over sequential (I/O-bound, meaningfully faster for a multi-photo batch) and over unbounded-parallel (gentle on the rate limiter + the single-file-buffered route). Each item gets its own progress bar, its own `setInterval` poll loop (tracked in a `Map<key, interval>` instead of one shared ref, individually cleared), and its own rendered result row — one item failing (upload error, poll error, `failed` classification) never blocks or hides the others. Updated the stale "Week 7-8 scope, deliberately unstyled" header comment. **Verification:** `typecheck`/`lint`/`next build` all clean (fixed a real type-narrowing error + a `react-hooks/exhaustive-deps` ref-cleanup warning surfaced along the way; built to an isolated `distDir` since `next dev` was live, reverted after, including an incidental `tsconfig.json` reformat the build triggered — reverted separately so the commit is page-only). **Live-checked the underlying endpoint the page now loops:** 3 sequential `POST /api/photos/upload` calls under one throwaway session each returned a distinct `photoId` (202), confirming the single-file endpoint handles the repeated-independent-call pattern the queue relies on; seed cleaned, 0 residual rows. **No browser/CDP click-through this pass** (multi-file drag/drop + the concurrency interleaving are easiest to observe visually — re-read the render logic post-edit as a substitute; flagged as a recommended follow-up, consistent with recent Developer passes on this codebase). MR draft `reports/mr-drafts/multi-file-upload.md`. **Files:** `frontend/src/app/upload/page.tsx`.

**2026-07-08 — Developer fixed a real dead-end bug: failed/duplicate photo cards on `/organize` now always get a Move-to-folder dropdown, commit `4e33e25` (frontend-only, no push, no Claude trailer).** Found via manual testing (report described directly, not a Tester report file): `PhotoCard`'s action area was an if/else-if keyed on `photo.status` — a `failed` card got ONLY "Reclassify", a `duplicate` card got ONLY "Not a duplicate?", and every other status got ONLY the Move dropdown, mutually exclusive. A photo stuck failing classification, or a duplicate flag the user disagreed with but didn't want to re-trigger detection on, had **no manual way to file it into a folder** — most visible for such photos sitting in Unfiled. Backend `PATCH /api/photos/:id` (`backend/src/routes/photos.ts`) was confirmed to already ignore `aiClassificationStatus` entirely (verified live via HTTP below) — purely a frontend gating bug, no backend file touched. **Fix:** removed the `photo.status !== "failed" && photo.status !== "duplicate"` guard around the Move `<select>` in `frontend/src/app/organize/page.tsx` — it now renders unconditionally on every card; the Reclassify / "Not a duplicate?" buttons keep their existing status-gating unchanged, so failed/duplicate cards now show BOTH stacked. Added `.organize-card-action { display:flex; flex-direction:column; gap:6px }` to `globals.css` for the two-control spacing (no other card styling touched). **Second bug found+fixed in the same pass:** `handleMove` reconciled real folders' `photoCount` on a successful move but never decremented the sidebar's `unfiledCount` when the source was Unfiled (the `setFolders` mapping is a harmless no-op for `UNFILED_FOLDER_ID` since no real Folder row has that id — nothing else covered it). This bug pre-existed (would have hit any `done`-status orphan reachable in Unfiled per `9664b6b`) but was newly reachable for failed/duplicate cards once they got a Move control. Fixed with `setUnfiledCount((prev) => Math.max(0, prev - 1))` guarded on `sourceFolderId === UNFILED_FOLDER_ID`, same "server confirms success, targeted local update" pattern as the real-folder counts; stale comment claiming Unfiled never reaches `handleMove` updated. **Verification:** `typecheck`/`lint` clean; `next build` clean (isolated `NEXT_DIST_DIR` scratch dir since `next dev` was live on the shared `.next`); live HTTP check against the running Docker stack — seeded a throwaway user + Unfiled `failed` photo + Unfiled `duplicate` photo, confirmed both listed via `GET /api/photos/unfiled`, `PATCH`'d both to a real folder (200, correct `folderId`/`folderName`), confirmed both then absent from `/unfiled` — proves the backend path this fix now exposes in the UI; seed cleaned up immediately, 0 residual rows, throwaway scripts deleted (not committed). No browser/CDP click-through run this pass (re-read the render logic post-edit as a substitute — confirmed the `<select>` renders unconditionally alongside the status-gated button). MR draft `reports/mr-drafts/unfiled-manual-move-fix.md`. **Files:** `frontend/src/app/organize/page.tsx`, `frontend/src/app/globals.css`.

**2026-07-07 — Developer fixed a real navigation bug: every authed owner page now has a consistent full top-bar nav, commit `2588f67` (frontend-only, no push, no Claude trailer).** Found by direct inspection (no Tester report this cycle): after login, `/dashboard` had no link to `/upload` or `/organize` at all — both were URL-only dead ends — and every other page's top bar was missing most of its cross-links (`/organize` and `/browse` had NO nav links whatsoever; `/upload` had NO top bar at all; `/share` had one ad-hoc inline-styled link). Fixed by giving all 8 authed pages (`/dashboard`, `/organize`, `/browse`, `/upload`, `/share`, `/guests`, `/activity`, `/search`) the same top-bar link set to the other 7 pages, ordered **Upload · Organize · Browse · Search · Guests · Activity** (plus **Dashboard** via a new home/logo link on the "PhotoSphere AI" title, `.organize-topbar-logo-link`, present on every page except `/dashboard`'s own). Reused the existing `.dashboard-guests-link` + `.dashboard-topbar-right` classes as-is (no new nav component, no rename — considered a generic `.topbar-nav-link` rename but skipped it as an unnecessary bigger diff; flagged as an optional follow-up). `/upload` gained a top bar for the first time. **Files:** `frontend/src/app/{dashboard,organize,browse,upload,share,guests,activity,search}/page.tsx` + `globals.css`. **Verification:** `typecheck`/`lint`/`next build` all clean (built to an isolated `.next-verify` distDir via a temp `next.config.js` edit, reverted after, since `next dev` was live); all 8 pages served `200` live; `grep`-verified each page's rendered href set = exactly its 7 counterparts, no self-links, no gaps. No browser-DOM click-through pass done this cycle (low risk — pure additive links over already-verified pages). MR draft `reports/mr-drafts/consistent-topbar-nav.md`.

**2026-07-06 — Developer built the P4/P5/P6 FRONTEND UIs (frontend-only, no backend file touched, no push, no Claude trailer).** Built against the three Tester-verified backends + the chosen wireframes (`design/wireframes/folder-mgmt.svg` P4 Option A, `search.svg` P6 Option A; P5 a thin button). **Files:** `frontend/src/lib/api.ts` (+`foldersApi.rename/merge/remove`, `downloadAllApi.ownerFolderUrl/guestFolderUrl`, `searchApi.search` + types), NEW `frontend/src/app/search/page.tsx`, `frontend/src/app/organize/page.tsx` (P4 kebab menu + inline rename + merge/delete modals + P5 owner Download-all), `frontend/src/app/browse/page.tsx` (P5 owner Download-all), `frontend/src/app/g/[token]/page.tsx` (P5 guest Download-all), `frontend/src/app/dashboard/page.tsx` + `activity/page.tsx` (Search nav link), `frontend/src/app/globals.css`. **P4:** per-folder `⋯` kebab (Rename/Merge into…/Download all/Delete) on real folders only (not Unfiled; on AI folders too per F5). Rename inline, 409-collision inline. Merge modal (destination = owner's OTHER folders), **409 flips to the shared-with-guest block** ("revoke first" + Go-to-Guests link). Delete confirm with the **"N photos move to Unfiled — not deleted"** copy, **same 409 shared-block**. Merge/delete refresh the tree + counts from the server on success. **P5:** owner Download-all on `/organize` + `/browse` folder headers (real+non-empty only); guest Download-all on `/g/[token]` gated on `permissionLevel === "download_all"` (already exposed per-folder — NO GAP). **Zip download = a credentialed top-level browser navigation** (`window.location.assign`) to the streaming endpoint (Content-Disposition attachment), NOT a fetch-into-memory. **P6:** dedicated `/search` page (owner-gated, draft/applied filter split, `.organize-grid`+`PhotoViewer` reuse, pagination, loading/empty/error states, cancelled stale-guard); Search nav link added to dashboard + activity. **typecheck + lint clean; `next build` clean** (built to an isolated `.next-verify` dir so the live `next dev` `.next` was untouched; temp config/tsconfig edits reverted). **Live-drive verified over HTTP:** rename 409/400/200, merge 200 (moved=3, targetCount=5), merge+delete of a live-shared folder→409 (nothing moved), post-revoke delete→200 (4 photos→Unfiled, confirmed in `/api/photos/unfiled`), search q/limit-400/unfiled/empty-whole-library, download-all empty→400 + not-owned→404; unauth→401 + `/search` client-gates to `/login`. Seed cleaned (0 residual). MR draft `reports/mr-drafts/folder-search-ui.md`. **No deviation.** This completes the P4/P5/P6 UIs — the entire Phase-1 local-first slice is now built end-to-end; next is a Tester browser pass.

**2026-07-06 — Developer built P6 (basic search) BACKEND, commit `1c7ba12`. Backend + tests only, no UI (P6's search UI is wireframe-blocked — out of scope), no schema change, no migration, no new index (S5), no new dep, no audit (S7), no push. NO Claude trailer (author Abhishek).** New `backend/src/routes/search.ts` — `GET /api/search`, owner-scoped, mounted `/api/search` in `app.ts` (top-level GET, no `:id` shadowing). All filters via a Zod query schema `searchQuerySchema` added to `lib/validation.ts` (+ `SEARCH_CATEGORIES` enum, `UNFILED_FOLDER_LITERAL`). Built on the confirmed S1–S7 defaults: **`q`** substring on `originalFilename` case-insensitive (`contains`+`mode:insensitive` = ILIKE, S4), trimmed/empty→absent; **`from`/`to`** range on **`createdAt`** (S2, EXIF `takenAt` deferred), invalid date / `from>to` → 400; **`folderId`** UUID→restrict (not-owned/nonexistent → 404 house rule) or literal **`unfiled`**→`folderId:null` (S3); **`category`** Zod enum (unknown→400) implemented as an **owner-scoped folder-name match** (S1 — match the caller's OWN folders whose `name`=category, then `folderId IN (those)`; no-match→empty; category+specific-folder intersected, category+unfiled→empty); **`limit>100`→400** (house rule, not clamp). **LEAK-PROOF:** every query starts from `ownerId=req.user.id`; folder/category lookups are owner-scoped so nothing widens past the caller. **S6:** empty query → whole library newest-first paginated. Reuses `toPhotoCard`/`PHOTO_CARD_SELECT` → pre-signed 60s thumbs, never a raw key; `{photos,total,limit,offset}` shape (same as `GET /api/folders/:id/photos`). New `backend/src/__tests__/search.smoke.test.ts` (20 tests, Prisma-seeded, no worker: q case-insensitive+no-key-leak, date range/invalid/from>to, folderId owned/not-owned-404/unfiled, category match/unknown-400/no-match-empty/**owner-scoped-not-another-owner's-Nature**, combined AND, limit>100+garbage 400, S6 newest-first, **two-owner overlapping-filename leak test both directions**, 401). **typecheck + lint clean; full backend suite 137/137 (was 117, +20).** MR draft `reports/mr-drafts/search.md`. No deviation. **P4/P5/P6 backends are now ALL built; the P4 + P6 search UIs remain wireframe-blocked, P5 is a thin button.**

**2026-07-06 — Developer built P5 (bulk "download all" / folder zip) BACKEND, commit `3e7683f`. Backend + tests only, no UI, no schema change, no migration, no push. NO Claude trailer (author Abhishek).** Two endpoints stream a ZIP assembled on-the-fly from authorized MinIO reads — the client receives ONLY zip bytes, never a raw `s3Key`/pre-signed URL. New shared helper `backend/src/lib/folderZip.ts` (`streamFolderZip`: `archiver('zip')` piped to `res`, per-object read-stream append, filename de-dup, Z5 abort+destroy on mid-stream failure — never a silently-incomplete 200, never buffered to memory/temp-file); new `backend/src/lib/folderDownload.ts` (shared Z4 inclusion = only stored `done` originals + Z6 empty→400 + Z3 >500-cap→409 pre-flight, unit-testable); `backend/src/lib/storage.ts` gained `getObjectStream()` (authorized server-side read stream, distinct from the client pre-signed-URL helper). Owner `GET /api/folders/:id/download-all` (404 not-owned, no audit per Z7/AP1). Guest `GET /api/guest/folders/:id/download-all` (404 outside permitted set → Z1 `download_all` gate else 403 → Z7 ONE `folder_downloaded` row on success only). Additive `folder_downloaded` action string in `lib/audit.ts`. **Dep added (Z2): `archiver ^7.0.1` + `@types/archiver ^6.0.2`** (pure Node; `npm audit` count unchanged). New `folder-zip.smoke.test.ts` (11 tests, real MinIO reads, zip validated at ZIP-format level with NO unzip dep — proves valid zip, exact entry count, entry names are original filenames not keys, no raw-key/URL anywhere in the body; Z1 403 gates; cross-owner/unpermitted 404; Z6 400; Z4 skips; de-dup; Z3 cap 409; Z7 one-row-on-success / none-on-refusal). **typecheck + lint clean; full backend suite 117/117 (was 106, +11).** MR draft `reports/mr-drafts/bulk-zip-download.md`. No deviation. P4 already built; P6 (search) remains unbuilt.

**2026-07-06 (later) — Developer CLOSED the P4 `/unfiled` reachability gap it flagged, commit (see MR draft). Backend + tests only, no push, no schema change.** `backend/src/routes/photos.ts` `GET /api/photos/unfiled` WHERE broadened from `folderId: null AND status IN ('failed','duplicate')` to **`folderId: null AND status NOT IN ('pending','processing')`** — "unfiled" now means *not in any folder* whatever the reason. This surfaces a `done` photo orphaned by `DELETE /api/folders/:id` (F2 moves photos to `folderId = null` but leaves them `done`) — previously invisible in both the `/organize` Unfiled bucket and the `/dashboard` Unfiled tile, silently breaking F2's "photos remain reachable" promise (recreating the 2026-07-03 "photos unreachable" bug class). Status enum (free string, `schema.prisma:133`): `pending|processing|done|duplicate|failed`; no distinct `uncategorized` status (Uncategorized = folder only). Phrased as `NOT IN (pending,processing)` so future terminal statuses surface by default and transient mid-pipeline photos stay excluded. **No filed photo can leak** (anchored on `folderId IS NULL`; negative test proves a filed `done` photo never appears). Response shape / pagination / pre-signed thumbnails unchanged → no frontend change. Added 4 tests to `folder-mgmt.smoke.test.ts` (new-case orphan-appears, filed-done-never-appears, pending/processing-excluded, failed-still-appears) on a dedicated deterministic-count owner. **No count-assertion changes elsewhere** — the two `/unfiled` assertions in `classification.smoke.test.ts` are on fresh single-unfiled-photo users, unaffected. **typecheck + lint clean; full backend suite 106/106** (was 102). MR draft `reports/mr-drafts/folder-mgmt.md` deviation section flipped to RESOLVED.

**2026-07-06 — Developer built P4 (folder rename / merge / delete) BACKEND, commit (see MR draft). No UI, no schema change, no migration, no new dep, no push.** Extended `backend/src/routes/folders.ts` with `PATCH /api/folders/:id` (rename, 409-off-@@unique P2002, empty→400, cross-owner→404, no audit per F4), `POST /api/folders/:id/merge` (both-owned→404, self→400, cross-collection→400 per F3, F1 live-share guard→409-FIRST, move+both-counter reconcile in ONE `serializableTransaction()`, delete A, `folder_merged` audit per F4), `DELETE /api/folders/:id` (F1 guard→409-FIRST, F2 photos→Unfiled `folderId=null` in a `serializableTransaction()`, `folder_deleted` audit). Built on confirmed F1–F6 defaults; F5 allows all ops on ai_generated + custom. Added `folderRenameSchema`/`folderMergeSchema` to `lib/validation.ts`; two additive `AuditAction` strings (`folder_merged`/`folder_deleted`) to `lib/audit.ts`. New `backend/src/__tests__/folder-mgmt.smoke.test.ts` (17 tests, Prisma-seeded, no worker dep). **typecheck + lint clean; full backend suite 102/102 (was 85, +17).** MR draft `reports/mr-drafts/folder-mgmt.md`. **DEVIATION FLAGGED for Master/Planner:** the P4 delete AC says orphaned photos "appear under `/api/photos/unfiled`", but that route filters `status IN (failed,duplicate)` — a `done` orphan (`folderId=null, status=done`) satisfies "unfiled" but not that route's filter. Did NOT broaden it (would change a Tester-verified surface + is a product-semantics call, not a Developer guess). Delete is spec-correct (`folderId=null`); test asserts that directly. Decision needed: broaden `/unfiled` or amend the AC. P5/P6 remain unbuilt.

**2026-07-06 (Master reconciliation) — P7 audit-viewer is BUILT end-to-end; the folderName gap is fully CLOSED.** Master made the final trivial 2-line edit directly — `frontend/src/app/activity/page.tsx` `detailLine` now prefers `metadata.folderName` with a graceful id fallback — commit **`c3db148`**, no Claude trailer, `typecheck`/`lint` clean, `/activity` serves 200. **P7 is code-complete.** A Tester regression pass on 2026-07-06 STALLED mid-setup (agent watchdog timeout, NOT an app issue) — environment verified healthy afterward and the pass being re-run/substantively-verified by Master.

**2026-07-06 (later) — Developer closed the P7 backend metadata gap: `photo_viewed`/`photo_downloaded` now capture `folderName`, commit `78cff9d`.** `backend/src/routes/guest.ts` only — the two success-path `logAudit(...)` calls now include the folder name. Additive; no migration. `npm test -w backend` 85/85.

**2026-07-06 — Developer built the P7 owner audit-log viewer UI (`/activity`), commit `238a3ee`.** Built against `design/wireframes/audit-viewer.svg` (Option A). No backend file touched. `frontend/src/app/activity/page.tsx` + `frontend/src/lib/api.ts` (`auditApi.list`) + `globals.css`. typecheck/lint/build clean.

**2026-07-06 — Developer completed Week 11–12 polish P2 + P3 (both zero-code-change) + the whole-build MR draft, commit `f1ce08d`. Follows Part A + P1 in `7fd5c15`.** P2 (rate-limiter coverage audit) and P3 (7-page empty/error/loading sweep) both resolved to zero code change — the enumeration table and per-page findings are the deliverable. `npm test -w backend` 85/85.

**2026-07-05 — Developer fixed both Guest Access frontend bugs, commit `c27c09d` (frontend-only).** BUG-1 (wrong OTP no longer ejects the owner) + BUG-2 (returning approved guest lands directly in unlocked via a load-time probe). Privacy constraint re-confirmed. typecheck/lint/build clean; verified live over HTTP.

**2026-07-04 (later) — Developer built the three Guest Access frontend pages, commit `a2cfb3f`** (`/share`, `/guests`, `/g/[token]`), against the tested backend + picked wireframes. Privacy constraint verified live. MR draft `reports/mr-drafts/guest-access-ui.md`.

**2026-07-04 — Developer built the Guest Access + OTP backend/API/data-model core, commit `dad0b74`.** 5 additive models + migration `20260703215312_add_guest_access_otp`, `lib/guestSession.ts`, `middleware/requireGuest.ts` (+ `getPermittedFolderIds()`), `lib/notifications/index.ts` (mock OTP), `lib/otp.ts`, `middleware/inviteRateLimiter.ts`, four routers. Backend suite 71/71. MR draft `reports/mr-drafts/guest-access-otp.md`.

(Earlier Week 1–8 Developer actions — AI-classification layer, dashboard-stats, organize UI, browse+viewer, dashboard page — omitted here for brevity; all built, tested clean, and pushed. See git history / prior reports.)

## Open Bugs
**0 OPEN.** The `/upload` single-file-only limitation (no `multiple` attribute, drag/drop took only the first dropped file) is FIXED 2026-07-08 (`8d5b74a`) — see Last Developer Action. Frontend-only; backend's one-file-per-request contract intentionally unchanged. Not independently Tester-verified via a browser click-through yet (build-clean + live-HTTP-verified-per-call by the Developer). The Unfiled manual-move dead-end (failed/duplicate cards had no Move dropdown at all, plus a stale `unfiledCount` on move-out) is FIXED 2026-07-08 (`4e33e25`) — see Last Developer Action. Frontend-only, backend already correct. Not independently Tester-verified via a browser click-through yet (build-clean + live-HTTP-verified by the Developer). The top-bar navigation dead-end (`/upload`/`/organize` unreachable from anywhere, inconsistent nav across all 8 authed pages) is FIXED 2026-07-07 (`2588f67`) — see Last Developer Action. Not independently Tester-verified via a browser click-through yet (build-clean + live-200 + href-grep verified by the Developer). Both bugs from the 2026-07-05 20:15 Guest Access frontend regression are FIXED (commit `c27c09d`) AND Tester-verified fixed (2026-07-05 20:25, 30/30 + backend 71/71). Backend Guest Access verified SAFE/CORRECT (68/68 live + 71/71 suite, 2026-07-04). Week 11–12 audit+polish + P7 verified clean (41/41 backend, 85/85 suite). The full Week 1–12 slice has been through the plan→build→test loop with zero open bugs. **P4/P5/P6 backends verified clean 2026-07-06 18:10 (208/208 — 137 suite + 71 live; 0 bugs, 0 security holes) — the F1 guard, P5 no-raw-key + Z1 gate, and P6 leak-proof owner-scoping all confirmed adversarially; all three backends SHIP-READY (their UIs remain wireframe-blocked / a thin button, unbuilt).** **The trash system (specs/trash-system.md) is now BUILT end-to-end 2026-07-08 — backend (5 commits, 173/173 suite) + frontend (`861ff25`, the `/trash` page + `/organize` multi-select delete) — NEITHER layer yet Tester-verified, NOT pushed.** The P4 `/organize` delete-confirm modal's stale `photosOrphaned`-reading copy is FIXED as part of the frontend commit above (now correctly says "moves to Trash, recoverable for 7 days").

Non-bug items flagged for attention (not blocking):
- **P4 delete → `/api/photos/unfiled` filter mismatch — RESOLVED 2026-07-06** (was flagged when P4 landed `057605c`). `/api/photos/unfiled` broadened to `folderId: null AND status NOT IN ('pending','processing')` so folder-delete `done` orphans stay reachable; 4 tests added; suite 106/106; no frontend change. See the Last Developer Action entry + `reports/mr-drafts/folder-mgmt.md` (deviation section now RESOLVED).
- **Duplicate-of-a-duplicate chain edge case** (AI-classification cycle). Accepted trade-off, not reproduced live, no data loss.
- **P7 `/activity` browser-DOM pass outstanding** — a belt-and-suspenders adversarial in-browser render/filter/pagination pass never ran (two Tester harness stalls); the endpoint underneath is 41/41-verified and the render code is build-clean. Offered to retry.
- **P4/P5/P6 UI browser-DOM pass outstanding** — same harness-stall category, 3rd time; Master substantively verified instead. Offered to retry or Abhishek can click through locally.
- **Dev-DB seed accumulation** — ~26 users / 28 guest_users / 138 audit_log rows of `@example.com` test data from prior Tester cycles; a clean-slate wipe is available on request.
- Next 15→16 major migration + vitest 2→4 major (the only fixes for the remaining non-reachable `npm audit` items) — both deferred pending an explicit go-ahead (see Notes/Risks + Pending Decision 16).
- `lib/storage.ts` has no `deleteObject` capability yet — required net-new for `specs/photo-deletion.md`'s recommended hard-delete default; not a gap in anything already shipped (nothing before this spec ever needed to delete a MinIO object). **UPDATE 2026-07-13: this is now stale — `deleteObject` WAS added (trash-purge path) and `specs/production-upload-batch.md` will add `createMultipartUpload`/`getPresignedUploadPartUrl`/`completeMultipartUpload`/`abortMultipartUpload` on top of it.**

## Pending Decisions Awaiting User Input

**RESOLVED 2026-07-14 — S3 CORS gap.** Abhishek applied option (a) directly to the real bucket (Master doesn't have IAM permissions to do it via the AWS SDK — confirmed `AccessDenied` on even a read-only `GetBucketCors` call with the app's runtime credentials, correctly least-privilege). Re-verified live in-browser immediately after — see top "Last updated" entry. No longer blocking anything.

**NEW 2026-07-13 (even later) — `/settings` page layout (3 lightweight SVG options), blocking ONLY the frontend half of `specs/plan-tiered-upload.md`. Everything else in that spec is built. Awaiting Abhishek's pick via Master before Developer builds the page + the "Settings" topbar nav link on all 8 authed pages.** Records: `design/wireframes/proposals/settings-option-{a,b,c}.svg`.
- **Option A — dedicated `/settings` page, one card, a 3-way segmented control (Free/Pro/Studio) that applies instantly on click.** Matches this app's existing "click = immediate action" precedent for low-stakes toggles (e.g. the guest permission-level dropdown). Lowest friction, smallest amount of new UI state (no separate "Apply" step to forget). Con: no explicit confirm step for what is, after all, a quota/priority change — though it's a no-billing testing control, not a real purchase, so an accidental click is cheap to undo (just click again).
- **Option B — dedicated `/settings` page, 3 stacked radio rows each showing a mini comparison table (files/batch, storage, priority), plus an explicit "Apply plan" button.** More informative at a glance (the numbers are visible without hovering/reading fine print) and the explicit Apply step avoids a mis-click instantly changing state. Con: more vertical space, one extra click for the common case (most testing sessions will want to just try a tier, not deliberate over it).
- **Option C — no dedicated page; a profile-menu dropdown off a new topbar avatar icon.** Smallest build footprint (a menu, not a page) but this app has no avatar/profile-menu affordance anywhere today — building one just to hang this one control off it is new UI on top of new UI, and it doesn't leave room for any future account-level setting to live somewhere findable. Also skips the spec's own ask for a page reachable from the shared topbar nav (same place Trash/Activity/Search live).
- **Developer's recommendation: Option A.** Matches the existing nav precedent (a real page next to Trash/Activity/Search), matches the "testing-only, cheap-to-undo" framing better than an extra confirm step would, and is the least amount of new code for what both the spec and Abhishek's own instructions describe as "a small settings toggle, not a complex page." Option B is the safer runner-up if Abhishek wants the numbers spelled out more explicitly or prefers an explicit-apply step for consistency with other destructive/quota-affecting actions in this app.
- **PICKED 2026-07-14 (Abhishek): a merge of A + B.** Keep A's instant-apply segmented control (no separate "Apply plan" button — click = immediate switch, matching this app's existing low-stakes-toggle precedent) but show each tier's comparison numbers (batch limit / storage / priority) inline on the control itself, per B's information density — e.g. each segment reads "Free — 50/batch · 5GB · Low" rather than a bare label. This is Master's synthesis of the two picks (both options were selected together, not offered as a literal third layout) — flagged explicitly in case Abhishek meant something else; Developer should confirm this reading before or while building if there's any doubt, rather than assume.
- **BUILT 2026-07-14 (Developer), commit `f18d409`.** Developer's read: the A+B merge reconciles cleanly with no leftover ambiguity — A and B differ on exactly two independent axes (instant-apply vs. explicit-button; sparse label vs. dense stats) and the merge just takes one side of each axis, nothing needed inventing. Built exactly as described above; saved as `design/wireframes/settings.svg`. If Abhishek meant something else by "both," flag it and Developer will revise — but no reasonable alternative reading presented itself while building.

**RESOLVED 2026-07-13 (even later, Abhishek) — Plan-tiered upload PTU1/PTU3, both now BUILT exactly as decided (see the top "Last updated" entry for full detail):**
- **PTU1 — RESOLVED: promoted to a real Prisma enum** (`enum Plan { free pro studio }`). Built via a hand-written migration (`prisma migrate dev` refused non-interactively); confirmed lossless (67/67 existing rows were already `"free"`).
- **PTU3 — RESOLVED: downgrade-while-over-the-new-limit is allowed, no guard.** Built + verified live: the switch succeeds, and the existing 413 quota check fires on the very next upload attempt, no new/different code path.

**NEW 2026-07-13 (later still) — Plan-tiered upload PTU2/PTU4/PTU5/PTU6, scoped in `specs/plan-tiered-upload.md`. BUILT on the spec's own recommended defaults; still open for veto (unchanged from before, kept for the record):**
- **PTU1. `User.plan` — promote to a real Prisma enum now, or keep it a plain `String`?** It's been a plain `String @default("free")` since the original schema, read in exactly one place before this spec (`checkGuestLimit`'s `plan !== "free"` string comparison). This spec adds real behavioral branching on its value in ≥4 more call sites. **Recommended: promote to a Prisma enum (`enum Plan { free pro studio }`)** now, while the touch surface is still small — eliminates typo bugs (`"Studio"` vs `"studio"` vs `"premium"`) at both the TypeScript and Postgres-constraint level; the migration is low-risk since every existing row is already exactly `"free"`. Cost: one migration + updating `checkGuestLimit`'s comparison + `lib/plans.ts`'s map types. Veto toward keeping it a plain string if Abhishek would rather not touch the migration/`checkGuestLimit` surface this pass.
- **PTU2. Storage-limit: sync-at-switch (decided) vs. compute-live.** Decided in the spec (sync-at-switch, zero changes to either existing quota-check call site) — flagged here only because Master's brief called it a genuine fork worth surfacing, not because it's still undecided. Veto toward compute-live only if Abhishek anticipates `plan` ever being written by something other than the new switcher (nothing does today).
- **PTU3. Downgrade-while-over-the-new-limit: allowed (current recommendation) or blocked with a 409?** Recommended: allowed, no special guard — matches how being over-quota for any other reason already behaves (413 on the next upload attempt, not an account-level block). Veto toward a 409-block-the-downgrade if Abhishek wants a harder guardrail.
- **PTU4. Exact `PRIORITY_BY_PLAN` integers.** Recommended: `studio: 1, pro: 5, free: 10` (BullMQ: lower = first; spread with headroom for a future tier). Confirm the exact numbers, or state a different scheme.
- **PTU5. Audit the plan switch?** Recommended: yes, a `plan_changed` audit row (`fromPlan`/`toPlan` metadata) — consistent with auditing other account-affecting changes (`account_deleted`) while cosmetic changes (name update) stay unaudited. Veto toward no audit row if this is judged too low-stakes to log (it's a dev-testing control, not a real customer action).
- **PTU6. Rate limiter on `PATCH /api/auth/plan`?** Recommended: none beyond whatever default/global limiter already applies — this isn't a secret-guessing surface like `/change-password`/`/login`. Veto toward adding one anyway purely for consistency with every other account-mutation route in `auth.ts`.

**NEW 2026-07-13 — Production upload batch (PUB1–PUB10), scoped in `specs/production-upload-batch.md`. BUILT (`e86f126`). PUB4/PUB7/PUB10 — the three load-bearing ones — are RESOLVED by Abhishek 2026-07-13 (see below); PUB1/2/3/5/6/8/9 stand on Planner's recommended defaults, still vetoable:**
- **PUB1. `MAX_BATCH_FILES` exact cap.** Recommended: **1000** (covers Abhishek's stated 500-1000 upper bound; per-file 50MB ceiling and the whole-batch quota check already bound total bytes). Confirm the number, or whether it should instead be a total-bytes cap rather than a flat file count. **Superseded in practice by `specs/plan-tiered-upload.md`'s per-plan caps (50/500/1500) — this flat number now only survives as the absolute cross-tier ceiling once that spec builds.**
- **PUB2. Client-side multipart part size.** Recommended: **fixed 8MB parts** (above S3/MinIO's 5MB-minimum-except-last-part rule, small enough for cheap per-part retries). Confirm or propose a different size.
- **PUB3. Presigned part-upload URL TTL.** Recommended: **3600 seconds (1 hour)** — longer than the read-side 60s helpers since a part can legitimately take a while and a full batch's issuance-to-completion window can span many minutes. Veto toward a shorter TTL if exposure window matters more than slow-connection tolerance.
- **PUB4 — RESOLVED (Abhishek, 2026-07-13): (a) a daily BullMQ repeatable job**, same `upsertJobScheduler` pattern the trash-purge job already established. Developer registers it alongside `registerTrashPurgeJob()`.
- **PUB5. No `uploadSessionId` FK on `Photo`.** A completed batch item is a plain `Photo` row with no back-reference to which `UploadSession` it came from — no "show me all photos from batch X" after the fact, only the session's own aggregate counts survive. Recommended: **accept this** (nothing in the product needs per-batch photo grouping today; trivially addable later as an additive column if that changes). Veto if day-one traceability matters.
- **PUB6. When does the `Photo` row get created — at `/initiate` or at `/complete`?** Recommended: **at `/complete`, after successful multipart assembly** — avoids ever having a `pending` row pointing at a MinIO object that doesn't exist yet (a failure mode the existing single-file route has to actively guard against via a try/catch rollback; the batch flow can simply not have that problem). Confirm the ordering.
- **PUB7 — RESOLVED (Abhishek, 2026-07-13): a new companion table**, `UploadSessionFile` (schema now written into the spec) — the per-file bookkeeping `/complete`/`/abort` use to look up `key`/`uploadId` server-side, scoped to session/owner, rather than trusting the client's payload. Schema footprint is two new tables, not one.
- **PUB8. Should tiny batches (1-3 files) keep using the old single-file endpoint from the UI, or does `/upload` always use the batch flow regardless of count?** Recommended: **always the batch flow**, even for 1 file — one frontend code path, not two (the old single-file endpoint stays reachable for API-direct callers/scripts, just not called by the web `/upload` page anymore). Veto toward a size-based split if multipart handshake overhead on tiny uploads is judged not worth it.
- **PUB9. `/complete` call granularity for a 1000-file batch** — one call per file (1000 requests) vs. one call for the whole batch (one huge request/blast-radius) vs. chunked. Recommended: **chunked, e.g. every 50 completed files or every 30 seconds, whichever comes first, plus a final call for the remainder.** Confirm the chunk size or propose different granularity.
- **PUB10 — RESOLVED (Abhishek, 2026-07-13): Uppy.** New dependency (`@uppy/core` + `@uppy/aws-s3-multipart`, approved per the new-dependency house rule) — headless (no `@uppy/dashboard` prebuilt UI required, `/upload` keeps its own page UI) unless Developer judges the prebuilt Dashboard is a net simplification. GoldenRetriever (IndexedDB resume) stays deferred, addable later as a pure plugin addition.

**NEW 2026-07-08 — Photo deletion (PD1–PD7), scoped in `specs/photo-deletion.md`. SUPERSEDED by `specs/trash-system.md`** (see "Ready Spec" above) — kept below for the historical reasoning trail only, do not build against PD1/PD2 as written:
- **PD1 (LOAD-BEARING). Hard delete vs. soft delete.** Hard = DB row + MinIO original/thumbnails gone irreversibly. Soft = a `deletedAt`/status flag, hidden everywhere, no recovery UI this pass. **Recommended: hard delete.** No existing soft-delete pattern anywhere in the schema to extend; a half-built "trash" with no restore surface is worse than none (implies recoverability it can't deliver). Requires a new `deleteObject` in `lib/storage.ts` (doesn't exist today). Veto toward soft-delete if Abhishek wants undo-safety even without a trash UI yet.
- **PD2 (LOAD-BEARING, mirrors P4's F1). Deleting a photo whose folder has a live guest `folder_permission`.** Options: block with 409 (F1's posture, "revoke the share first") or allow it (guest gets an ordinary 404 next time, same as any missing resource). **Recommended: allow — no 409 block.** Explicitly narrower blast-radius than F1: F1 exists because merge/delete acts on an ENTIRE shared scope (privilege escalation or a whole relationship silently severed); one photo inside a shared folder doesn't touch the grant itself and behaves like any other already-removed resource. Veto toward F1-style blocking if Abhishek judges even a single photo disappearing without warning as unacceptable.
- **PD3. The duplicate-chain reference problem.** If original A is deleted and duplicate B points at it (`B.duplicateOfPhotoId = A.id`) — block A's deletion, cascade-null B's reference (recommended — the column has no DB-enforced FK per Day2.md, so this is data hygiene not business logic), or leave it dangling. A `duplicate`-status photo itself deletes freely with no special case (nothing can point at a duplicate, per the dedup invariant).
- **PD4 (hard requirement, not a decision).** `Folder.photoCount` decrement via `serializableTransaction()` on every delete that removes a photo from a real folder — stated for completeness.
- **PD5 (LOAD-BEARING). Bulk-delete endpoint shape.** One `POST /api/photos/bulk-delete` all-or-nothing, vs. partial-success with a per-id result list (recommended — a bad id in the batch shouldn't block the good ones; matches the destructive-but-idempotent-in-intent nature of the operation), vs. no dedicated endpoint (client loops the single-delete N times — rejected, worse UX + N separate transactions). Includes a recommended 100-item cap, no new rate limiter (owner-on-own-data, low abuse risk).
- **PD6. The multi-select UI.** Full propose→pick→build wireframe round (recommended — structurally as significant a new interaction as P4's kebab menu: a new grid-wide select mode + action bar + destructive confirm) vs. a thin add like P5's button. **Does not block backend build either way.**
- **PD7 (hard constraint, not a decision).** Confirm-before-destroy copy must be honest about irreversibility under PD1's hard-delete default — explicitly must NOT reuse P4's reassuring "moves to Unfiled" style language, since this really is permanent. Stated for completeness.

**Carried forward, resolved (for the record):** auth spec decisions 1–5, auth visual layout (Option B), the upload-pipeline's 7 flagged assumptions, reclassification-UI wireframe (Option A), Dashboard page wireframe (Option B) — all confirmed/shipped with no veto. The 10 AI-classification assumptions (1–10), the Next-migration decision (16), the 4 dashboard-stats assumptions (11–15), and the 12 guest-access decisions (G1–G12) were all built on stated defaults with no veto received — treated as implicitly accepted after multiple regression cycles.

**Guest Access UI wireframes (U1/U2/U3) — RESOLVED 2026-07-04, all built + Tester-verified:** U1 `/share` Option B, U2 `/guests` Option C, U3 `/g/[token]` Option B (records under `design/wireframes/`). U3's accepted preview-first privacy trade-off holds (decorative locked tiles pre-approval, no real image/pre-signed URL until a guest session exists).

**Week 11–12 audit+polish (AP1–AP10) — open for veto, built on defaults + Tester-clean.** Summary: AP1 audited actions = sharing/access surface only; AP3 denormalized `owner_id` on `audit_log`; AP4 log guest `photo_viewed`; AP5 fire-and-forget post-commit writes; AP2 keep-forever retention; AP6 filters = action+actorType+date-range; AP10 Helmet CSP dev-relaxed. **AP7/AP8/AP9 (folder rename-merge-delete / bulk download-all / basic search) were deferred to their own spec — now delivered as `specs/folder-mgmt-download-search.md` (the F/Z/S batch below supersedes them).**

**P7 owner activity-log viewer — PICKED 2026-07-06: Option A, a dedicated `/activity` page** (record: `design/wireframes/audit-viewer.svg`). BUILT (`238a3ee`/`78cff9d`/`c3db148`); substantively verified.

---

**the deferred P4/P5/P6 backlog is now scoped in `specs/folder-mgmt-download-search.md`, with per-part recommended defaults (namespaced F/Z/S) — BUILT (see above), decisions remain open for veto.**

*P4 — folder rename / merge / delete (namespace F):*
- **F1 (LOAD-BEARING — security/correctness). A live guest share on a folder being merged-away or deleted.** A guest granted `folder_permission` on folder A: if A merges into B, A's photos now live in B (never shared); if A is deleted, photos move to Unfiled. **Recommended: BLOCK the merge/delete with 409 while any live permission points at the folder** ("revoke the share first"). Rejected alternatives: silently migrating the grant to B is a privilege escalation (B may hold private photos); silently revoking severs an active client without the owner realizing. Veto toward silent-revoke-with-a-warning if preferred.
- **F2. Delete → what happens to the folder's photos.** Recommended: **move to Unfiled (`folderId = null`)**, NOT cascade-delete photos/MinIO objects, NOT refuse-if-non-empty. Veto toward cascade-delete only if "delete folder = delete photos" is the intended semantic (then also spec MinIO cleanup + a confirm guard).
- **F3. Merge across collections.** Recommended: **require same collection** (400 otherwise) — matches the single-default-collection reality + clean `@@unique` semantics.
- **F4. Audit rename/merge/delete?** Recommended: **audit merge + delete** (new `folder_merged`/`folder_deleted` action strings — additive, no schema change) since they're destructive + interact with sharing; **do NOT audit plain rename** (cosmetic, high-frequency, matches the AP1 owner-content exclusion).
- **F5. AI-generated vs custom folders.** Recommended: **allow rename/merge/delete on BOTH types** (folders are folders); accept that the worker may re-create an AI folder by its category name on the next matching upload (benign). Veto to restrict ops to `custom` folders (AI folders immutable, 409/403 on attempt).
- **F6. Folder "reorder" (roadmap § 11 mentions it on the PATCH).** Recommended: **out of scope this pass** (no `sortOrder` column; would need an additive migration + drag UI). `PATCH /api/folders/:id` = rename only.

*P5 — bulk download-all / folder zip (namespace Z):*
- **Z3 (LOAD-BEARING — the P5 architecture decision). Inline-stream vs. BullMQ job.** Recommended: **inline-stream this pass** (archiver piped to the HTTP response, on-the-fly from MinIO reads, no temp file) **with a guard cap — >500 photos (or an est-bytes threshold) → 409 "too large, narrow it down"**; async (a stored zip built by a job + a pre-signed URL when ready) DEFERRED until real usage hits the cap. Veto toward async-from-the-start if large-folder support is a hard MVP requirement (bigger spec: a `zip_jobs` surface + cleanup).
- **Z1 (LOAD-BEARING — permission intent). Guest zip permission level.** Recommended: **the guest zip endpoint requires `download_all`**; a `download`-only guest gets 403 (this is what the distinct level is for). Veto to treat `download` as sufficient (then `download_all` becomes vestigial).
- **Z2. New dependency `archiver`.** Recommended: **add it** (pure Node, stream-oriented, standard). Flagged per the new-dep rule.
- **Z4. Which photos go in the zip.** Recommended: only photos with a stored original; **skip `failed` (no original) and `duplicate` (same bytes as the original already included)**; include `done`/uncategorized.
- **Z5. Mid-stream read failure.** Recommended: **abort the archive + destroy the response** (client gets a failed download, we log it) rather than a 200 that silently omits photos.
- **Z6. Empty folder.** Recommended: **400 "no downloadable photos"** before streaming (not a valid-but-empty zip).
- **Z7. Audit download-all?** Recommended: **audit the GUEST zip** (`folder_downloaded`, actorType guest — the "who downloaded" differentiator); **not** the owner's own zip (owner-on-own-data, per AP1).

*P6 — basic search (namespace S):*
- **S1 (LOAD-BEARING — scope + implementation). Is AI-label/category search in scope, and which column.** Recommended: **include a `category` filter, implemented as matching the photo's folder name** (AI folders ARE named for their category) — no new column/index. **Raw-label free-text search DEFERRED** (labels aren't in a queryable per-photo column; would need more infra). Veto to drop category entirely, or to include raw-label search now.
- **S2 (LOAD-BEARING — which date). Date-range filter axis.** Recommended: **filter on `createdAt` (upload time)** this pass — always present; EXIF `takenAt` can be null and mixing a coalesce complicates the SQL. EXIF-capture-date search noted as a clean follow-up.
- **S3. `folderId` filter (not-owned + Unfiled).** Recommended: **not-owned → 404** (house rule); **`folderId=unfiled` → `folderId = null` photos**.
- **S4. SQL `ILIKE` vs full-text infra.** Recommended: **plain SQL** (Prisma `contains`/insensitive + range/equality) — fits the local Postgres slice, zero new infra. `tsvector`/`pg_trgm`/a search service DEFERRED.
- **S5. New index for search?** Recommended: **none this pass** (leading-wildcard `ILIKE` can't use a btree anyway; other filters ride existing columns). Add later if profiling shows a slow query.
- **S6. Empty query behavior.** Recommended: **return the owner's whole library newest-first, paginated** (no-filter search = browse-all). Veto to require ≥1 filter (400 otherwise).
- **S7. Audit search?** Recommended: **no** (owner-on-own-data read, per AP1). Veto to log searches (low value, high volume).

**UI wireframe rounds queued (P5 is a thin button, all others need a full round):**
- **P4 — NEEDS a propose→pick→build round** on `/organize`: the rename affordance, the merge picker + confirm (the real interaction — pick source, pick destination, confirm the consequence, handle the F1 "shared — revoke first" 409 + F2 "photos move to Unfiled" delete-confirm messaging). **BUILT** (`ac1da6a` picked Option A kebab, `33731cb` shipped it).
- **P5 — THIN add, likely no full round:** a "Download all" button on owner folder views (`/browse`/`/organize`) and on the guest portal `/g/[token]` (shown only when the guest holds `download_all` per Z1). **BUILT** as a thin button, no round needed.
- **P6 — NEEDS a propose→pick→build round:** a new search-bar + results information surface. **BUILT** (`ac1da6a` picked Option A dedicated `/search` page, `33731cb` shipped it).
- **UPDATED — Trash page + multi-select delete (was PD6, now per `specs/trash-system.md`'s closing recommendation):** queued, not yet proposed. Recommended ONE combined propose→pick→build round covering BOTH a new Trash page (list, Recover/Delete-forever per item, Empty-trash button) AND `/organize`/`/browse`'s multi-select-delete (select-mode toggle, checkboxes, "Delete selected (N)" action bar) — designed together so the confirm-dialog copy ("moved to Trash, recoverable for 7 days" — note this is now the OPPOSITE of PD7's original "permanent, cannot be recovered" framing) and the Trash page itself don't contradict each other in tone. Backend (built 2026-07-08) does not need to wait for this. **STATUS: BUILT** (`861ff25`) — see above, superseding this queue entry.
- **NEW 2026-07-13 — `production-upload-batch`'s frontend half will need Abhishek's PUB10 call (Uppy vs. hand-rolled) before Developer builds it, but does NOT need a wireframe/SVG round** — this extends `/upload`'s existing per-file progress-list UI with batch-level progress, not a new visual surface.
- **UPDATED 2026-07-13 (even later) — `plan-tiered-upload`'s new `/settings` page wireframe round is PROPOSED, awaiting Abhishek's pick (see Pending Decisions above).** 3 lightweight SVG options in `design/wireframes/proposals/settings-option-{a,b,c}.svg`, Developer recommends Option A. Everything else in the spec is built; this is the only remaining blocker.

## Next Scheduled Actions
- 06:00 / 18:00 daily — Tester Agent regression passes (still not cron-automated in this environment). A belt-and-suspenders browser-DOM pass of the P7 `/activity` page AND the P4/P5/P6 UIs are the two outstanding Tester items on already-built work, alongside a full regression pass on the trash system (backend + UI, neither yet Tester-verified) and a first pass on `production-upload-batch` (built, not yet Tester-verified).
- **UPDATED 2026-07-13 (even later): `specs/plan-tiered-upload.md`'s backend is BUILT (289/289 suite green).** The one remaining step is Abhishek's pick on the `/settings` wireframe round (see Pending Decisions above) — Developer will build the page + the "Settings" nav link the moment that's picked.
- **New 2026-07-13: a Tester pass on `specs/production-upload-batch.md`** (built, `e86f126`) — the storage-layer/schema/backend/frontend are all done; a 500+ file batch + network-inspection + corrupted-file partial-success + abort-then-verify-MinIO-state run per the spec's own Success Signal is the next step.
- **New (2026-07-09, Abhishek's idea, accepted): a product-metrics dashboard.** Abhishek proposed tracking, from day one: visitor→signup conversion, signup→first-upload conversion, time-to-first-upload, photos-uploaded-per-user, albums-shared-per-user, guest-invite-acceptance-rate, Day 1/7/30 retention, Free→Pro conversion, CAC. Master's assessment (agreed by Abhishek): split this into two phases rather than building it all at once —
  - **Phase A — buildable now, cheap:** time-to-first-upload, photos-per-user, albums-shared-per-user, and guest-invite-acceptance-rate (this last one is nearly free — `access_requests`' created→approved/denied/expired transitions, plus the new `access_request_touches` link-forwarding data from bug #10, already carry everything needed). Recommend this largely rides on the EXISTING audit log (`lib/audit.ts`) as aggregate queries — no new tracking system, just a reporting layer over data already being written.
  - **Phase B — DEFERRED, not buildable yet, missing prerequisites:** visitor→signup conversion (no marketing site/traffic exists yet — just a login page), Day 1/7/30 retention (no real multi-day user base to measure), Free→Pro conversion (Stripe/billing is explicitly out of scope per CLAUDE.md/roadmap until approved — there's no real "Pro" conversion event to measure yet, even though `plan-tiered-upload` now gives the ACCOUNT a `pro` value, no payment ever triggers it), CAC (no ads running). Revisit once auth/billing/marketing actually exist.
  - **Needs a Planner spec before building** (where do the aggregates live — a new `/api/metrics` owner-facing endpoint? a page? is this owner-facing, i.e. Abhishek's own usage, or meant to generalize to future multi-tenant use? none of that is decided yet — flagging per CLAUDE.md's "ambiguity goes to a pending decision, not into code").
- Other standing items, each needing explicit go-ahead: the **Next 15→16 major migration** + **vitest 2→4 major** (Pending Decision 16 + Notes/Risks — the only fixes for the remaining non-reachable `npm audit` items); a **dev-DB clean-slate wipe** (test-seed accumulation); and **whether/when to push** any future work (feature branch only, no Claude trailer, fresh go-ahead each time).

## Notes / Risks
- **Mobile wrapper (Option 1 of Abhishek's phased mobile plan) — scaffolded 2026-07-09, NOT yet built/tested.** Abhishek wants to test a wrapped mobile app before committing to a real native rewrite (Option 2). Capacitor added to `frontend/` (`@capacitor/core`, `@capacitor/cli`, `@capacitor/ios`, `@capacitor/android` as devDependencies — no new npm audit findings beyond the already-documented/deferred Next/glob ones). `frontend/capacitor.config.ts` points the WebView at the Mac's LAN dev server (`http://10.10.144.146:3000`) rather than a static export — deliberate, since this app has a dynamic `/g/[token]` route that static export can't pre-render, and there are no Next API routes/middleware to lose by not exporting. Backend CORS (`src/app.ts`) now accepts a second origin via `MOBILE_DEV_ORIGIN` (LAN IP:3000) additively, alongside the existing `FRONTEND_ORIGIN` (localhost:3000) — both `.env` files updated. Frontend dev server now started with `-H 0.0.0.0` so it's reachable from the phone's WiFi, not just this Mac. **Blocked on Abhishek installing Xcode himself** (App Store, ~15GB, requires his Apple ID sign-in — not scriptable) since he's testing on an iPhone; Android Studio was NOT installed (he only has an iPhone to test with, so Android was deprioritized). Once Xcode is installed: `npx cap add ios` inside `frontend/`, then open the generated Xcode project and run it on his device (free personal Apple ID provisioning is enough for local device testing, no paid Developer account needed yet). **Hard caveat already explained to Abhishek: this is LAN-only testing, not a real deployment** — the app only works while his Mac is on, `npm run dev` is running, and his phone is on the same WiFi (unrelated to the Vercel deployment below — the mobile wrapper still points at the local dev server, not the deployed frontend). ~~No public hosting exists for this app yet.~~ **CORRECTED 2026-07-14 (Abhishek): this is stale.** A real frontend deployment exists at `https://photo-sphere-ai-frontend.vercel.app` — Abhishek confirmed it's "basically working" and is the live/production version. **Current local work (this branch, `feature/ai-classification`) is upgrading and testing ahead of that deployment** — the batch-upload + plan-tiered-upload features built this session exist only locally/unpushed so far, not yet on Vercel. Backend deployment target/URL for that Vercel frontend not yet confirmed with Abhishek (unclear if it's Railway, a separate deploy, or something else) — flagging rather than guessing; ask before assuming what backend it talks to if that becomes relevant.
- **GitHub remote is connected; `feature/ai-classification` is pushed through `37cfd14`** (18 commits — the 3 frontend fixes + the full Trash system backend+UI — pushed 2026-07-08 on Abhishek's go-ahead, fast-forward, feature branch only, no PR, no `Co-Authored-By: Claude` trailer). **Standing push rules:** feature branch only, NEVER master (the remote has no master/main — default is `feature/upload-pipeline`), no Claude co-author trailers, fresh go-ahead each time.
- **A separate `feature/local-dashboard` branch exists and is pushed**, wiring a personal local-only "insights dashboard" tool into the loop (an untracked `dashboard/` folder, gitignored by design). Not merged anywhere — don't act on `dashboard/FEATURE_QUEUE.md` mentions unless it's actually checked out/merged.
- **Runtime state:** backend `:4000` runs `NODE_ENV=development` with `NOTIFICATIONS_EXPOSE_OTP="true"` (mock OTP readable via the flag-gated dev endpoint for local testing — keep "false" outside local dev/test). `.env` is gitignored; on a fresh checkout set `GUEST_SESSION_TTL_HOURS` (default 24) + `NOTIFICATIONS_EXPOSE_OTP`. Frontend `:3000` + the worker run detached via `nohup` (won't survive a reboot; `curl localhost:4000/health` before assuming they're up). Process tree verified clean 2026-07-06 (one server watcher, one worker watcher, one `next dev`). Docker Compose (Postgres/Redis/MinIO) stable/healthy (multi-day uptime). Live verification passes tend to exhaust the signup/login IP rate-limit buckets — expect a Tester pass to seed via Prisma or wait out the 15-min window.
- **`npm audit` posture (do NOT panic at "1 critical / 5 high"):** none is reachable in this local-first slice. The `next` highs + transitive `eslint-config-next`/`glob` only clear via the Next 15→16 major migration (deferred); the `vitest` "critical" requires the Vitest UI server (`@vitest/ui` NOT installed, no `--ui` used) so it's inert here, and only clears via a vitest 2→4 double-major. Both breaking-major upgrades stay deferred pending an explicit go-ahead. `next` pinned at `14.2.35`.
- Real AWS/GCP services (S3, Vision API, EKS, Terraform, Stripe) remain fully deferred — the swap-in seams (classification provider, notification provider, storage) are kept one-file so "swap in real X" stays a contained change whenever approved. **P5's zip must stay local (stream from MinIO) — no hosted zip service.** **`specs/production-upload-batch.md`'s multipart wrappers follow the SAME `lib/storage.ts` swap seam — no code change needed to point them at real S3/R2 later.** **`specs/plan-tiered-upload.md`'s plan switcher is explicitly NOT a billing surface — no Stripe, no payment step, per CLAUDE.md's ground rules — it only writes `user.plan`/`user.storageLimitBytes` directly, clearly labeled a testing control.**
- UI design decisions stay SVG-only, no Figma. Committed design records: `design/wireframes/auth.svg`, `reclassify-ui.svg`, `dashboard.svg`, `share-panel.svg`, `guest-management.svg`, `guest-portal.svg`, `audit-viewer.svg`, `folder-mgmt.svg`, `search.svg` (proposals retained under `design/wireframes/proposals/`). **Photo-deletion's multi-select UI (PD6) shipped as the Trash system's UI round.** `production-upload-batch`'s frontend does NOT need a new SVG round (extends the existing `/upload` progress-list UI). **`plan-tiered-upload`'s new `/settings` page DOES need one — no settings/account surface exists yet.**
- **On reduced-review builds / harness stalls:** Developer sessions have been interrupted by usage limits and Tester passes have stalled inside their own browser-automation harness (not app defects) multiple times. Each time, Master independently re-verified (typecheck/lint/tests + diff/DB inspection) before trusting. A recurring operating condition; hasn't cost correctness so far.
- MR drafts: `reports/mr-drafts/feature-ai-classification.md`, `dashboard-stats-and-upload-polish.md`, `organize-reclassification-ui.md`, `browse-and-photo-viewer.md`, `dashboard-page.md`, `guest-access-otp.md`, `guest-access-ui.md`, `audit-and-polish.md`, `audit-viewer-ui.md`, `folder-mgmt.md`, `bulk-zip-download.md`, `folder-search-ui.md`, `consistent-topbar-nav.md`, `unfiled-manual-move-fix.md`, `multi-file-upload.md`, `trash-system.md`.
- **`feature/ai-classification` now holds 5 unpushed commits** (`1b7f17e` spec revision, `6443d87` backend fix, `ccaa429` docs, `9aa2129` frontend fix, `ea280ca` docs) on top of the previously-pushed `37cfd14` — a fresh explicit push go-ahead is needed, per the standing push rules.
- **Photo-restore-cascades-folder fix — Master independently live-verified end-to-end 2026-07-09, closing the gap the Developer builds flagged (backend's own tests covered it in isolation; frontend's live-check was blocked because no backend was listening on :4000 during that session).** With backend/worker/frontend all confirmed running, Master ran real HTTP+DB checks against the exact scenario Abhishek reported (delete a photo, then delete its folder too, then restore just the photo): **(1)** no live same-named folder exists → the photo is auto-restored into a brand-new folder (confirmed different `folderId` from the original) with the original name, and the original trashed folder is confirmed STILL trashed and untouched; **(2)** a live same-named folder exists, no `onConflict` → confirmed the exact `409 {error:"conflict", conflictingFolderId, conflictingFolderName}` shape the frontend's `PhotoCollisionPanel` is coded to parse; **(3)** `onConflict:"existing"` → confirmed the photo lands in the pre-existing live folder (`folderId` match verified) and the original trashed folder remains untouched. All three scenarios behave exactly as designed; the bug Abhishek reported (recovering a photo used to bring back the ENTIRE trashed folder) is confirmed fixed. Throwaway verification scripts + seed data deleted after, 0 residual rows.
- **A large amount of untracked backend audit/refactor work exists in the working tree as of 2026-07-13** (`BACKEND_AUDIT.md`, `AUDIT_FIXES_TRACKER.md`, `SCALABILITY_ROADMAP.md`, new `lib/logger.ts`/`lib/plans.ts`/`lib/validateEnv.ts`, new migrations for detection-cache/person-faces/password-reset-tokens, modifications across most of `backend/src`) — none of this was touched by this Planner pass; flagging its presence so Master/Developer don't lose track of it when reconciling `git status` alongside the new upload-batch spec work. **This Planner pass (`plan-tiered-upload.md`) only wrote the spec file + this STATUS.md update — no application code touched, consistent with the Planner role.**
