---
description: Invoke the planner subagent to scope the next roadmap item into a spec before building
---

Invoke the **planner** subagent (via the Task tool) per its role in `agents/PLANNER.md`, to turn the next unchecked roadmap item (or the current gap identified in `agents/STATUS.md`) into a spec in `specs/`. When it's done, report back which spec is ready for the developer agent, and surface any open questions to Abhishek.
