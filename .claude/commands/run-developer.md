---
description: Invoke the developer subagent directly against the latest tester report or the current sprint goal
---

Invoke the **developer** subagent (via the Task tool) per its role in `agents/DEVELOPER.md`, to act on the most recent file in `reports/` (if one exists and hasn't been actioned per `agents/STATUS.md`), otherwise to continue the current sprint goal from `agents/STATUS.md`. When it's done, report back what changed, what's blocked, and surface any pending UI decisions to Abhishek.
