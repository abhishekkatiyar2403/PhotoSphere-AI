---
description: Invoke the tester subagent directly for a one-off regression pass, outside the full orchestration cycle
---

Invoke the **tester** subagent (via the Task tool) to run a full regression pass right now, per its role in `agents/TESTER.md`. When it's done, report the one-line pass/fail summary back to Abhishek and note the report's file path.
